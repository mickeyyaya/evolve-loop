# Runtime Reference (full detail)

> Moved verbatim out of the root [CLAUDE.md](../../CLAUDE.md) on 2026-06-05 to keep it under the 40k-char context limit. CLAUDE.md carries the digest; **this file is the canonical full detail** for the env-var table, operator commands, ship classes, and the publishing pipeline. The docs-contract test (`go/cmd/evolve/docs_contract_test.go`) scans BOTH files, so new `EVOLVE_*` vars may be documented here.

## Autonomous execution (bypass-permissions / yolo / auto-approve) — full text

When operating in autonomous mode, AGENTS.md Rule 4 ("stop and ask") is overridden. All 8 items below remain **mandatory** — bypass means "don't ask the user," not "skip integrity checks."

1. **Continue all cycles without pausing.** Never output "should I continue?" between cycles. Complete every requested cycle end-to-end.
2. **Run the FULL pipeline every cycle.** Launch Scout, Builder, Auditor for every cycle. No inline edits skipping agents, no batch-claiming multiple cycles. Each cycle must produce `scout-report.md`, `build-report.md`, `audit-report.md`.
3. **The phase gate runs at every transition.** Phase ordering is enforced natively by the Go orchestrator (`go/internal/core`) and the `evolve guard phase` kernel hook; it is non-negotiable in bypass mode.
4. **Never fabricate cycles.** Every cycle number must correspond to real Scout → Build → Audit → Ship → Learn execution. Claiming a number without doing the work is a CRITICAL violation.
5. **Phase agents MUST be invoked through the native bridge (`evolve subagent run`, or the in-process `go/internal/bridge` launcher driven by `evolve loop`/`evolve cycle run`).** In-process `Agent` is denied by `orchestrator.json:disallowed_tools` AND by the `evolve guard phase` precondition whenever `cycle-state.json` exists. No bypass. The runner enforces per-agent CLI permission profiles in `.evolve/profiles/`, generates a challenge token, and writes a tamper-evident ledger entry.
6. **OS-level sandboxing wraps every claude subprocess.** `EVOLVE_SANDBOX` takes `auto` (default) | `on` | `off` — not `0/1` (see the env-var table below). When active, the runner wraps `claude -p` in `sandbox-exec` (macOS) / `bwrap` (Linux); `auto` disables it only when nested-Claude is detected. `EVOLVE_SANDBOX_FALLBACK_ON_EPERM=1` is REQUIRED for nested-claude (auto-enabled by `evolve loop` via `evolve detect-nested-claude` when nesting is detected). Auditor/Evaluator profiles run `read_only_repo: true`.
7. **Eval-quality pre-flight on every eval.** The discover→build gate runs the native eval-quality check (`evolve eval quality-check`) against each new eval. A tautological / low-kill-rate eval is flagged. Promotion path: WARN-only → fail-gate after one verification cycle.
8. **Adversarial Auditor mode is default-on.** Runner prepends "ADVERSARIAL AUDIT MODE" framing requiring positive evidence for PASS. Auditor defaults to Opus (different family from Builder's Sonnet) to break same-model-judge sycophancy. Disable only with `ADVERSARIAL_AUDIT=0`.

**The rule is: maximum velocity, zero shortcuts.** Go fast by being efficient, not by skipping steps.

**Runtime constraints.** Per-cycle git worktrees are provisioned natively by the orchestrator (`go/internal/core`; manage manually with `evolve worktree create|list|cleanup`), recorded in `cycle-state.json:active_worktree`. Orchestrator and phase agents MAY NOT call `git worktree add/remove` (denied by profiles). The native failure-adapter (`go/internal/core`) computes a deterministic PROCEED/RETRY/BLOCK from `state.json:failedApproaches[]` — the orchestrator follows it verbatim. Ledger hash-chain via `prev_hash` + `entry_seq`; verify with `evolve ledger verify`. The native ship phase advances `state.json:lastCycleNumber` after a successful ship.

**SSE / streaming endpoints**: disable middleware buffering, add explicit timeouts, provide a cancel-UI button. Don't rely on browser-side timeout alone.

## Shell & environment conventions (full table)

Shell scripts target **bash 3.2** (macOS default). Banned/required:

| Status | Pattern | Reason |
|---|---|---|
| Banned | `declare -A` | bash 4+ |
| Banned | `mapfile` / `readarray` | bash 4+ |
| Banned | `${var^^}` / `${var,,}` | bash 4+ |
| Banned | `sed -i ''` | BSD-incompatible — use `> "${file}.tmp.$$" && mv` |
| Banned | `date -d` | GNU-only — use `date -u -j -f` on macOS; fallback chain `gdate || date -d || date -j -f` |
| Required | `set -uo pipefail` | NOT `set -e` for orchestrator scripts capturing sub-script exit codes |
| Required | Atomic writes via `mv` of `${file}.tmp.$$` | Crash-safe |
| Required | `git diff HEAD` for tree-state SHA | Match audit-binding model (untracked files don't count) |

`skills/<name>/` is canonical; `.agents/skills/<name>/` are symlinks → `../../skills/<name>/` for cross-CLI auto-discovery. Git tracks content at the canonical path.

## Current behavior (env-var reference)

Defaults reflect production posture on current main (release line v18.12.0). Detail docs linked per row.

| Subsystem | Env var | Default | Effect / reference |
|---|---|---|---|
| EGPS gate | `acs-verdict.json` | enforced | Cycle ships only if `red_count == 0`. WARN level removed in v10.0.0. See [docs/architecture/egps-v10.md](../architecture/egps-v10.md). |
| EGPS Tester | `EVOLVE_TEST_PHASE_ENABLED` | `1` (default-on) | When `1`, TDD-Engineer writes behavioral predicates before Builder; Tester validates after. When `0`, Builder writes own predicates (v10.1 fallback, degrades quality). Flipped default in cycle-86 (predicate-quality Layer 4). |
| Structural eval gates | `policy.gates.eval_gate` | `enforce` (default-on) | Two structural inter-phase gates mounted at the per-phase `DeliverableReviewer` seam (`internal/evalgate`): **Gate A** (after scout) — every slug scout SELECTED in `scout-report.md` must have a real `.evolve/evals/<slug>.md` file (cycle-166); **Gate B** (after tdd) — the selected slugs' eval predicates must not be tautological no-ops, via `internal/evalqualitycheck` (cycle-204). `off`/`shadow`/`enforce`. At `enforce` a CERTAIN violation aborts the cycle; the gates FAIL OPEN on any ambiguity (parse failure, zero slugs, advisory WARN, unreadable file), so enforce-default never false-blocks a healthy cycle. `off` = the `noopReviewer` default. |
| Deliverable contract gate | `policy.gates.contract_gate` | `enforce` (default-on, ADR-0034) | The deliverable-contract gate (`internal/deliverable`), chained AFTER evalgate at the same `DeliverableReviewer` seam via `core.ChainReviewers`. Runs the SAME verifier as the agent-callable `evolve phase verify` self-check, so the agent's pre-finish check and the host gate can't drift. Checks WELL-FORMEDNESS ONLY (deliverable exists at the contracted path from `phasecontract`, required sections + parseable verdict for markdown, valid JSON + required keys via a tolerant reader for `routing-plan.json`/`cycle-state.json`, not stray in the worktree) — semantic correctness stays the auditor's job (anti-Goodhart). `off`/`shadow`/`enforce`. **Fail-open on ambiguity** (unknown phase, unreadable dir), **fail-closed on a confirmed violation** at enforce. A **circuit breaker** trips on contract/quality violations (not exit codes): after N consecutive blocks (default 3) it demotes `enforce`→advisory and logs `CIRCUIT OPEN` so a miscalibrated gate cannot brick the loop (state in `.evolve/contract-gate-breaker.json`; a clean cycle resets it). Also drives the cache-safe `## Deliverable Contract` prompt block + exact-path footer the bridge injects. `off` = the `noopReviewer` default. See [docs/architecture/deliverable-contract.md](../architecture/deliverable-contract.md). |
| Contract correction retry | `EVOLVE_CONTRACT_CORRECTION_RETRIES` | `2` (default-on) | On a deliverable-contract violation (DeliverableReviewer `Approve=false`) the orchestrator re-dispatches the phase with the violation text injected as a `## Correction` prompt block (`injectCorrectionPrefix`), up to N times, before aborting the cycle (today's behavior is the floor). `0` = immediate abort (byte-identical pre-feature). Separate from `EVOLVE_PHASE_MAX_ATTEMPTS` (bridge-timeout retries). Within-phase (no new cycle numbers); each correction logs a `contract_correction` ledger entry. Scope: well-formedness only — semantic/audit-FAIL is out. Impl: `orchestrator.go` correction loop + `core.PhaseRequest/BridgeRequest.CorrectionDirective`. |
| Phase-B observability | `EVOLVE_TRACKER_ENABLED` | `0` (opt-in) | When `1`, replays NDJSON via `tracker-writer.sh` post-phase. v10.6+ candidate flips to default-on. See [docs/architecture/phase-tracker.md](../architecture/phase-tracker.md). |
| Triage phase | `EVOLVE_TRIAGE_DISABLE` | `0` (Triage on) | Runs Layer C Triage on every cycle. Soft WARN if skipped without override. |
| Build planner | `EVOLVE_BUILD_PLANNER` | 1 (advisory, v12.3+) | Opt C build-planner phase (wired v10.19; cycle-103). `0` = shadow (no-op); `1` = advisory (build-plan.md produced). 3-cycle rollout: shadow→advisory→enforce. Revert: `EVOLVE_BUILD_PLANNER=0`. See [docs/architecture/adr/0019-build-planner-phase.md](../architecture/adr/0019-build-planner-phase.md). |
| Reflection journal | `EVOLVE_REFLECTION_JOURNAL` | `1` (advisory v10.20) | Per-phase Reflection Journal: each phase agent appends `## Reflection` section + `<phase>-reflection.yaml` sidecar. Reflector (every cycle) + Retrospective/Memo consume YAML. v10.20 = advisory (missing YAML → WARN); v10.21 = enforce (phase-gate blocks missing YAML). Revert: `EVOLVE_REFLECTION_JOURNAL=0`. See [docs/architecture/reflection-journal.md](../architecture/reflection-journal.md) and [docs/architecture/learn-phase.md](../architecture/learn-phase.md). |
| Auto-retrospective | `EVOLVE_DISABLE_AUTO_RETROSPECTIVE` | `0` (on) | **DEPRECATED (ADR-0039, honored one more release; `config.Load` WARNs `deprecated-flag` when set)** — use `policy.json:failure_floor`, which wins when both are set. FAIL/WARN cycles auto-run retrospective + merge-lesson. Cost ~$0.30-0.50/cycle. |
| Failure floor | `.evolve/policy.json:failure_floor` | `{always_learn: true, audit_fail_routes_to: "retrospective"}` | The ONE user surface for failure-learning policy (ADR-0039). `audit_fail_routes_to` ∈ {`retrospective`, `memo`} picks the learning phase after an audit FAIL (router Rule 1, ahead of the deprecated enable-chain); `always_learn: false` downgrades the DEFAULT route to memo (an explicit `retrospective` wins). Unknown values fall back to the default — SOME learning phase always routes. The deterministic floor (FailedRecord + retrospective/lesson artifacts on EVERY abnormal termination — retro-bridge death, `cycle reset`, loop fatal) is NON-configurable, like the integrity floor. |
| Fan-out | `EVOLVE_FANOUT_ENABLED` | `0` (off) | Parallel sub-personas. Concurrency cap `EVOLVE_FANOUT_CONCURRENCY=2`. (The former per-worker cost cap `EVOLVE_FANOUT_PER_WORKER_BUDGET_USD` was removed with the token-budget cost gates.) Builder/Intent/Orchestrator/TDD excluded by `parallel_eligible` (single-writer invariant). See [docs/architecture/sequential-write-discipline.md](../architecture/sequential-write-discipline.md). |
| Plan review | `EVOLVE_PLAN_REVIEW` | `0` (off) | Four-lens review (CEO/Eng/Design/Security) between discover and build. Verdict gates via `gate_plan_review_to_tdd`. |
| Strict audit | `.evolve/policy.json:workflow.strict_audit` | `false` (fluent) | WARN ships by default (v8.28.0+). Set `true` to block ship on WARN (audit + ship phases) and restore failure-adapter legacy block-on-recurring. Migrated from the `EVOLVE_STRICT_AUDIT` env dial (flag-reduction, ADR-0064). (Note: WARN removed in v10.0.0 — applies to pre-EGPS bootstrap cycles only.) |
| Fleet planning | `.evolve/policy.json:fleet` | `{count: 1, concurrency: 1, plan_source: "triage"}` | S1+S2 of the FLEET-AS-POLICY goal: the multi-cycle fleet planning config surface (`count` lanes, per-lane `concurrency`, todo `plan_source`). Absent block ⇒ `count=1`, `concurrency=1` — byte-identical sequential single-cycle execution (today's behavior). `count>1` with the resolved `plan_source=triage` ⇒ `evolve loop` runs native WAVES: each batch iteration produces one single-writer triage plan, fans it out (`fleet.PlanFromTriage`) to up to `count` disjoint-file lanes launched through the same `fleet.Supervisor` + exec launcher `evolve fleet` uses (lanes inherit `EVOLVE_FLEET=1`/`EVOLVE_FLEET_SCOPE`), and ships serialize on the existing `.evolve/ship.lock`; `--max-cycles` counts waves, not lanes. `fleet.PlanFromTriage` reads a wave's `committed_floors`, falling back to the committed cards' target packages, then to the decision's `top_n[].id` cards when both are absent (real triage decisions commonly declare no `committed_floors` at all) — and a wave whose plan commits zero lanes never consumes an iteration: it WARNs and falls through to the sequential path instead (D1 empty-plan guard). A non-positive `count` clamps to 1 (never a zero/negative-lane wave); a non-positive `concurrency` follows the resolved `count`. `plan_source` closed vocabulary: see [control-flags.md](../architecture/control-flags.md) for the `triage`/`manual` fail-safe table. S1+S2+S3 covers the FLEET-AS-POLICY goal through cycle 467. S3 guards: before planning, each wave runs a dirty-control-plane preflight — `fleet.PreflightControlPlane` against the MAIN checkout refuses the wave when any uncommitted change (modified tracked file or untracked addition) touches `guards.IsProtectedSurface`, naming the file(s) and the `evolve ship --class manual` remediation, and the iteration WARNs + falls back to sequential (the fleet-trial-#1 class: a dirty `.evolve/policy.json` killing an audit-PASSED lane at ship). Active `clihealth` quota benches shrink the wave's effective lane count via `fleet.QuotaAwareCount` (one WARN per benched family naming family + bench reason; clamped at 1 — a fully-benched fleet degrades to the sequential path, never zero lanes). The loop's cancellable ctx also threads through the plan path (`wavePlanFn` takes ctx; PR #298 reviewer note), so shutdown cancels wave planning, not just launched lanes. Zero new `EVOLVE_FLEET_*` env vars — config-only. |
| Budget — cost gates (REMOVED) | `EVOLVE_MAX_BUDGET_USD`, `EVOLVE_BATCH_BUDGET_CAP`, `EVOLVE_BUILDER_COST_THRESHOLD`, `EVOLVE_BUILDER_COST_GUARD_STRICT`, `--budget-usd N` / `--budget` / `--batch-cap-usd` | n/a (removed) | **Removed.** Every cost gate — loop stop, phase FAIL, batch cap, checkpoint-by-cost — was removed; cost is now **display-only telemetry** (`total_cost_usd`, per-phase `cost_usd`), tracked per-cycle across LLM CLIs but never used as a cap input. The cost-budget **CLI flags are no longer parameters**: `loop`/`cycle run` strip any legacy `--budget-usd`/`--budget`/`--batch-cap-usd` before parse and emit a one-line WARN (graceful, so old scripts don't error) — they are absent from `--help`. Use `--cycles N` to bound a run (or let the advisor decide). The cost env-vars above are likewise inert. See [control-flags.md](../architecture/control-flags.md#budget-cluster). |
| Artifact backfill | `workflow.backfill_enabled` in `.evolve/policy.json` | `true` | When enabled, scans `<phase>-stdout.clean.txt` for the phase artifact header after `ErrArtifactTimeout` exhaustion; writes a WARN-verdict artifact and allows the cycle to continue rather than aborting. See [docs/architecture/artifact-backfill.md](../architecture/artifact-backfill.md). |
| Phase timing output | `phase-timing.json` | generated | JSON array written to workspace at cycle end tracing per-phase latency + outcome; fields: `{phase, duration_ms, verdict, cost_usd}`. See [docs/architecture/phase-timing-and-diagnostics.md](../architecture/phase-timing-and-diagnostics.md). |
| Phase latency ceiling | `retry.phase_latency_ceiling_s` in `.evolve/policy.json` | `900` | Ceiling (seconds) beyond which a single phase is considered slow, triggering a `phase_latency` warning anomaly in `cycle-health.json`. |
| Failure diagnostic output | `<phase>-failure-diag.json` | generated | Written to workspace when a mandatory phase exhausts its retries and aborts; fields: `{phase, cycle, error_message, exit_code, attempt_count, timestamp}`. See [docs/architecture/phase-timing-and-diagnostics.md](../architecture/phase-timing-and-diagnostics.md). |
| Interaction telemetry | `<phase>-interactions.ndjson` + `interaction-summary.json` | generated | ADR-0045 I1. Append-only NDJSON ledger of every interaction injection (nudge / auto-respond / salvage / kernel answer / correction re-dispatch) — typed Event (`kind`, `phase`, `cycle`, `trigger`, `rung`, `decision_id`, neutralized `payload`, `rule_id`) + Outcome (`result`, `latency_ms`, `cost_usd`) — written beside each phase and rolled up per-cycle into `interaction-summary.json` (the `by_rung` distribution is the §10(d) acceptance metric). **Records at EVERY `EVOLVE_PHASE_RECOVERY` stage including `off`** — telemetry is exempt; only ACTIONS gate. Pane-derived payloads are neutralized through `panetrust` before write (threat S10). Impl: `internal/interaction` Recorder/Rollup. See [docs/architecture/interaction-protocol.md](../architecture/interaction-protocol.md). |
| Promoted interaction-rule registry | `.evolve/instincts/interaction-rules/<id>.yaml` | generated | ADR-0045 I4. Per-rule auto-respond promotions (RE2 regex + response keys + per-rule shadow/enforce stage), absent-only, re-validated against the immutable embedded healthy-pane corpus at every load (corpus-rot demotes a now-matching rule). Enforce-stage rules fire via `bridge.loadPromotedPrompts`; sits under the same instincts root as the ADR-0044 fatal-signature registry. The MINTING side (an LLM advisor that proposes rules) is a named follow-up. |
| Checkpoint trigger — cost % (REMOVED) | `EVOLVE_CHECKPOINT_AT_PCT`, `EVOLVE_CHECKPOINT_WARN_AT_PCT` | n/a (no-op) | **Removed** with the token-budget cost gates (see the Budget row above). Both flags are StatusDead in the Budget Cluster — accepted but ignored. There is no cost-percentage checkpoint trigger, and `--resume` does not read them. See [control-flags.md](../architecture/control-flags.md#budget-cluster). |
| Context autotrim | `EVOLVE_CONTEXT_AUTOTRIM` | `0` (opt-in) | Head-60%/tail-35% prompt trim above `EVOLVE_PROMPT_MAX_TOKENS=30000`. See [docs/architecture/context-window-control.md](../architecture/context-window-control.md). |
| Worktree base | `EVOLVE_WORKTREE_BASE` | auto | Resolved by `preflight-environment.sh`: `.evolve/worktrees/` > `$TMPDIR/evolve-loop/<hash>`. Never auto-enable `EVOLVE_SKIP_WORKTREE=1` (operator-only emergency hatch). |
| Inner sandbox | `EVOLVE_SANDBOX` | auto | `false` when nested-Claude detected; control via `EVOLVE_SANDBOX=on/off`. `EVOLVE_INNER_SANDBOX` and `EVOLVE_FORCE_INNER_SANDBOX` retired in cycle-7 (no Go reader; Go bridge uses `sandbox.ShouldWrap`). |
| Observer auto-spawn | `EVOLVE_OBSERVER_AUTOSPAWN` | `1` (default-on, cycle-122 Fix 3 / ADR-0030) | When `1` (or unset), `evolve loop`'s orchestrator wires `core.WithObserver(observer.NewCoreAdapter())` so a per-phase stall detector spawns from `RunCycle` automatically — restoring the pre-v12 bash-dispatcher behavior that the Go port silently dropped between v12.0.0 and cycle-122. When `0`, falls back to `noopObserver{}` (byte-identical to pre-fix behavior; rollback hatch). See [docs/architecture/adr/0030-phase-observer-autospawn-in-evolve-loop.md](../architecture/adr/0030-phase-observer-autospawn-in-evolve-loop.md). |
| Stall detector (manual) | `EVOLVE_OBSERVER_ENFORCE` | `1` (default-on since v10.18.0) | When `1`, the standalone `evolve phase-observer` subcommand kills its watched subagent (SIGTERM) on stall. **CORRECTION (cycle-122):** previously documented as also applying to `evolve loop`'s autonomous loop; that was factually false from v12.0.0 through cycle-122 (the bash-dispatcher auto-spawn was dropped without re-wiring). `EVOLVE_OBSERVER_AUTOSPAWN=1` (above) restores the auto-spawn path; `_ENFORCE` is consulted by the spawned observer for kill-vs-log behavior. |
| Stall threshold | `EVOLVE_OBSERVER_STALL_S` | `600` | Primary stall threshold for phase-observer (manual subcommand AND auto-spawned adapter per ADR-0030). Bridges from `EVOLVE_INACTIVITY_THRESHOLD_S` (DEPRECATED). |
| Stall liveness probe (auto-spawn) | (no flag) | default-on | cycle-190: at the stall threshold, BEFORE emitting `stall_no_output`, the auto-spawn observer (`internal/adapters/observer`) consults a tmux-pane liveness probe — it locates the bridge session by the `-c<cycle>-<phase>-` infix and hashes `capture-pane -p`; a changed pane (spinner/token-counter advancing during a long single "Incubating" turn) ⇒ agent alive ⇒ reset the clock + emit benign `stall_probe_active` instead. Fixes a false-positive when a tmux agent thinks for minutes then dumps its artifact (both stdout-log + workspace mtime stay flat in that window). Non-tmux phases / no session ⇒ no-op (legacy). No env flag (probe is unconditional; nil-probe = pre-fix behavior). See [knowledge-base/research/observer-false-stall-tmux-liveness-2026-06-02.md](../../knowledge-base/research/observer-false-stall-tmux-liveness-2026-06-02.md). |
| Stall poll | `EVOLVE_OBSERVER_POLL_S` | `5` | Stat-poll interval (seconds) for the auto-spawned observer's stdout-log growth check. |
| Soft-stall nudge | `EVOLVE_OBSERVER_NUDGE_S` | `300` (opt-out via `=0`) | Set `EVOLVE_OBSERVER_NUDGE_S=0` to disable (code default: 300 s). When `>0` and `NUDGE_S <= idle < STALL_S`, phase-observer appends ONE `nudge` envelope to the agent inbox (`<workspace>/.bridge-inbox/<agent>.ndjson`) before the hard SIGTERM — a draining `*-tmux` driver injects it to prompt the agent to summarize+continue or finalize. Body overridable via `EVOLVE_OBSERVER_NUDGE_BODY`. Inert for headless drivers (no drain loop). See [docs/architecture/adr/0023-live-injection-and-launch-rules.md](../architecture/adr/0023-live-injection-and-launch-rules.md). |
| Bidirectional channel — feed | `EVOLVE_CHANNEL` | **DEPRECATED** — folded into `EVOLVE_PHASE_RECOVERY` (ADR-0045 I6; honored one release with a WARN, then removed). The channel is now IMPLIED by the stage (`enforce` → on; `off`/`shadow` → off, byte-identical); an explicit value is still honored one release. Single source: `bridge/channel.Enabled` + `channel.ResolveStage` (shared by the bridge driver and the observer adapter). When `1` (or stage=`enforce`), enables the live bidirectional channel: the autospawn observer (ADR-0030) spawns a `channel.Producer` goroutine that wakes the dormant `phasestream.Normalizer` and writes a filtered live `<agent>-channel.ndjson` feed beside the phase. **Live source (RT0–RT5, corrected from the original ADR):** a tmux driver's `<phase>-stdout.log` is empty until exit, so each tick the driver runs `capture-pane` through `panestream.PaneDelta` (per-CLI `Profiles`) and appends newly-stable content to `<agent>-pane.live`, and writes correlation breadcrumbs (`inject_applied`/`idle_reached`) to `<agent>-breadcrumbs.live` (NOT stderr); the Producer is pointed at that `.live` pair by `CoreAdapter.channelSourcePaths` (RT3 transport-aware: headless → legacy `<phase>-stdout/-stderr.log`). busy→idle is detected by `panestream.PaneBusy` (interrupt/cancel affordance present, or ollama's idle placeholder absent — **the input-box marker persists during generation so it is NOT the signal**); codex has no capturable busy signal → weak-signal degradation (monitoring works, span not bracketed). `pipe-pane` was tried + reverted (raw 2D cursor stream needs a terminal emulator; `capture-pane` is pre-rendered). Headless `claude -p` phases degrade to read-only monitoring (`Supervisor.Ask` returns `ErrTransportNoInject`). Off (default) → byte-identical (no producer goroutine, no `.live`/feed files, no extra capture). See [docs/architecture/adr/0037-bidirectional-channel.md](../architecture/adr/0037-bidirectional-channel.md). |
| Bidirectional channel — supervisor | `EVOLVE_CHANNEL_SUPERVISOR` | `0` (opt-in) | Reserved to gate auto-attaching a `channel.Supervisor` to a running phase (programmatic ask-on-stall via `StallPolicy`). When `1`, the orchestrator will wire a supervisor on phase launch. Currently manual-wiring only; auto-attach not yet wired. See [docs/architecture/adr/0037-bidirectional-channel.md](../architecture/adr/0037-bidirectional-channel.md). |
| Live command injection | `evolve bridge send` (CLI) | n/a | Facet A: queue a live command for an already-running `*-tmux` agent — `evolve bridge send --workspace=DIR --agent=NAME [--kind=command\|interrupt\|nudge\|system_rule] [--source=cli] <body>`. The driver drains its inbox from the artifact-wait poll loop and injects: `command`/`nudge`/`system_rule` are idle-gated (wait for the prompt marker), `interrupt` sends ESC first. Cursor seeks to EOF on launch, so only post-launch sends are delivered (no backlog replay on named-session resume). Go-tmux-only. See ADR-0023. |
| Launch-time system prompt | `EVOLVE_SYSTEM_PROMPT` / `EVOLVE_<AGENT>_SYSTEM_PROMPT` | unset | Facet B: per-agent system-level rules prepended as a `## Rules` block to the prompt at launch (CLI-agnostic, at the same seam as the interactive-policy block). Precedence: `EVOLVE_<AGENT>_SYSTEM_PROMPT` > `EVOLVE_SYSTEM_PROMPT` > profile `system_prompt` > read(profile `system_prompt_file`) > none. Order in the prompt: rules < policy < body. See ADR-0023. |
<!-- Shared Agent Values: researchCache schema uses research_fingerprint + research-cache.sh utility -->
| Research cache | `EVOLVE_RESEARCH_CACHE_ENABLED` | `0` (opt-in) | Adds `state.json:researchCache.entries[<sha>]`. Cache hit when fingerprint matches + `cycle - produced_at_cycle ≤ EVOLVE_RESEARCH_CACHE_MAX_AGE` (default 5). |
| Research tool | `workflow.allow_deep_research` in `.evolve/policy.json` | `false` | When `true`, lifts the per-agent quota cap. Does not disable hook telemetry. See [docs/architecture/research-tool.md](../architecture/research-tool.md). |

Migration note: `EVOLVE_ALLOW_DEEP_RESEARCH` is retired; use
`workflow.allow_deep_research` in `.evolve/policy.json`.
| Research tool | `EVOLVE_RESEARCH_QUOTA_SOFT` | *(planned)* | Soft quota: allows over-quota web calls but emits WARN in guards.log. Not yet implemented in `research-quota-gate.sh` as of cycle-89. |
| Research tool | `EVOLVE_RESEARCH_HOOK_DISABLED` | `0` | When `1`, `research-quota-gate.sh` is a no-op but counters still increment (telemetry-only mode). |
| Research tool | `EVOLVE_KB_SEARCH_PATHS` | `knowledge-base/research/:.evolve/instincts/lessons/:docs/research/` | Colon-separated root paths for the native knowledge-base search (`go/internal/research`). |
| Subscription proxy | `EVOLVE_ANTHROPIC_BASE_URL` | unset | When set, exported as `ANTHROPIC_BASE_URL` before every `claude -p` invocation. **Proxy-agnostic**: target must speak Anthropic Messages API format (`POST /v1/messages`). **Not required for subscription auth** — `claude -p` reads `~/.claude.json` OAuth credentials natively. Use only for custom endpoints (LiteLLM, corporate gateway). Example: `export EVOLVE_ANTHROPIC_BASE_URL=http://127.0.0.1:4000/v1` (LiteLLM default). Note: `hermes proxy start` does not exist in hermes-agent; do not use it. See `knowledge-base/research/hermes-agent-proxy-integration.md`. Run `evolve doctor` to detect your active auth mode. |
| Incremental intent | `EVOLVE_INTENT_DELTA` | `0` (opt-in) | When `1`, `intent-batch-resolve.sh` runs before the intent phase to compute `INTENT_MODE=full\|delta` by comparing `GOAL_HASH` against `state.json:currentBatch.goalHash`. In delta mode, the intent persona emits `intent-delta.md` (patch) or `[intent-unchanged]` instead of a full `intent.md`; `gate_intent_to_research` accepts both. Requires `EVOLVE_REQUIRE_INTENT=1`. See [docs/architecture/incremental-intent.md](../architecture/incremental-intent.md). |
| Antigravity adapter — require-full | `EVOLVE_AGY_REQUIRE_FULL` | `0` | When `1`, `agy.sh` exits 99 if neither `agy` nor `claude` binary is found (same opt-in as `EVOLVE_GEMINI_REQUIRE_FULL`). Default: graceful degradation. |
| Antigravity adapter — binary override | `EVOLVE_AGY_BINARY` | unset | Testing seam: override the `agy` binary path. Honoured only when `EVOLVE_TESTING=1`. Used by ACS predicates to force NATIVE/DEGRADED mode in tests. |
| Go binary path override | `EVOLVE_GO_BIN` | unset | Path to the Go binary. When unset, the entrypoint resolves `<project_root>/go/bin/evolve`. Set to the cross-compiled artifact path (e.g. `<HOME>/.local/bin/evolve-darwin-arm64`) for system-wide install. The Go binary is the sole runtime: `evolve cycle run`, `evolve loop`, `evolve doctor`, `evolve guard`, `evolve ledger`, `evolve acs`, `evolve ship`. (The bash dispatcher and `EVOLVE_USE_LEGACY_BASH` rollback hatch were removed in the Go-only consolidation; there is no bash fallback.) |
| Native Go ship | `evolve ship` (native, no flag) | always native | The ship phase runs the native Go implementation (`go/internal/phases/ship/`): self-SHA TOFU, audit-binding, EGPS gate, atomic commit+ff-merge+push, gh release. CLI surface: `evolve ship [--class cycle\|manual\|release\|trivial] [--dry-run] "<msg>"`. (The `EVOLVE_NATIVE_SHIP=0` shell-out to a bash `ship.sh` was removed in the Go-only consolidation; ship is native-only.) Parity history: the test matrix in `go/internal/phases/ship/native_test.go` pins commit-message footers, exit codes, and ledger semantics. |
| Interactive policy (global) | `EVOLVE_INTERACTIVE_POLICY` | `recommended_or_first` (default-on, v12.1+) | Bridge prepends a deterministic policy block to every phase prompt so subagents self-resolve `AskUserQuestion`/y/N prompts without hanging the autonomous loop. Values: `recommended_or_first` (pick option labeled "(Recommended)" or first), `escalate` (no block, fail loudly on ambiguity — legacy posture), `auto_yes` (binary y/N → yes; multi-option falls back to recommended-or-first). Unknown values silently default to `recommended_or_first` to protect autonomy from typos. Block is < 200 tokens and deterministic to preserve Claude prompt-prefix cache. Implemented in `go/internal/adapters/bridge/bridge.go:injectPolicyPrefix`. |
| Interactive policy (per-agent) | `EVOLVE_<AGENT>_INTERACTIVE_POLICY` | unset | Per-agent override of the global policy. `<AGENT>` is the agent name uppercased with hyphens → underscores: `scout` → `EVOLVE_SCOUT_INTERACTIVE_POLICY`, `tdd-engineer` → `EVOLVE_TDD_ENGINEER_INTERACTIVE_POLICY`. Precedence: `req.Env` per-agent > process env per-agent > `req.Env` global > process env global > default. Use to pin one phase (e.g., `EVOLVE_AUDITOR_INTERACTIVE_POLICY=escalate`) while every other phase stays on autonomy. |
| Phase max attempts | `EVOLVE_PHASE_MAX_ATTEMPTS` | `2` | Bounds per-phase retries on a recoverable bridge timeout or transient failure in the autonomous loop. Valid range: [1,5]. Non-numeric or out-of-range values fall back to default 2 (except values > 5 which are clamped to 5). Non-transient errors are not retried. |
| Retry backoff base | `EVOLVE_RETRY_BACKOFF_BASE_S` | `5` | Base duration (seconds) for exponential backoff between phase attempts on transient error or non-canonical verdict. Sleep = `base * 2^(attempt-2)` clamped to `max(base, 30)` seconds. Sleep only on attempt >= 2. Set to `0` to disable backoff sleep (test default). |
| Plan-mode dispatch (per-phase) | `EVOLVE_<PHASE>_PERMISSION_MODE` | unset (v12.1+) | Sets the Claude Code `--permission-mode` flag for a single phase invocation. Values per Claude Code: `plan`, `acceptEdits`, `default`. Wired into all 6 phase runners (intent, scout, triage, tdd, build, audit) via the shared `go/internal/phaseflags/` package — call site is one line: `phaseflags.For("<phase>").Resolve(profileDir, req.Env)`. Precedence: `req.Env[EVOLVE_<PHASE>_PERMISSION_MODE]` > `profile.permission_mode` (JSON field in `.evolve/profiles/<phase>.json`) > unset. profile.extra_flags pass through unconditionally. The combined slice is appended to `BridgeRequest.ExtraFlags`; only Claude drivers honor `--permission-mode` (claude-p, claude-tmux). Codex/Gemini drivers refuse it gracefully (`plan_mode_degraded` ledger entry — wired in a later v12.1 slice). |
| Default execution path (tmux) | per-phase `profile.cli` (fallback `claude-tmux`) | tmux-LLM for every phase | The agent-bridge runs every phase through an interactive **tmux-LLM** driver by default — `claude-tmux` / `codex-tmux` / `agy-tmux` / `ollama-tmux`. Every `.evolve/profiles/*.json` pins a `-tmux` cli (or leaves `cli` empty → `llmroute` falls back to `claude-tmux`); the guard test `go/cmd/evolve/tmux_default_profiles_test.go` fails the build if a profile pins a non-tmux default. The headless drivers (`claude-p`, `codex` exec, `agy -p`) remain available but are **opt-in only** — selected via precedence `EVOLVE_<AGENT>_CLI` > `EVOLVE_CLI` > `profile.cli` > `.evolve/policy.json` pin > `claude-tmux` (`go/internal/llmroute/llmroute.go`), never as a profile default. claude subscription auth for the tmux path is detected from the macOS login Keychain (`bridge.doctorAuth`, service `Claude Code-credentials`). |
| Stdout filter | `EVOLVE_STDOUT_FILTER` | `on` (v12.2+) | After each phase the runner writes `<phase>-stdout.clean.txt` next to the raw `<phase>-stdout.log` — a human-readable trace (~8–20% the size of raw) with `stream_event.*` redraw noise dropped, hook envelopes compressed to one line, tool_result payloads middle-truncated, and `assistant.thinking.signature` blobs stripped. Raw file is BYTE-FOR-BYTE UNCHANGED so cyclecost.go and phaseobserver continue to read raw without churn. Set `off` to skip. Filter is best-effort; failures WARN to stderr and never block the phase. Implementation: `go/internal/logfilter/`, wired in `go/internal/phases/runner/runner.go` after `bridge.Launch()` succeeds via the `stdoutFilterFn` test seam. Per-line dispatch (try JSON parse → known event type → JSON rules; else plain-text rules with consecutive-dedup + spinner/box drop) unifies stream-json and tmux-scrollback in one pipeline. Measured retention on cycle-106 build-stdout.log: 200 KB → 15 KB (7.6%). See `knowledge-base/research/stdout-noise-profile-2026-05-26.md`. |
| Cycle outcome label | `FinalVerdict` (orchestrator output field) | `PASS\|FAIL\|WARN\|SHIPPED_VIA_BUILD\|SKIPPED_AUDIT_ADVISORY\|SKIPPED_UNKNOWN` (v12.2+) | The dispatcher's per-cycle JSON summary used to emit a bare `SKIPPED` whenever the formal ship phase didn't run, conflating "build phase committed inline" with "audit blocked" with "no signal." Now disambiguated: HEAD moved during cycle → `SHIPPED_VIA_BUILD`; retro decision text contains `would-have-blocked` → `SKIPPED_AUDIT_ADVISORY`; else bare `SKIPPED_UNKNOWN` (loud label invites inspection). PASS/FAIL/WARN pass through unchanged. Implementation: `Orchestrator.finalizeOutcome` in `go/internal/core/orchestrator.go`; `gitHEAD` seam injectable for tests. Source incident: cycle-107 (2026-05-26) shipped commit `4519cea` but reported bare `SKIPPED`. |
| Ledger manual entries | `cycle_label` field on `LedgerEntry` | unset | Manual operator entries (release audits, ad-hoc events outside the integer cycle sequence) MUST use `"cycle": 0` plus `"cycle_label": "<semantic>"` (e.g., `"manual-release-v10.16.0"`). Reserved: numeric `cycle` field is exclusively for the integer cycle sequence. Legacy entries that wrote `"cycle": "<string>"` (which broke the dispatcher's `ledger iter` walker with `json: cannot unmarshal string into Go struct field LedgerEntry.cycle of type int`) are absorbed by a defensive unmarshaler in `go/internal/core/ports.go` — string cycles land in `CycleLabel` with `Cycle=0`. The on-disk ledger is never rewritten (would break the SHA256 hash chain). |
| Unfinished-cycle guard | `EVOLVE_FORCE_FRESH` | `0` (guard on) | A fresh `evolve loop` run REFUSES (exit 2, `stop_reason=unfinished_cycle`) when an unfinished cycle is detected — `cycle-state.json` ahead of `lastCycleNumber`, OR unreadable (truncated by a SIGKILL'd dispatcher) — and prints the resume∥reset fork. This prevents silently clobbering a stuck cycle's history. Resolve by `evolve loop --resume` (continue) or `evolve cycle reset` (seal it — archives workspace + cycle-state snapshot + `reset-manifest.json` to `.evolve/runs/cycle-<N>.reset-<ts>/`, never deleted; advances `lastCycleNumber`; auditable ledger entry). `EVOLVE_FORCE_FRESH=1` restores the prior silent-clobber (history NOT sealed). Implementation: `unfinishedCycle` + the guard in `go/cmd/evolve/cmd_loop.go`; `core.SealCycle` in `go/internal/core/reset.go`. |
| Readiness gate | `EVOLVE_SKIP_PREFLIGHT` / `EVOLVE_SKIP_PREFLIGHT_BOOT` | `0` (gate on) | A fresh `evolve loop` run runs a DETERMINISTIC host-side readiness gate AFTER the unfinished-cycle guard and BEFORE the first cycle — it verifies the pipeline can actually run (4 accumulate-then-decide checks: **pipeline-structure** = every spine phase has a registry factory + phasecontract and every profile CLI/`cli_fallback` resolves to a known driver; **llm-cli-status** = each distinct CLI binary present; **host-capabilities** = tmux + writable `.evolve`/`.evolve/runs` Halt, sandbox-degraded/low-disk/stale `evolve-bridge-*` tmux Warn; **bridge-boot** = really boots each `-tmux` REPL via `bridge.BootSmokeTest` under a 90s budget). On any Halt it aborts BEFORE spending LLM budget: exit 2, `stop_reason=preflight_failed` (cycle=0), and writes `.evolve/loop-preflight.json` (the full per-check result, levels as strings) + a human Summary to stderr. Catches the cycle-258 `ExitREPLBootTimeout` at batch start instead of ~30 min in. `EVOLVE_SKIP_PREFLIGHT=1` bypasses the whole gate; `EVOLVE_SKIP_PREFLIGHT_BOOT=1` runs the cheap checks but skips the real boot (bridge-boot downgrades Halt→Warn — CI/offline). Implementation: `go/internal/looppreflight/` (mirrors the `releasepreflight` Options→Run→Result seam blueprint) + the gate hook in `go/cmd/evolve/cmd_loop.go`. Standalone operator probe: `evolve doctor boot <driver>`. |
| Commit gate | `EVOLVE_BYPASS_COMMIT_GATE` | `0` (gate on, v13.0.0+) | `evolve ship --class manual` is the single chokepoint for interactive commits (bare `git commit` is ship-gate-denied), and it requires a fresh commit-gate review attestation: `.commit-gate/attestation.json` whose `tree_state_sha == sha256(git diff HEAD)`. Produce it with `/commit` (code-simplifier + code-reviewer + language reviewer + lint + targeted tests). Missing/malformed/stale (reviewed tree ≠ staged tree) → ship refuses with an `IntegrityError`. `EVOLVE_BYPASS_COMMIT_GATE=1` skips the check (routine use is a CLAUDE.md violation); `--dry-run` is exempt (commits nothing). Implementation: the gate is `evolve commit-gate run` (`go/internal/commitgate/`); enforcement is `go/internal/phases/ship/commitgate.go`. See [skills/commit/SKILL.md](../../skills/commit/SKILL.md). |
| User policy | `EVOLVE_POLICY_BYPASS` | `0` (policy on) | User-controlled rule layer in `.evolve/policy.json` (optional; absent = no rules, malformed = loud fail). `mandatory_phases` are merged ADDITIVELY into the routing spine at cycle start (`cmd_cycle.go`→`unionPhases`) so the advisor can never drop a user-declared mandatory phase (the non-configurable integrity floor `ship ⇒ build ∧ audit` still applies on top). `pins` are hard per-phase CLI/model dispatch overrides consulted by the runner via `internal/llmroute`: a pin is ABSOLUTE (beats `EVOLVE_<AGENT>_CLI/_MODEL` env), validated within the profile's `allowed_clis` + `model_tier_envelope` (out-of-bounds → phase hard-fails), and a pinned CLI is exempt from the capability probe (never silently demoted). `EVOLVE_POLICY_BYPASS=1` skips policy for a run (routine use defeats the guard). See [docs/architecture/policy-config.md](../architecture/policy-config.md); impl `go/internal/policy`. |
| Spine artifact floor | policy `recovery.spine_floor` (NO env var — policy-only dial) | `enforce` (R8.5 flip, 2026-07-16) | The artifact-backed spine floor (`SpineSatisfiedUpTo`, `go/internal/core/statemachine.go`): before dispatching a mandatory-anchor phase, the digest must prove every mandatory predecessor left its handoff artifact — `handoff-<phase>.json` first, then the per-phase fallbacks (scout → non-empty `scout-report.md`; build → git-derived change evidence; audit → `acs-verdict.json`, the deterministic `generateACSVerdict` output, whose FAIL verdict is carried honestly so the floor refuses ship on a red audit). At `enforce` (default) a CLEAN absence aborts the cycle FAILED-EXPLAINED (worktree preserved, failure-learning fed); a DEGRADED read (EISDIR / permission / digest error) always fails open with a WARN; `recovery.spine_floor: "shadow"` in `.evolve/policy.json` is the no-recompile escape hatch (WARN + proceed, the pre-R8.5 behavior). Deliberately DECOUPLED from `recovery.phase_recovery`: that dial also arms the bidirectional channel (ADR-0045 I6) and the failure-adviser promotion path, and stays `shadow`. Flip evidence: a 536-run-dir replay (env-gated harness `TestSpineReplay_LocalHistory`, `internal/core`) showed 0 would-block transitions on every cycle shape since ~cycle-480 once the scout/audit digest fallbacks (`go/internal/router/digest.go`) landed. |
| Dynamic phase routing | `EVOLVE_DYNAMIC_ROUTING` | `advisory` (registry-pinned default since 2026-06-06, retro migration steps 1-3 landed; was `off` from v13.0.0/PR #4 — `off` remains the operator escape hatch) | Rollout stage for the Go routing kernel (`go/internal/router`, `core.PhaseAdvisor`): `off`/`0` = legacy static sequence drives (byte-identical to pre-routing); `shadow` = router computes + logs the would-have-routed plan, static still drives; `advisory`/`enforce` = the advisor DRIVES (ADR-0024 §1 floor LIVE as of PR-5): at cycle start the advisor's whole-cycle plan is clamped by `ClampPlanToFloor` and drives run/skip for every **non-mandatory** phase, and `enforceNext` (now active at Advisory+, re-validated by `CanTransition` + `SpineSatisfiedUpTo`) lets it override the static successor. **Two separate guarantees:** the *configurable* never-skip set is `EVOLVE_MANDATORY_PHASES` (default `scout,build,audit,ship`); the *non-configurable* integrity floor is `ship ⇒ build ∧ audit ∧ (tdd unless trivial)`, forced into the plan even when those phases are absent from the mandatory set — so an operator may shrink the mandatory set to e.g. `tdd` and the advisor still cannot reach ship without a real build+audit. A planner failure ⇒ nil plan ⇒ fall back to the configurable spine (fail-safe to static). NOTE: a no-ship cycle ending early (e.g. `scout→end`) is honored by the pure kernel but NOT yet end-to-end — the state-machine graph has no such edge, so the static spine still runs (cost-only, not a safety gap; SM-edge widening is follow-up). Companion flags: `EVOLVE_ROUTING_MODE` (`llm` default = LLM proposes + kernel clamps / `static` = triggers + spine only, no advisor plan), `EVOLVE_MANDATORY_PHASES` (CSV; omitting `audit`/`ship` emits a `weak-spine` WARN — now backstopped by the integrity floor), `EVOLVE_CONDITIONAL_MANDATORY` (`phase:expr`, default `tdd:cycle_size!=trivial`), `EVOLVE_MAX_OPTIONAL_INSERTIONS` (int, default `4`; not applied to plan-driven inserts — the plan is the advisor's clamped whole-cycle selection), `EVOLVE_USE_PHASE_REGISTRY` (`0` to ignore `docs/architecture/phase-registry.json`). Precedence: env > registry file > built-in default. First live advisory cycle was cycle-108. See [docs/architecture/dynamic-phase-routing.md](../architecture/dynamic-phase-routing.md) and ADR-0024. |
| Swarm harness | `EVOLVE_SWARM_STAGE` | `shadow` (no dispatch, v13+/ADR-0032) | Rollout dial for the multi-tmux-LLM-CLI subagent swarm (one phase dispatches N heterogeneous workers): `shadow`/off = planner+validator only, **no dispatch** (build/scout run N=1, pipeline byte-identical); `advisory` = workers launch in parallel, integration computed but non-authoritative; `enforce` = merge-train (writers) / synthesis (readers) is the phase output. **Writer/reader asymmetry (the core rule):** writers require COMPLETELY DISJOINT file ownership → a non-disjoint partition is rejected → fall back to N=1 (when not cleanly independent, do NOT swarm writers); readers allow overlapping focus regions (read overlap wastes tokens, never corrupts). Each writer worker runs in its own branch+worktree+sandbox; the only serialized section is the merge-train. Teardown is guaranteed: live SessionRegistry + structured concurrency (process-group kill → tmux kill-session → `#{pane_dead}`) AND a crash-safe on-disk manifest + `evolve swarm reap` sweep. Companion flags: `EVOLVE_SWARM_PLANNER` (legacy `=1` enables the `swarm-plan` phase), `EVOLVE_SWARM_CONCURRENCY` (default 2), `EVOLVE_SWARM_BUDGET_USD` (cumulative ceiling). See [docs/architecture/adr/0032-swarm-harness.md](../architecture/adr/0032-swarm-harness.md) + [docs/architecture/swarm-harness.md](../architecture/swarm-harness.md) (operator/debugging guide) + [knowledge-base/research/swarm-concurrency-2026-05.md](../../knowledge-base/research/swarm-concurrency-2026-05.md). |
| Concurrent loop — lane | `EVOLVE_LANE` | hash-of-worktree-root (8 hex chars) | Human-readable lane name for a worktree in the sibling-worktree concurrent-loop model (ADR-0054). Overrides the hash-of-root default computed by `runscope.EnvLane` (e.g. `EVOLVE_LANE=campaign` → branch prefix `cycle-campaign-N`, tmux session prefix `evolve-bridge-cycle-campaign-N`). Readability-only — correctness never depends on the override; the hash default is collision-safe across distinct worktree roots. Read by `internal/runscope`. See [docs/architecture/adr/0054-concurrent-evolve-loop-sibling-worktrees.md](../architecture/adr/0054-concurrent-evolve-loop-sibling-worktrees.md). |
| Concurrent loop — orphan reaper | `EVOLVE_REAP_ORPHANS` | `1` (active) | Operator opt-out for the Tier-3 liveness orphan session reaper (ADR-0054 Slice 3). Default 1 = active: `sessionreaper.ReapOrphans` runs in `looppreflight` and removes tmux sessions from SIGKILL'd runs whose `.lease` is stale. Set `0` to disable reaping for a run (e.g. for debugging leaked sessions). **Safety invariant:** a run whose `.lease` is Fresh is NEVER reaped regardless of this flag; the lease is the sole liveness oracle. This flag is an operator convenience opt-out — it does NOT gate `sessionreaper`'s core reap logic. Operator backstop: `evolve swarm reap-orphans [--dry-run]`. See `internal/sessionreaper` and ADR-0054. |
| Concurrent loop — CLI admission | `EVOLVE_CLI_MAX_CONCURRENT_<CLI>` | `0` (unbounded) | Per-CLI cross-process admission cap for the sibling-worktree concurrent-loop model (ADR-0054 Slice 4). Replace `<CLI>` with the uppercase CLI name (e.g. `EVOLVE_CLI_MAX_CONCURRENT_CODEX=2`, `EVOLVE_CLI_MAX_CONCURRENT_CLAUDE=3`). Default `0` = unbounded (byte-identical to pre-concurrency behavior — safe default). A failed acquire degrades to uncapped + WARN; admission control NEVER blocks a phase outright. Holder set stored in `$XDG_RUNTIME_DIR/evolve/cli-<name>.slots` (fallback `os.TempDir()`); stale holders auto-pruned by TTL (lease-as-liveness self-healing). Read by `internal/bridge/driver_tmux_repl.go` via `internal/cliadmit.Acquire`. See ADR-0054. |

## Operator commands

> **Operator commands (read-only):**
>
> - `evolve dossier verify [--project-root P]` — validates every `knowledge-base/cycles/cycle-N.json` dossier (ADR-0055 D3): parses JSON, calls `dossier.Validate()`. Exit `0` when all dossiers are valid (or the directory is absent). Exit `1` on any parse or validation error. Pure reader — safe to run mid-batch or in CI. Env: `EVOLVE_PROJECT_ROOT` (default: `.`).
> - `evolve guard list-audit-fails [--evolve-dir DIR] [--json]` — enumerates `state.json:failedApproaches[]` entries with `classification=code-audit-fail` that are still within their 30-day retention window. Surfaces the count + identity of the audit failures the retro-phase failure-adapter has been tallying. Pure read; never mutates state. Use `--json` for scripted triage.
> - `evolve eval diversity-check <evalsDir> [slug]` — suite-level adversarial-diversity check (v13.0.0+): flags an eval suite that is positive-only by counting how many evals carry negative + edge cases. Exit `0` PASS, `1` WARN, `2` HALT (cohesive suite, zero negative cases), `10` bad args. Companions: `evolve eval quality-check <eval.md>` (Level-0 single-file tautology detection) and `evolve eval verify <eval.md> <workspace>` (independent eval re-execution). Read-only.
> - `evolve skills publish [--target codex,agy,ollama] [--dry-run] [--install] [--check] [--ollama-base M] [--codex-home D] [--no-prune]` — project the repo's canonical skills into foreign LLM CLIs (ADR-0041). Default is stage-only (writes gitignored mirrors under `.evolve/publish/`, runs the read-only `agy plugin validate`) — safe. `--install` performs the mutating steps: copy into `$CODEX_HOME/skills/evolve-*`, `agy plugin install`, `ollama create evolve-*` (read-only reasoning/review skill subset only). Modelfile `FROM` base resolves `--ollama-base` → `EVOLVE_OLLAMA_BASE` → `llama3.1:8b` (the ollama-tmux driver default). Idempotent; prune deletes only artifacts carrying the `EVOLVE-PUBLISH:projection` provenance marker. `--check` exits 2 on staged-mirror drift (CI-safe, no writes).
> - `evolve inbox batches [--json] [--max N]` — the deterministic backlog grouping (2026-07-16, `internal/inboxbatch`): campaign / file-area (hub-capped) / dep signals → union-find clusters → dep-topological order → cap-split batches ranked by max member weight. This is the SAME grouping the triage prompt receives (its `inbox_batches` section instructs the agent to prefer selecting a whole batch as `top_n`, amortizing one cycle's pipeline across related items instead of one-item-per-cycle). Pure read; run from the project root.
> - `evolve setup detect [--json]` — onboarding digest (read-only): per-CLI binary/auth-mode/capability-tier/verdict + per-phase current routing + envelope/cross-family/allowed_clis constraints, with `.evolve/policy.json` pins overlaid (`source=policy-pin`) and any floor breach reported as `pin_violation` (a malformed policy → top-level `policy_error`). Backs the in-session `/setup` skill. (Step 9b removed the `evolve setup validate` subcommand + the `llm_config.json` it clamped; the same envelope + allowed_clis clamp now lives in `policy.ValidatePin`, surfaced inline by `detect` and hard-enforced at dispatch.) See [docs/architecture/setup-onboarding.md](../architecture/setup-onboarding.md) + ADR-0051.

> **Campaign commands (multi-cycle goal decomposition — ADR-0054):**
>
> - `evolve campaign study --workspace <dir>` — invoke the `preliminary-study` LLM phase; writes `campaign-plan.json` into the workspace and renders the human-readable plan to stdout for operator approval. Requires a configured LLM bridge and a valid `.evolve/phases/preliminary-study/` phase config.
> - `evolve campaign replan --workspace <dir> --feedback <text>` — re-run the study with operator feedback; loads and verifies the previous plan, generates a revised one, prints a diff (added/removed/modified cycle IDs), and renders the updated plan.
> - `evolve campaign run --plan <campaign-plan.json> [--simulate]` — load, verify, and execute the campaign plan as dependency-ordered waves. Each wave's file-disjoint cycles run concurrently via `fleet.Supervisor`; a failed cycle receives one localized retry before the wave fails. `--simulate` exercises wave execution without LLM calls (deterministic plumbing test).
>
> The plan is **deterministically verified** (`campaign.Plan.Verify`: version≥1, non-empty goal, unique cycle IDs, acyclic depends_on DAG) before any render or execution. The LLM study output is advisory; all trust boundaries remain downstream (per-cycle build∧audit∧tdd gates). See `docs/architecture/adr/0056-advisor-driven-preliminary-study-cycle.md` and `docs/architecture/campaign-planning-citations.md`.

> **Operator commands (write a verdict file):**
>
> - `evolve acs suite --cycle N [--root .] [--evolve-dir .evolve]` — runs the bash EGPS predicate suite (cycle-N predicates + regression-suite + red-team) and writes `<evolve-dir>/runs/cycle-N/acs-verdict.json`; exit `2` when any predicate is RED, `0` when all green. Host-side deterministic replacement for the deleted `run-acs-suite.sh` (ADR-0025). `evolve acs run --cycle N <pkg>` runs `go test -json` on a package into the same verdict file.

> **Session cost isolation (v10.8.0+):** `claude -p` subagent invocations bill to the OAuth session that launched the dispatcher (the parent Claude Code session), not the batch budget meter. To isolate `/evo:loop` costs from your prior session context, run `/clear` before starting a new evolve-loop batch. The batch meter (`state.json:currentBatch.cycleAccruedCostUSD`) tracks per-cycle accumulation but cannot capture OAuth session charges.

## Ship classes (`evolve ship --class <X>`, native)

| Class | Use case | Verification |
|---|---|---|
| `cycle` (default) | `/evo:loop` cycle commits | Full audit-binding: recent PASS, SHA match, HEAD/tree bound, `acs-verdict.json` red_count==0 |
| `manual` | Operator-driven manual commits | Skips audit-binding, but requires a fresh commit-gate review attestation (`.commit-gate/attestation.json`; bypass with `EVOLVE_BYPASS_COMMIT_GATE=1`); interactive y/N. CI mode: `EVOLVE_SHIP_AUTO_CONFIRM=1`. |
| `release` | `evolve release` pipeline only | Skips audit (version-bump mutates files post-audit); logs RELEASE class loudly |

Bare `git push origin main` is denied by ship-gate (v8.13.0+). `EVOLVE_BYPASS_SHIP_VERIFY=1` is a permanent compatibility bridge but emits deprecation WARN — prefer `--class manual`.

**Binary-churn discard (`cycle`/`manual` staging):** before `git add -A`, these classes discard
post-audit rebuild churn of the tracked `go/evolve` (restore from index) and remove an *untracked*
`go/evolve` so unaudited binaries never ride into commits. The discard **never deletes the
currently-executing binary** — e.g. the gitignored `go/bin/evolve` that the kernel hooks and the
rollback shellout (`evolve ship --class manual`) run — it is skipped with a
`[ship] WARN: churn discard skipped …` line instead (cycle-764 fix; previously a *successful*
manual ship deleted the running binary → exit 127 on the next invocation; heal was
`make -C go build && evolve reset-sha -operator`). `release` ships skip the discard entirely —
the pipeline's rebuild-binary step is the audited producer of `go/evolve`.

### Ship self-healing (repair ladder, ADR-0039 §8)

Ship attempts ONE typed, provably-safe repair per error code per run before surfacing a
failure — no new bypass env vars, no rebase, no force-push, no content deletion:

- **Stale TOFU pin** (`SELF_SHA_TAMPERED`): re-pins when the running binary matches the blob at
  `HEAD:<bin>` (verified rebuild of committed source); any divergence still BLOCKs.
- **Merged-but-unpushed** (`AUDIT_BINDING_HEAD_MOVED`): when HEAD already carries the
  audit-bound tree and origin is strictly behind, ship completes with a push-only closure.
- **Untracked colliders** (`GIT_FF_MERGE_DIVERGED`): byte-identical main-side copies are
  removed; differing copies are quarantine-moved to `.evolve/quarantine/cycle-<N>/` (with
  `manifest.json`) — inspect/restore from there; content is never deleted.
- **Push race** (`GIT_PUSH_REJECTED`): one fetch + ff-retry; a diverged origin reclassifies to
  `needs-reaudit` (local commit preserved — the recovery chain re-audits on the new base).

Operator-visible: `[ship] REPAIR:` log lines; `ship.repair_attempted` / `ship.repair_outcome`
signals; declined attempts annotate `ship-error.json`. On an unresolved ship failure the cycle
worktree is PRESERVED (not pruned) — reclaim via `evolve loop --resume` or `evolve cycle reset`.

### Rebuilding the tracked `go/evolve` binary (recurring chore)

`go/evolve` is a **tracked build artifact**; rebuild it from HEAD whenever loop behavior changes — a stale
binary truncates cycles (e.g. a pre-A0 binary emits no `boot_ms`, a pre-18.0.0 binary lacks the artifact-timeout
reconciliation). The build is **reproducible**, and the ship gate's TOFU self-SHA pin makes an *intentional*
same-version rebuild trip `SELF_SHA_TAMPERED` — the new binary isn't at `HEAD:go/evolve` until the very commit
being gated puts it there, a chicken-and-egg the auto-repair can't resolve, so it declines. Procedure:

```bash
cd go
HEAD_SHORT=$(git -C .. rev-parse --short=12 HEAD)
LDF="-X github.com/mickeyyaya/evolve-loop/go/pkg/version.version=<VERSION> \
     -X github.com/mickeyyaya/evolve-loop/go/pkg/version.commit=${HEAD_SHORT} \
     -X github.com/mickeyyaya/evolve-loop/go/pkg/version.builtAt=<FIXED-UTC-TIMESTAMP>"
go build -trimpath -buildvcs=false -ldflags="$LDF" -o evolve ./cmd/evolve   # run twice → byte-identical
```

- `-buildvcs=false` is **required** for determinism (else the git VCS stamp varies between builds); `-trimpath`
  strips absolute paths; pin a **fixed** `builtAt` (never `date`) so two builds hash-match.
- Ship as a chore: `EVOLVE_SHIP_AUTO_CONFIRM=1 EVOLVE_BYPASS_COMMIT_GATE=1 ./go/evolve ship --class manual "chore(build): …"`.
  The bypass is justified for a **zero-source-diff binary artifact** — the source was already reviewed and the
  gate's lint/test/review steps no-op on a binary blob; verify *reproducibility* (2× byte-identical) instead.
- **SELF_SHA dance** (expected on a same-version rebuild): clear the pin and re-ship — TOFU re-pins on the
  first run. `.evolve/state.json` is gitignored, so this never pollutes the commit:
  ```bash
  jq 'del(.expected_ship_sha)' .evolve/state.json > .evolve/state.json.tmp.$$ && mv .evolve/state.json.tmp.$$ .evolve/state.json
  ```
- `go build ./...` (and `go test` on `cmd/evolve`) rebuild this binary as a side effect — `git checkout -- go/evolve`
  after non-release builds to keep commits source-only.
Ship-local residues (`GIT_FF_MERGE_DIVERGED`, `COMMIT_PREFIX_GATE`, `GIT_DETACHED_HEAD`,
`WORKTREE_RESOLVE`) route to the debugger phase, never back into the audit↔ship loop.

## Publishing releases

"publish" ≠ "push". Use the native self-healing pipeline:

```bash
evolve release X.Y.Z                  # full publish
evolve release X.Y.Z --dry-run        # simulate
evolve release X.Y.Z --skip-tests     # hot-fix (CI-pre-verified)
```

Each release is classified in its notes as a **binary-release** (the compiled
binary changed → a new macOS fingerprint, needs a fresh approval) or a
**config-release** (binary byte-identical to a prior version → no new approval).
Releases publish one universal macOS artifact (`evolve_darwin_all.tar.gz`) plus
`checksums.txt`.

## Corporate deployment (fingerprint approval)

For running the loop where executables are approved by SHA256 fingerprint — the
air-gapped `install.sh --binary` recipe, `EVOLVE_GO_BIN` pinning, the
approval-request flow, and the codesign alternative — see
[corporate-deployment.md](corporate-deployment.md).

Pipeline lifecycle: pre-flight (`evolve release-preflight`) → version bump (`evolve version-bump`) → auto-changelog (`evolve changelog-gen`, conventional commits) → consistency check (`evolve release-consistency`) → atomic ship via native `evolve ship` → marketplace propagation polling (`evolve marketplace-poll`, 5 min) → cache refresh → auto-rollback (`evolve rollback`) on post-push failure.

Auto-bumped version markers: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `skills/loop/SKILL.md` (heading), `README.md`, `CHANGELOG.md`. `evolve release-consistency` is the standalone consistency verifier.

Full vocabulary (push, tag, release, propagate, publish, ship): [docs/guides/publishing-releases.md](../guides/publishing-releases.md).
