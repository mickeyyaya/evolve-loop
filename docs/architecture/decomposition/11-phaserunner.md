# Unit 11 — Phase-runner verdict engine (`internal/phases/runner/verdict`)

- **Program:** [ADR-0103](../adr/0103-component-breakdown-program.md) · units table row 11 (inserted after the `05` row, before the `…` row — it jumps the §12 order on the cycles-1634/1636 F4 triage class, not on file size) · design §12 row note (signal-center-design.md §12) · §5.1 module sentence · §5.2 kind row
- **Extracted from:** `go/internal/phases/runner/reconciliation.go` (210 lines — `phaseReconciliation` :14-19, `reconcileDeliverable` :24-210, 187 lines, nesting 6 at :141-146), `classification.go` (206 lines — `classifyPhaseOutcome` :16-187, 172 lines; `verifyRootsFor` :189-206) and their helpers in `runner.go` (719 lines): the settle bounds :315-318, `verifyReconcileDeliverable` :326-386, `forensicSnapshot`/`forensicCodes` :388-412, `acsFloorRescues` :414-449, `classifiedArtifact` :451-501, `artifactSnapshot`/`statArtifactSnapshot`/`artifactUnchangedSince` :519-545; plus the challenge-token read twin preparation.go:120-126 ≡ runner.go:440-446 single-sourced. `settleSleep` (runner.go:320-324) STAYS in the host. Both stage files are deleted.
- **Module tag:** `runner` (already in the closed set: signalcenter/event.go:71 const, :97-102 `knownModules`; zero `RegisterCode` sites, zero producers, no signalcenter import anywhere in `internal/phases/runner` before this unit) · **Kind:** `runner.warning` (NEW, non-terminal) · **Status:** built 2026-09-14 in `dev/decompose-phaserunner` (uncommitted — the orchestrator lands it as ONE ship commit); synthesized from the judged three-lens panel (byte-identity A · seam/testability C · observability B), built on A with B's code vocabulary and root proof and C's invariants table, probe goldens and preserved-quirk list; the completeness critic's three blockers and every correction folded (§9 "Critic folds").

Base: `8e8f080f` (HEAD == origin/main in `dev/decompose-phaserunner`). Every `file:line` below is against that tree.

## 1. Context

`BaseRunner.Run` (runner.go:547-582) is the Template Method every subagent phase executes through — the ten packages that embed `*runner.BaseRunner` (audit, build, buildplanner, debugger, intent, scout, swarmplan, specrunner, tdd, triage) plus the swarmrunner `Decorator` that wraps scout and build (cmd_cycle.go:505/:509) and the spec runners registered at cmd_cycle.go:569/:1003 and phaseregistrar/registrar.go:194. Its last two steps — `reconcileDeliverable` (:570) and `classifyPhaseOutcome` (:581) — turn (bridge response, bridge error, `deliverable.Verify`) into the `core.PhaseResponse` that core's dispatch loops route on (cyclerun_dispatch.go:227-252, retry_opts.go:174-192). That verdict engine was the component's least observable, most failure-prone code: two decisions were SILENT (the contracted-but-malformed branch classification.go:87-90 and the ship-guard downgrade :120-124 — the exact class of the cycle-1634/1636 F4 triage, "the Classify diagnostic the C1 record could not carry"), one was stderr-only with the forensic content the C1 record cannot carry (`[VERDICT-FORENSIC]` reconciliation.go:170-172 — "the retro's #1 ask"), one was a response diagnostic with no stream twin (the optional degrade :96-113), and two INFO trail lines described one belief (`[runner] ACS-FLOOR` :151 and `[runner] RECONCILED` classification.go:184). None of the four stderr lines was pinned by any test (grep over `internal/`, `cmd/`, `acs/` `*_test.go` for `VERDICT-FORENSIC|ACS-FLOOR|RECONCILED phase|WARN stdout filter`: zero hits, 2026-09-14).

The engine's rules are load-bearing and were pinned only through `Run` with fakes: the teardown arm order stale-refusal → OK → optional → ACS floor → forensic FAIL (reconciliation.go:81-183; cycles 1550, 120, 603/921/924/931), two cancellation policies for ONE ladder (`context.WithoutCancel` at :73 vs the ctx-honouring clean-exit call classification.go:80; rationale runner.go:341-366, cycles 824/825), the single-read rule (classification.go:50-58, runner.go:490-501), the dual return on the FAIL arms (a POPULATED response AND `fmt.Errorf("%s: bridge: %w", …)` :183/:200 so `errors.Is` on `core.ErrArtifactTimeout` / `ErrTransientBridgeFailure` survives — `IsInfraTeardownError` and `bridgeExitCode` read it in core), and the bridge response being meaningful on the error arms (`bres.CostUSD/Tokens/BootMS` read after `bridgeErr != nil`). Three copy-pasted response literals repeated six base fields (:100-112, :173-182, :190-199; a fourth at classification.go:137-149). Two beliefs were spelled twice: the challenge-token read (preparation.go:121-122 vs runner.go:440-446) and the `.evolve` join (classification.go:203 vs `paths.EvolveDirOf`).

Why unit 11 jumps the §12 order (runner.go is 719 lines, under the 800 bar): six functions over the 50-line bar in one package (`New` 76, `preparePhaseExecution` 92, `resolveDispatchPlan` 89, `dispatchPhaseAttempts` 141, and the two above), 24 stderr emit sites over three sinks, and a reserved module tag with nothing behind it. This unit takes the verdict half only. Refused, with reasons: the dispatch/routing/cli_health/fence chain (unit 11b — the ProtectedSurface rows integrity_surface.go:128-129 name `requiresExplanationSandbox` and the `RequireSandbox` call site in dispatch.go; ~30 tests pin the `BridgeRequest` literal; four process-env reaches; 18 INFO trail lines that a WARN-filtered console would DELETE if coded); prompt preparation (a provider prompt-cache key; every prep failure is an error return the C1 chokepoint already codes); `New`'s default ladder and the two set-once globals `DefaultUniversalFallback`/`DefaultDiscoverCLIsFn` — root seams, 11b's; the escalation-report schema twin — the bridge unit's.

## 2. Boundary

| Moves to `verdict` (base lines) | Stays in `runner` / facaded | Why |
|---|---|---|
| `reconcileSettleRetries = 15`, `reconcileSettleInterval = 200ms` (runner.go:315-318) → exported `SettleRetries`, `SettleInterval` | host keeps PROJECTIONS `const reconcileSettleRetries = verdict.SettleRetries; reconcileSettleInterval = verdict.SettleInterval` in the seam file — three tests read the host spellings (runner_reconcile_test.go:394-395, runner_cleanexit_test.go:143-144, runner_reconcile_ctxcancel_test.go:50/:53); zero test edits | single-source-with-projection; the bound is the ladder's |
| `verifyReconcileDeliverable` (:373-386; its 47-line doc condensed onto the leaf function) → unexported `(*Engine).settle(ctx, phase, roots) settled` — loop verbatim (`attempt < SettleRetries && err == nil && !res.OK`; `ctx.Err()` before AND after each sleep; an error ⇒ zero retries) plus the re-probe COUNT the codes carry as `settle_attempts` and the two stale flags the gate stamps afterwards | `var settleSleep = time.Sleep` (:320-324) STAYS: `New` still defaults `sleepFn = settleSleep` and hands it to the leaf as `WithSleep`; runner_cleanexit_test.go:18 `init()` flips it for the whole host binary (acs/cycle276 runs that binary as a subprocess). The leaf has NO package var; its default is `time.Sleep` | the two cancellation policies stay ONE function called two ways; the dual sleep seam is a preserved host quirk (Q1, follow-up F2) |
| `forensicSnapshot`, `forensicCodes` (:393-412) → unexported, verbatim (`data, _ := os.ReadFile` kept — Q2) | — | fields of `RUNNER_TEARDOWN_FAIL` |
| `acsFloorRescues` (:432-449) → unexported, verbatim except the token read goes through `phasecontract.ChallengeToken` | — (only caller reconciliation.go:146 moves) | the audit gate, `coherence.ReadCycleVerdicts`, `"PASS"` twice, `strings.Contains` are the floor's |
| the challenge-token read + `TrimSpace` + non-empty rule (:440-446 AND preparation.go:121-122) → ONE exported `phasecontract.ChallengeToken(workspace string) (string, bool)` (`phasecontract/challenge_token.go`, beside `Contract.RequireChallengeToken` — the review fold of F1; it lived in the leaf as `verdict.ChallengeToken` between the build and the fold) | preparation.go:121-122 became `if token, ok := phasecontract.ChallengeToken(req.Workspace); ok { prep.prompt += … }` — a declared in-package edit; prompt bytes identical (runner_challengetoken_test.go ×3 green; consumer pin test 41, which also source-scans the runner package for the token filename) | never_duplicate: one spelling of "where this cycle's token lives" for the runner AND the engine; the four inline readers outside them (deliverable.go:331, bridge/completion.go:171, bridge/report.go:82, bridge/driver_common.go:63 — report.go keeps a file-present-but-empty distinction this signature folds) are named on the function's doc, not claimed — consolidating them is F1b |
| `classifiedArtifact` (:490-501) → unexported, verbatim | — | the single-read decision (verified bytes first, ONE disk fallback, absent + !OK ⇒ "") |
| `artifactSnapshot`, `statArtifactSnapshot`, `artifactUnchangedSince` (:522-545) → exported `Snapshot` (fields unexported — only `StatSnapshot` mints one), `StatSnapshot(path) (Snapshot, bool)`, unexported `unchangedSince(path, Snapshot) bool` | `phasePreparation.preDispatch` (preparation.go:26) is typed `verdict.Snapshot`; :97 is `verdict.StatSnapshot(prep.artifactPath)` — the only host caller | the (size, mtime) identity is taken pre-dispatch by the host and judged post-dispatch by the leaf — one type; the host never reads the fields |
| `reconcileDeliverable` (reconciliation.go:24-210) → `(*Engine).reconcile(ctx, d)` (pass-through :51 + the `IsInfraTeardownError` fork :61/:185) split into `reconcileTeardown` (:62-85 + the :86 switch: `rootsFor` → `settle(context.WithoutCancel(ctx), …)` → `staleGate` → OK \| `degradeOptional` \| `rescueViaACSFloor` \| `forensicFail`), `staleGate(d, s) settled` (:81-85 — stamps `stale`; sets `refused` + the :83 error only when `err == nil && res.OK`), `degradeOptional` (:91-113 → code 2), `rescueViaACSFloor(d, s)` (:141-153 — a plain function: the ONE late read :141-145 BEFORE the `!stale` check; no event), `forensicFail` (:154-183 → code 1), `substantiveFail` (:185-201 — no code, C1's), `responseBase(d)` (the six fields of :100-107/:173-180/:190-197), `teardownFields`/`cause`/`teardownKind` (the shared vocabulary; `"timeout"`\|`"transient"` via `errors.Is`); arm ORDER preserved verbatim | `phaseReconciliation` (:14-19) DELETED with the file — its only reader was classify, which moved; the substantive arm's `fmt.Errorf("%s: bridge: %w", …)` :200 and the teardown arm's :183 are byte-identical in the leaf | the ≤3-line triage target; 187 lines, nesting 6, three copy-pasted literals |
| `classifyPhaseOutcome` (classification.go:16-187) → `(*Engine).classify(ctx, d, r, classify Classify) core.PhaseResponse` split into `selectVerdictBytes` (:74-92 — the ctx-HONOURING ladder :80; returns a `verdictSource` struct), `writeCleanStdout` (:99-103 → code 5), `applyShipGuard(verdict, unverified) string` (:120-124, pure), `violationDiagnostics(vs) []core.Diagnostic` (:130-135, pure, bytes verbatim), `reconcileTrail(resp, d, r, verdict)` (:150-185 → code 3), `unverifiedSignal(d, before, after, src)` (code 4, fault-only); Diagnostics ORDER verbatim (Classify → fence :106 → violations :130-135 → reconcile warning :169-172 → ACS override :179-182) | `Hooks.Classify` STAYS the phase's: the host binds it per call as a `verdict.Classify` closure over Run's `req` (already stamped `WorktreeVerified` at runner.go:568) and `dispatchResult.bridgeResponse`, so the leaf never sees `core.PhaseRequest` or `runner.Hooks` | the classify half of one decision; the ship guard and the violation trail were untested |
| `verifyRootsFor` (classification.go:196-206) → unexported `rootsFor(d Dispatch) phasecontract.Roots`; `EvolveDir` via `paths.EvolveDirOf(d.ProjectRoot)` (the byte-identical join) under the same `ProjectRoot != ""` guard | — (both callers move; runner_verify_roots_cycle_test.go pins through `Run`) | the ONE translation, now also the ONE `.evolve` spelling (03b review fold (a)); leaf test 31's source scan forbids an inline `".evolve"` |
| the four stderr sites in the moved code (reconciliation.go:151, :170-172; classification.go:101, :184) → codes 3, 1, 5, 3 (§5) | ALL 20 other sites STAY VERBATIM through their sinks: routing.go:49/:72/:74/:81/:91/:98/:102, dispatch.go:82-84/:99/:127/:141/:151, preparation.go:87, cli_health.go:58/:64/:67/:126/:129, worktree_fence.go:33/:43. The `log` import leaves with the two deleted files; the leaf never imports `internal/log` | the operator's bar; the leaf writes no stderr |
| `Run` (runner.go:547-582): :548-568 verbatim; :570-581 → `return b.judge.Judge(ctx, dispatchOf(req, prep, dispatchPlan, dispatchResult), b.classifyWith(req, dispatchResult.bridgeResponse))` (36 → 22 lines) | STAYS: the spelling `Run(ctx, req)`, the early-response protocol for prepare/route (:549-566), `req.WorktreeVerified = dispatchResult.worktreeVerified` (:568). The reconcile-side `err != nil && early == nil` branch (:574-575) was UNREACHABLE (every error return at :183/:200 carries a response) and is removed with the fold — declared (rule 3); `Judge` collapses to `if early != nil { return *early, err }` | Template Method kept |
| — | STAYS verbatim: `Hooks`/`Skipper`/`InlinePromptProvider`/`SecondaryArtifactsProvider`; `Options` (every field name; acs/cycle46:180-186 reflects on `DisableStdoutFilter`) + ONE new field `Signals func() *signalcenter.Center`; `BaseRunner` MINUS the five fields the engine now holds alone (`verifyFn`, `sleepFn`, `optional`, `stdoutFilter`, `disableStdoutFilter` — the review fold of F4; the two `b.verifyFn` pins were retargeted through `Run`, see §6 tests 36b/36c) + ONE new field `judge`; `New` ends in `b.judge = wiredVerdictEngine(opts)` (eager, the engine's ONE moment of truth; its three seam ladders moved into `wiredVerdictEngine`, so `New` is 79 → 51 lines — the rest of F3 stays open); `withExplanationContract`/`requiresExplanationSandbox` (manifest row integrity_surface.go:128 stays true); the prompt exports (acs/cycle534/646 pin `BaseCycleContext`/`StaticPrefix` bytes); `PersonaAvailable`; preparation.go, routing.go, dispatch.go (row :129 stays true — NOT edited), cli_chain.go, cli_health.go, lanescope.go, worktree_fence.go | dispatch chain and prompt assembly are 11b/11c; the port `core.PhaseRunner` and the ten embedders see no change |
| — | NEW host seam file `verdict_engine.go` (113 lines): `signalSource` (unexported optional interface), `resolveSignals(opts)`, `wiredVerdictEngine(opts)` the ONE construction AND the ONE resolution of the engine's seams (no lazy accessor — a BaseRunner has no production literal, so the lazy branch was dead code with an unsynchronised write; review fold), `dispatchOf(req, prep, plan, res)` the ONE projection (KEYED literal — `go vet` composites rejects a positional literal of an imported struct), `classifyWith(req, bres)`, `SignalsWired()`, the two const projections | Strangler Fig: `runner.New(Options{…})` unchanged at all 15 production construction sites |
| — | NEW in `adapters/bridge/bridge.go` beside `SignalsWired`: `func (a *Adapter) Signals() *signalcenter.Center` (nil-receiver-safe — the typed-nil hazard of an optional-interface adoption); NEW in `swarmrunner`: `func (d *Decorator) SignalsWired() bool` forwarding through `interface{ SignalsWired() bool }` on `d.inner` in the `PersonaAvailable` idiom (swarmrunner.go:304-309); NEW additive root field `orchDeps.Runners map[core.Phase]core.PhaseRunner` (cmd_cycle.go, set from the runner map) | the production Center reaches every runner through the collaborator all 15 sites already inject; the root field follows the `orchDeps.Bridge` precedent (exists only for the S3 wiring proof) |
| — | Declared in-package edit #4: preparation.go:47's local `verdict` (the skip arm) renamed `skipVerdict` — the file now imports package `verdict` and a same-named local is a reviewer-fleet smell even where legal | the critic's shadowing fold |
| — | OUT OF SCOPE, named: the seven other production `BridgeRequest` assemblers (swarmrunner.go, retro.go:192-224 with its own fence/retry, subagent ×2, cmd_models_live.go, core/failure_advisor.go, core/phase_judge.go); the escalation-report schema twin; the dead store dispatch.go:155; acs/cycle25's standing red; the 9 Center-less `bridge.NewDefault(…, nil)` roots (triage/scout/intent/audit/retro/debugger/build/tdd factories + cmd_campaign.go) — pinned by `TestNilSignalBridgeRootsAreExplicit` | 11b / the bridge unit / the operator (§9 questions) |

Leaf home and imports. PATH `go/internal/phases/runner/verdict` (package `verdict`). It cannot live under `internal/core/<leaf>` like units 01-03b: `deliverable` imports `core` and the leaf is welded to `core.PhaseResponse/BridgeResponse/Diagnostic` and `deliverable.Result/Violation`. Under the host it is cycle-safe: `core`, `coherence`, `deliverable`, `signalcenter`, `paths`, `phasecontract` each reach `internal/phases/runner` 0 times (go list, 2026-09-14); the host imports the leaf, the leaf never imports the host — compiler-enforced. Direct imports (the allowlist pinned by `importgraph_test.go` — DIRECT imports only; `coherence` reaches `policy`, `profiles`, `phasespec`, `config`, `gcpolicy` transitively): stdlib `context`, `errors`, `fmt`, `os`, `path/filepath`, `strconv`, `strings`, `time`; `internal/core`, `internal/deliverable`, `internal/phasecontract`, `internal/coherence`, `internal/paths`, `internal/signalcenter`. NEVER directly `internal/log`, `internal/phases/runner`, `internal/adapters/*`, `internal/bridge`, `internal/llmroute`, `internal/policy`, `internal/prompts`, `os/exec`; the leaf writes no stderr, reads no env (`TestNoStderrNoEnv_InTheLeaf` pins `os.Stderr|os.Stdout|os.Getenv|os.LookupEnv|os.Environ|log.Diag|fmt.Print|fmt.Fprint` absent). Collision check: no `package verdict` and no `internal/verdict` in the tree; `internal/verdictcache` is a near-homonym kept distinct. Package sweep check: acs/cycle276:112 runs `./internal/phases/runner/` (single package), acs/cycle534:202-203 vets the runner DIRECTORY, `.apicover-enforce` row `./internal/phases/runner` — none sweeps the sub-package. ProtectedSurface fragments: `/go/internal/phases/runner/verdict/` (trailing slash) is not a substring of `/go/internal/phases/runner/verdict_engine.go`, and neither contains row :128's `/go/internal/phases/runner/runner.go` — each got its own row.

Line projection (landed): runner.go 719 → 500; reconciliation.go and classification.go DELETED (−416); preparation.go ±0 (four token edits); verdict_engine.go +93; adapters/bridge +11; swarmrunner +12; cmd_cycle.go +6; leaf 812 production lines across five files (`verdict.go` 180, `settle.go` 87, `reconcile.go` 222, `classify.go` 172, `artifact.go` 151 — the moved doc comments kept verbatim account for the width; every function < 50 lines, nesting ≤ 3, asserted by `limits_test.go`).

## 3. API

```go
package verdict // go/internal/phases/runner/verdict — ADR-0103 unit 11: the phase runner's verdict engine

// Codes — registered in init() via signalcenter.RegisterCode(signalcenter.ModuleRunner, …) with the §5 docs.
const (
    CodeTeardownFail          signalcenter.Code = "RUNNER_TEARDOWN_FAIL"
    CodeOptionalPhaseDegraded signalcenter.Code = "RUNNER_OPTIONAL_PHASE_DEGRADED"
    CodeReconciled            signalcenter.Code = "RUNNER_RECONCILED"
    CodeDeliverableUnverified signalcenter.Code = "RUNNER_DELIVERABLE_UNVERIFIED"
    CodeStdoutFilterFailed    signalcenter.Code = "RUNNER_STDOUT_FILTER_FAILED"
)

// The settle ladder's bounds — runner.go:315-318 verbatim (≈ 3 s liveness ceiling, cycle-921 / ADR-0072);
// the host projects them under their old names for the three settle tests.
const (
    SettleRetries  = 15
    SettleInterval = 200 * time.Millisecond
)

// Verify is the deliverable probe as the host resolves it (Options.VerifyFn or the catalog-aware default).
// Classify is the phase's verdict hook bound by the host PER CALL over its request and bridge response —
// the leaf never sees core.PhaseRequest or runner.Hooks (Strategy).
type Verify   func(phase string, roots phasecontract.Roots) (deliverable.Result, error)
type Classify func(artifact string) (verdict string, diags []core.Diagnostic, nextPhase string)

// Snapshot is the (size, mtime) identity of the canonical artifact at one instant; StatSnapshot is
// runner.go:528-534 verbatim (a non-empty regular file, Lstat). Fields unexported: only StatSnapshot mints one.
type Snapshot struct{ /* size int64; modTime time.Time */ }
func StatSnapshot(path string) (Snapshot, bool)

// (ChallengeToken lived here between the build and the review fold; it is phasecontract.ChallengeToken now —
// go/internal/phasecontract/challenge_token.go, beside Contract.RequireChallengeToken — and the leaf's ACS
// floor and the host's prompt preparation both read through it. The leaf never spells the token file: test 31b.)

// Dispatch is the ONE input shape (16 fields): the seven request fields the engine reads, what preparation
// snapshotted, and what the bridge did. The host projects it ONCE (dispatchOf, keyed); the leaf's own test
// constructs it POSITIONALLY so a new field fails to compile there. Bridge is meaningful even when
// BridgeErr != nil (CostUSD/Tokens/BootMS/ExitCode are read on the error arms).
type Dispatch struct {
    Cycle int; RunID, Phase, Workspace, Worktree, ProjectRoot string; ExplanationDocumentationVersion int
    ArtifactPath string; PreDispatch Snapshot; HadPreDispatch bool
    Bridge core.BridgeResponse; BridgeErr error; DurationMS int64; ResolvedModel, ModelSource string
    FenceDiagnostics []core.Diagnostic
}

// Engine judges one dispatch outcome. The probe is required positionally; the rest are options with the
// host's defaults. No clock, no store, no stderr.
type Engine struct{ /* verify Verify; sleep func(time.Duration); stdoutFilter func(workspace, phase string) error; optional bool; signals func() *signalcenter.Center */ }
type Option func(*Engine)
func New(verify Verify, opts ...Option) *Engine                        // nil verify panics at first use — no guard
func WithSleep(fn func(time.Duration)) Option                          // default time.Sleep; the host passes its resolved sleepFn
func WithStdoutFilter(fn func(workspace, phase string) error) Option   // nil = no companion (the host omits it under DisableStdoutFilter) — Null Object
func WithOptional(optional bool) Option                                // the WARN-degrade arm
func WithSignals(c func() *signalcenter.Center) Option                 // late-bound accessor; nil accessor or nil Center = Null Object
func (e *Engine) SignalsWired() bool

// Judge is the fifth and sixth step of the host's Run template: (populated FAIL response,
// fmt.Errorf("%s: bridge: %w", phase, bridgeErr)) on the teardown-FAIL and substantive arms; (WARN, nil) on
// the optional arm; (classified response, nil) otherwise.
func (e *Engine) Judge(ctx context.Context, d Dispatch, classify Classify) (core.PhaseResponse, error)

// unexported, in-package tested
type settled struct{ res deliverable.Result; err error; attempts int; stale, refused bool }
type reconciliation struct{ reconciled bool; verified deliverable.Result; acsFloorRescued bool; overriddenCodes string; attempts int }
type verdictSource struct{ artifact string; violations []deliverable.Violation; unverified bool; attempts int }
func (e *Engine) settle(ctx context.Context, phase string, roots phasecontract.Roots) settled
func rootsFor(d Dispatch) phasecontract.Roots                                                              // paths.EvolveDirOf
func (e *Engine) reconcile(ctx context.Context, d Dispatch) (reconciliation, *core.PhaseResponse, error)
func (e *Engine) reconcileTeardown(ctx context.Context, d Dispatch) (reconciliation, *core.PhaseResponse, error) // rootsFor → settle(WithoutCancel) → staleGate → OK | degradeOptional | rescueViaACSFloor | forensicFail
func staleGate(d Dispatch, s settled) settled                                                              // stamps stale; refuses (err + zero Result + refused) only when err == nil && res.OK
func (e *Engine) degradeOptional(d Dispatch, s settled) core.PhaseResponse                                 // → CodeOptionalPhaseDegraded
func rescueViaACSFloor(d Dispatch, s settled) (reconciliation, bool)                                       // the ONE late read first, then !stale && acsFloorRescues; no event
func (e *Engine) forensicFail(d Dispatch, roots phasecontract.Roots, s settled) (core.PhaseResponse, error) // → CodeTeardownFail
func substantiveFail(d Dispatch) (core.PhaseResponse, error)                                               // no event (the C1 chokepoint's)
func responseBase(d Dispatch) core.PhaseResponse                                                           // Phase, ArtifactsDir=Workspace, CostUSD, Tokens, DurationMS, BootMS
func teardownFields(d Dispatch, s settled) map[string]string                                               // teardown, exit, cause, verr, stale_leftover, settle_attempts, deliverable
func cause(s settled) string                                                                               // stale_leftover | unverifiable | malformed — from the flags, never from message text
func teardownKind(err error) string                                                                        // "timeout" | "transient"
func (e *Engine) classify(ctx context.Context, d Dispatch, r reconciliation, classify Classify) core.PhaseResponse
func (e *Engine) selectVerdictBytes(ctx context.Context, d Dispatch, r reconciliation) verdictSource        // the ctx-HONOURING ladder
func (e *Engine) writeCleanStdout(d Dispatch)                                                              // → CodeStdoutFilterFailed
func applyShipGuard(verdict string, unverified bool) string                                                // pure
func violationDiagnostics(vs []deliverable.Violation) []core.Diagnostic                                    // pure, bytes verbatim
func (e *Engine) reconcileTrail(resp *core.PhaseResponse, d Dispatch, r reconciliation, verdict string)    // → CodeReconciled (ONE event, via=verify|acs_floor)
func (e *Engine) unverifiedSignal(d Dispatch, before, after string, src verdictSource)                     // → CodeDeliverableUnverified when after == FAIL
func acsFloorRescues(phase, workspace, report string) bool                                                 // via phasecontract.ChallengeToken
func classifiedArtifact(res deliverable.Result, artifactPath, pane string) string
func forensicSnapshot(path string, tailN int) string                                                       // the discarded ReadFile error kept
func forensicCodes(vs []deliverable.Violation) string
func unchangedSince(path string, snap Snapshot) bool
const ( reportTail = 160; acsTail = 200 )
func (e *Engine) center() *signalcenter.Center
func (e *Engine) warn(origin string, d Dispatch, code signalcenter.Code, reason string, fields map[string]string) // the ONE producer: empty values dropped; Module runner, Kind runner.warning, WARN, Cycle/Phase/RunID from d
```

```go
package runner // go/internal/phases/runner/verdict_engine.go — the unit-11 seam

type signalSource interface{ Signals() *signalcenter.Center }          // unexported optional capability of a core.Bridge
func resolveSignals(opts Options) func() *signalcenter.Center          // Options.Signals wins; else the Bridge's method value; else nil
func wiredVerdictEngine(opts Options) *verdict.Engine                  // the ONE construction (needle `verdict.New(`) and the ONE resolution of the
                                                                       // probe (VerifyFn | catalog-aware over PhaseIO), clock (SleepFn | settleSleep),
                                                                       // filter (StdoutFilter | logfilter.Process; nil when disabled), Optional, Signals
func dispatchOf(req core.PhaseRequest, prep phasePreparation, plan phaseDispatchPlan, d phaseDispatchResult) verdict.Dispatch // keyed
func (b *BaseRunner) classifyWith(req core.PhaseRequest, bres core.BridgeResponse) verdict.Classify
func (b *BaseRunner) SignalsWired() bool                                // the root-wiring proof; the Decorator forwards it
const ( reconcileSettleRetries = verdict.SettleRetries; reconcileSettleInterval = verdict.SettleInterval )
```

Host deltas: `Options.Signals func() *signalcenter.Center` after `Diag`; `BaseRunner.judge` (and MINUS `verifyFn`, `sleepFn`, `optional`, `stdoutFilter`, `disableStdoutFilter` — review fold F4: the engine is their only home); `New` ends in `b.judge = wiredVerdictEngine(opts)` (its three seam ladders moved with them: 79 → 51 lines; runner.go 500 → 467); `Run` :570-581 → the one `Judge` call on `b.judge`; imports `coherence`, `os`, `path/filepath`, `logfilter` left runner.go, `verdict` and `signalcenter` joined (`logfilter`, `deliverable`, `phasecontract` joined verdict_engine.go); Run's godoc (runner.go:506-518, attached to `type artifactSnapshot` by a misplacement) re-attached to `func Run` with its stale steps corrected (D8). preparation.go: :26 `verdict.Snapshot`, :97 `verdict.StatSnapshot(`, :121-122 `phasecontract.ChallengeToken(`, :47 `skipVerdict`. `phasecontract/challenge_token.go` (+test): the re-homed reader (62/62 exports on that apicover row). `adapters/bridge/bridge.go`: `Signals()` beside `SignalsWired()` with a nil-receiver guard. `swarmrunner.go`: `SignalsWired()` beside `PersonaAvailable`. `cmd_cycle.go`: `orchDeps.Runners`. `signalcenter/event.go`: `KindRunnerWarning Kind = "runner.warning"` + `knownKinds`; NOT `terminalKinds`; event_test.go `all` (+1); the module count literal stays 26.

## 4. Patterns

| Pattern | Force it answers |
|---|---|
| **Extract Class** (`verdict.Engine`) + **explicit DI at construction** (`New(verify, opts…)`, functional `Option`) — the unit-02/03b positional-required-deps shape | the engine reads six receiver fields (verifyFn, sleepFn, optional, stdoutFilter, disableStdoutFilter, hooks.Classify) and four unexported stage objects; 16 host test files already inject VerifyFn/SleepFn — the leaf accepts exactly those, no globals; `limits_test.go` (50/4/800) makes the split real |
| **Strategy** — `Verify` (the catalog-aware probe or a fake) and `Classify` (each phase's own verdict rule incl. audit's I/O-bearing hook) bound PER CALL by the host (`classifyWith`) | the leaf judges bytes it never reads itself and a verdict rule it never owns; the hook needs `PhaseRequest` (22 fields) of which the engine reads 7 — a closure keeps the leaf free of `runner.Hooks` and of the request |
| **Parameter Object** (`Dispatch`, 16 fields; `dispatchOf` the ONE keyed projection) + **positional shape guard in the leaf test** + **Result Objects** (`settled`, `reconciliation`, `verdictSource`, unexported) + **Factory Method** (`responseBase`, `teardownFields`) | the two stage functions unpacked 12-15 locals from 4-6 stage structs; four literals repeated six fields; a new field must break the build (leaf test 14) — never `go vet` at the host |
| **Late-bound Center accessor + Null Object** (`WithSignals(func() *Center)`, read at every use; `(*Center)(nil).Emit` no-op; nil filter = disabled) + **optional-interface adoption** (`signalSource`) | the Center exists only in the production roots and is already carried by the injected Bridge; nine registry-factory roots are Center-less by design and stay byte-identical; `DisableStdoutFilter` folds to "no filter function" without a boolean in the leaf |
| **ONE eager construction (= ONE resolution of the seams) + source-scan guards** (`wiredVerdictEngine(opts)`, called once at the end of `New`; `TestVerdictEngine_OneConstructionSite`, needle `verdict.New(`, `nonTestSourcesMentioning` with the module root `../../..` and skip prefix `internal/phases/runner/verdict/`; `TestNew_BuildsTheEngineOnceEagerly`, `.judge = ` assigned once in the package) — NO lazy accessor: the protocol's `x()`/`wiredX()` pair was built, then dropped at the review fold (a BaseRunner has no production literal, so the lazy branch was dead code with an unsynchronised write whose only client was its own test) | `New` is exported; "one wired engine per runner" and "one moment of truth for the resolved seams" must be structurally true, not conventional |
| **Strangler Fig facade** — `Run` keeps its spelling and four-stage shape; `runner.New(Options{…})` unchanged at all 15 sites; `Options.DisableStdoutFilter` keeps its name; the const projections (the five duplicated BaseRunner fields were dropped at the review fold, F4 — the two `b.verifyFn` pins now go through `Run`) | ADR-0103's zero-call-site rule; ten embedders, the Decorator and phaseregistrar never learn the unit exists; three tests read the consts |
| **Single-source-with-projection + consumer pin** — `phasecontract.ChallengeToken` (pins: host test 41 + its source scan of the runner package, leaf test 31b's source scan), `Snapshot`/`StatSnapshot` (pin: test 41), `rootsFor` via `paths.EvolveDirOf` (pin: leaf test 31's source scan), `SettleRetries` (three host tests through the projections); the clean-stdout companion's filename is NOT re-spelled by the leaf (`RUNNER_STDOUT_FILTER_FAILED` carries the workspace only — leaf test 27 forbids a second key) | never_duplicate (HIGHEST): two spellings of one token contract inside the runner (four more outside it are named, F1b); six inline `.evolve` spellings across the runner (the leaf fixes its one; the rest are 11b's); the companion path is `logfilter`'s belief |
| **Extract Method ×15 under a `limits_test.go` guard** (`reconcileTeardown` / `staleGate` / `degradeOptional` / `rescueViaACSFloor` / `forensicFail` / `substantiveFail` / `responseBase` / `teardownFields` / `cause` / `teardownKind` / `selectVerdictBytes` / `writeCleanStdout` / `applyShipGuard` / `violationDiagnostics` / `reconcileTrail` / `unverifiedSignal`) — order preserved by construction | 187- and 172-line functions, nesting 6; the bar (< 50, ≤ 4) asserted by the copied failurelearning/limits_test.go |
| **One producer helper** (`warn`) with `Origin` = the emitting method, empty field values omitted (the `MaxFields = 12` budget: `RUNNER_TEARDOWN_FAIL` carries at most 11 keys) and a discriminating field per code (`cause` / `via` / `downgraded`) | one belief, one code, one line; the ≤3-line triage reads the failing step off `origin` — no `fields.step` (it would repeat the origin) |
| **Characterization goldens** (twelve PhaseResponse goldens, the forensic stderr line, the per-scenario probe/sleep counts incl. two cancelled-ctx rows, the empty stream record — captured on 8e8f080f, paths templated) | the response is the ledger/dossier/dashboard contract; the probe counts pin BOTH cancellation policies |
| **Import-allowlist + no-stderr/no-env source scans** (`importgraph_test.go`, failurelearning idiom) | the leaf-ness declaration ADR-0103 wants |
| Deliberately NOT used: an **FS interface** / `WithCycleVerdicts` — a missing file or a directory at a path provokes every error branch with `t.TempDir` (03b's YAGNI precedent); **predicate injection** for `IsInfraTeardownError` (the sentinels are core's); a **second INFO kind** (INFO is invisible on the console; 11b's); **Config-struct threading** of the Center (15 call-site edits for one accessor); a **`RUNNER_STALE_LEFTOVER_REFUSED` code** (a cause, not a belief); an **`Event.Attempt` stamp** (needs `phaseDispatchResult.attempts` in dispatch.go — ProtectedSurface row :129; 11b's `phase.dispatched` carries the attempt) | |

## 5. Module contract

| Item | Value |
|---|---|
| Module | `runner` — ALREADY in the closed set; NO `knownModules` edit, the 26-module count literal stays. Code prefix `RUNNER_`; all five match codeRE. Package (`verdict`) ≠ tag is deliberate: the tag is the component the operator triages, this leaf is its verdict engine, and unit 11b (the dispatch chain) will share the tag |
| Kind | `runner.warning` — NEW, non-terminal: const after `KindFailureLearningWarning`, `knownKinds`, NOT `terminalKinds` (the disposition is the orchestrator's C1 record); event_test.go `all` (+1). Rejected: a second INFO kind (invisible on the WARN-filtered console) |
| Common fields | `Event.Cycle = d.Cycle`, `Event.Phase = d.Phase`, `Event.RunID = d.RunID`; `Origin` = the emitting method; empty field values omitted; every value passes `Normalize` |
| `RUNNER_TEARDOWN_FAIL` WARN · origin `Engine.forensicFail` | a bridge INFRA teardown ended the session and no trustworthy deliverable rescued it — a mandatory phase FAILs and core's retry loop classifies the wrapped sentinel. reason = the FAIL diagnostic's own text (`<bridgeErr>` [+ `; deliverable not trustworthy: <Violations[0].Message | verr>`] — incl. the cycle-1550 refusal). fields (≤ 11 keys under `MaxFields = 12`, one slot of slack; leaf test 22 asserts no `truncated`): `teardown` ∈ {timeout, transient}, `exit`, `cause` ∈ {stale_leftover (the refusal fired), unverifiable (verr != nil), malformed (!res.OK — absent is `codes=missing_artifact`)}, `codes` (forensicCodes; omitted when empty), `verr` (omitted when nil), `roots` = `ws=… wt=… evolve=…` (the old line's own token, ONE field — the critic's fold), `report` = forensicSnapshot(artifact, 160) and `acs` = forensicSnapshot(acs-verdict.json, 200) (`size=N tail="…"` / `absent` verbatim), `stale_leftover` ∈ {true,false} (true with cause=malformed is real), `settle_attempts` (0-15 re-probes), `deliverable`. REPLACES reconciliation.go:170-172 — every former value survives as a field (the golden line reproduces from the fields: leaf test 22) |
| `RUNNER_OPTIONAL_PHASE_DEGRADED` WARN · origin `Engine.degradeOptional` | an optional phase hit a teardown with no trustworthy deliverable and degraded to WARN; the cycle continues. reason = the WARN diagnostic text verbatim (with the ` [deliverable unverifiable: %v]` suffix when the probe errored). fields: `teardown`, `exit`, `cause`, `verr`, `stale_leftover`, `settle_attempts`, `deliverable`. SILENT before — declared stream + stderr ADDITION (D4). Its own code: it returns (WARN, nil), a different belief and consumer from the FAIL arm's |
| `RUNNER_RECONCILED` WARN · origin `Engine.reconcileTrail` | a teardown was overridden by a well-formed deliverable (via Verify) or by the ACS deterministic floor; the response carries `Reconciled=true` and core files the `reconciled_timeout` ledger disposition (this event carries the REASON). reason = the trail message (classification.go:163 or :167 verbatim). fields: `via` ∈ {verify, acs_floor}, `verdict`, `deliverable`, `teardown`, `exit`, `settle_attempts`, `overridden_codes` (acs_floor only). REPLACES classification.go:184 AND reconciliation.go:151 — two lines, one belief, ONE event emitted with the verdict known; it is emitted AFTER `RUNNER_STDOUT_FILTER_FAILED` when both fire (formerly ACS-FLOOR printed before the filter WARN and RECONCILED after — D2; leaf test 27, host test 40) |
| `RUNNER_DELIVERABLE_UNVERIFIED` WARN · origin `Engine.unverifiedSignal` | a CONTRACTED deliverable was still not OK after the settle window on the clean-exit path AND the final verdict is FAIL — the ship guard downgraded a clean-ship verdict or Classify itself returned FAIL. FAULT-ONLY: a legitimate WARN/SKIPPED pass-through (intent delta's `[intent-unchanged]` → SKIPPED) emits nothing — the violation codes still reach the response diagnostics. reason: `contracted deliverable <path> failed verification after the settle window (<codes>); verdict <before> → <after>`. fields: `codes`, `downgraded` ∈ {true,false}, `verdict_before` (omitted when Classify returned ""), `verdict`, `settle_attempts`, `deliverable`. SILENT before — declared ADDITION (D5) |
| `RUNNER_STDOUT_FILTER_FAILED` WARN · origin `Engine.writeCleanStdout` | the clean-stdout companion of the phase's raw log (logfilter's `<phase>-stdout.clean.txt`) could not be written; the phase continues, the raw log stays the forensic source. reason: `stdout filter failed: <err> (the raw log stays the forensic source)`. fields: `workspace` only — the phase is the event's own and the companion's filename is the injected writer's belief (a `path` key the build added re-spelled logfilter.go:28 and named a file the engine never writes; dropped at the review fold, leaf test 27 forbids a second key). REPLACES classification.go:101 |
| NOT coded (one belief, one producer) | the substantive bridge error (reconciliation.go:185-201) and every plain error return — the C1 chokepoint emits `ORCHESTRATOR_PHASE_ABORTED` / `_PHASE_VERDICT_FAIL` from the SAME returned error (leaf test 19 pins zero `RUNNER_*` events on that arm); the runner's codes carry only what that record cannot. The bridge's `BRIDGE_*`/`LIVENESS_*` describe the session, the runner's the verdict — no overlap. Stale-leftover refusal and settle exhaustion are FIELDS, not codes |
| Console rendering | root `StderrSink` (values unquoted, sorted by key): `[runner] runner.warning WARN RUNNER_TEARDOWN_FAIL cycle=N phase=audit seq=s origin=Engine.forensicFail — bridge: launch exit=81: core: bridge artifact timeout; deliverable not trustworthy: … acs=absent cause=malformed codes=missing_challenge_token,bad_verdict deliverable=… exit=81 report=size=246 tail="…" roots=ws=… wt=… evolve=… settle_attempts=15 stale_leftover=false teardown=timeout`. All five codes are WARN, so the three formerly-INFO lines change FORMAT on stderr but do not disappear (an INFO code would have deleted them from the console) |
| Removed lines | four of the package's 24: reconciliation.go:151, :170-172; classification.go:101, :184 → codes 3, 1, 5, 3. None was pinned by any test. The 20 others stay verbatim (§2); their codes are drafted below for 11b |
| Registry facts honoured | `RegisterCode` does not check `Module.Known()` — moot, `runner` is known; `TestRegistryConflicts_RealRegistryIsClean` and `TestCode_FormatAndModulePrefix` green; `docs/architecture/signal-codes.md` regenerated 92 → 97 with `### runner` between `### outcome` and `### ship`; `TestSignalsCodes_RepoDocIsInSyncWithTheLinkedRegistry` green after `make -C go build && ./go/bin/evolve signals codes generate`. The code docs avoid bare `\|` characters (the generator does not escape table cells — a pre-existing quirk of the failurelearning rows) |
| Drafted for unit 11b (registered THEN, not now) | `RUNNER_EVENTS_PRODUCER_FAILED` (dispatch.go:127), `RUNNER_CHAIN_REORDERED` `fields.cause` ∈ {probe, driver_bench, bench} (routing.go:81, cli_health.go:58/:64), `RUNNER_UNIVERSAL_FALLBACK_APPLIED` (routing.go:91), `RUNNER_ALL_CANDIDATES_BENCHED` (cli_health.go:67), `RUNNER_FAMILY_BENCHED` (:129), `RUNNER_BENCH_WRITE_FAILED` (:126), `RUNNER_ESCALATION_REPORT_MALFORMED` (:116-118, silent), `RUNNER_WORKTREE_FENCE_FAILED` with treefence `Outcome` fields (worktree_fence.go:33/:43); the existing INFO kind `phase.dispatched` with `Attempt` stamped from `len(attemptLog)` for routing.go:98/:102 + dispatch.go:82-84/:141/:151 |

## 6. Tests (red first)

Every test was written before the code it names. Step 1's host pins were GREEN characterization tests on 8e8f080f, each proven RED by hand-applying its named mutant once (§10); step 2's tests were red because the package, the kind and the codes did not exist (`go vet` on the leaf: `undefined: Snapshot`; on signalcenter: `undefined: KindRunnerWarning`); step 3's because the accessor, the fields, the exports and the projection did not exist (`b.verdictEngine undefined`, `a.Signals undefined`, `Decorator … SignalsWired undefined`, `d.Runners undefined`). `go test -count=1` always; fakes and `t.TempDir` only; no `t.Skip`; every leaf test injects `WithSleep` (no real sleep anywhere — the leaf suite runs in 0.4 s).

Step 1 — host pins and goldens (package runner, `verdict_scenarios_test.go` / `verdict_golden_test.go` / `verdict_pins_test.go`, zero production edits): the ONE scenario table (`verdictScenarios`, twelve rows — the design's ten plus `transient_malformed_mandatory` for the transient sentinel and both stale rows) drives a `goldenBridge` with the fixed `BridgeResponse{ExitCode 81, CostUSD 1.5, Tokens{Input 42}, BootMS 7}` and a fixed clock through `runner.New` + `Run`.

1. `TestRun_VerdictResponses_MatchTheGoldenOnEveryArm` — `json.Marshal(resp)` with `{ws}/{wt}/{root}` templated == `verdict/testdata/response_<scenario>.golden.json` (12 files); the error string and `errors.Is` per row — kills `BootMS dropped from the substantive literal`, `%w → %v`, `ArtifactsDir from Worktree`.
2. `TestRun_VerdictDiagnostics_OrderIsClassifyFenceViolationsReconcileOverride` — Classify ×2 → the fence (a read-only phase on a non-repository worktree: one `snapshot unavailable` diagnostic) → violations; rescued: Classify ×2 → the trail → the ACS override; Verify-reconciled: the trail only — kills `fence diags appended after violations`, `reconcile diag before Classify diags`.
3. `TestRun_ContractViolations_BecomeErrorDiagnosticsOncePerViolation` — kills `violations dropped`, `severity warning`.
4. `TestRun_ShipGuardDowngrade_LeavesNextPhaseUntouched_AndPassesThroughFailWarnSkipped` — PASS/""/"BOGUS" → FAIL; FAIL/WARN/SKIPPED unchanged; NextPhase "ship" preserved; a VERIFIED "BOGUS" passes through — kills `WARN removed from the case list`, `nextPhase cleared on downgrade`.
5. `TestRun_TeardownFail_DiagnosticCarriesTheCause` (retargeted at the fold — see D1) — the first violation's message; the stale refusal named — kills `Violations[0] → [1]`. On the base this test also captured the `[VERDICT-FORENSIC]` line into `verdict/testdata/stderr_forensic.golden.txt` (two lines: malformed, stale — the report lengthened to 246 bytes so the 160-byte tail is a real suffix) and proved `tail 160 → 200`, `tail from the head` and `roots.EvolveDir empty` red; the directory-at-acs-verdict arm was asserted by shape only (st_size of a directory is platform-dependent; ubuntu and macos both run CI).
6. `TestRun_TeardownFail_ReturnsPopulatedResponseAndAnInfraTeardownError` — kills `zero response on the error arm`, `verify consulted on the substantive arm`.
7. `TestRun_VerdictProbeCounts_MatchTheGolden` + `TestRun_VerdictProbeCounts_CancelledCtx` — VerifyFn/SleepFn counts per scenario == `verdict/testdata/probes.golden.json` (17 rows: the twelve + settles-on-3rd ×2, clean-never-settles, cancelled_teardown 16/15, cancelled_clean 1/0) — kills `WithoutCancel removed`, `pre-sleep ctx check dropped`, `bound off-by-one`.
8. `TestRun_Teardown_VerifyProducedNoBytes_LateReadFeedsBothRescueAndClassify` — kills `late read dropped`.
9. `TestRun_StdoutFilter_RunsAfterVerdictSelectionAndBeforeClassify` — `[verify, filter, classify]`; `DisableStdoutFilter` → `[classify]` — kills `filter after Classify`.
10. Throwaway `TestGoldenWriter_CaptureOnTheBase` (deleted): wrote the twelve response goldens, `probes.golden.json`, `stderr_forensic.golden.txt` and `stream_sequences.golden.txt` (EMPTY for module `runner` on 8e8f080f — every scenario `[]`; the record of the base) on 8e8f080f.

Step 2 — the leaf and the closed set (package verdict unless noted):

11. `TestRunnerCodes_AreRegisteredWithDocsUnderModuleRunner` — five codes, `RUNNER_` prefix, docs non-empty, exactly five under the module.
12. `TestKind_ClosedSetAndTerminal` extended (signalcenter) — `KindRunnerWarning` Known and not Terminal — kills `kind left out of the closed set`, `kind marked terminal`.
13. `TestLimits_FunctionsFilesAndNestingStayWithinTheBar` + `TestImportGraph_LeafImportsOnlyTheDeclaredPackages` + `TestNoStderrNoEnv_InTheLeaf`.
14. `TestDispatch_HasExactlyTheDeclaredFields` — positional literal of all 16 fields.
15. `TestNew_PanicsAtFirstUseWithoutVerify` + `TestWithSignals_NilIsTheNullObject` + `TestWithSignals_ReadsTheCenterLive` + `TestSignalsWired` — kills `guard added`, `Center snapshotted at construction`.
16. `TestSettle_BoundedAt1PlusRetries_StopsOnOKOrError_CountsReprobes` — never-OK 16/15/attempts 15 at `SettleInterval`; OK on probe 4 → 4/3/3; verr → 1/0/0 — kills `< → <=`, `loop on verr`, `attempts miscounted`, `250ms`.
17. `TestSettle_HonoursCancellationBeforeAndAfterTheSleep` — kills `pre-sleep ctx dropped`, `post-sleep ctx dropped`.
18. `TestJudge_TeardownLadderIsCancellationImmune_CleanExitLadderIsNot` — kills `WithoutCancel removed`, `WithoutCancel added to the clean path`.
19. `TestJudge_PassThroughWhenNoBridgeError` (the no-probe half scoped to `reconcile`; through `Judge` the clean path probes exactly once — the critic's fold) + `TestJudge_SubstantiveBridgeError_FailsWithoutTheDeliverableOrTheStream` — kills `a RUNNER_ code on the substantive arm`, `optional arm reached on a substantive error`.
20. `TestJudge_TeardownErrorChainSurvivesTheWrap` — kills `%w → %v` on the forensic arm.
21. `TestReconcileTeardown_ArmOrderIsStaleOKOptionalACSFloorForensic` — eight rows: stale refuses BOTH doors (the floor fixture staged, so the floor WOULD rescue if consulted; `cause=stale_leftover`, `stale_leftover=true`); stale but malformed → `cause=malformed` (the `&& res.OK` gate); a rewritten leftover reconciles; a vanished leftover reads as changed; OK before optional; optional before the floor/FAIL; the floor before the FAIL; the FAIL last — kills `stale gate disarmed`, `floor when stale`, `&& res.OK dropped`, `optional before OK`. Optional-vs-floor order: EQUIVALENT (audit is never optional), kept structurally.
22. `TestForensicFail_EmitsOneEventWithTheGoldenFields` — the fields reproduce `stderr_forensic.golden.txt` line 1 byte-for-byte; the ≤ 11-key budget (no `truncated`); `verr` absent; transient → `teardown=transient cause=unverifiable`; the bare-bridgeErr message shape; no event on the OK arm — kills `tail from the head`, `codes joined by ;`, `cause mapping swapped`, `origin misnamed`, `teardownKind swapped`, `event on the OK arm`.
23. `TestDegradeOptional_WarnResponseAndOneEvent` — kills `suffix dropped`, `error returned on the WARN arm`.
24. `TestRescueViaACSFloor_RescuesOnlyAudit_PassPass_TokenEchoed_LateReadOnce` — rescued; six decline rows; a padded token trimmed; the late read adopts path + bytes, and with a DIRECTORY at the path afterwards `classify` still judges the adopted bytes (the second-read pin) — kills `audit gate dropped`, `TrimSpace dropped`, `late read dropped`, `ArtifactPath not adopted`. `auditRan ignored`: EQUIVALENT (`ReadCycleVerdicts` returns `audit == ""` whenever `auditRan` is false, so `audit != "PASS"` already declines).
25. `TestReconcileTrail_EmitsOneReconciledEventViaVerifyOrACSFloor` — kills `two events per rescue`, `override diag without codes`, `Reconciled unset`.
26. `TestClassify_UnverifiedDeliverable_SignalsOnlyWhenTheFinalVerdictIsFail` + `TestApplyShipGuard_DowngradesOnlyUnverifiedCleanShipVerdicts` — kills `event on pass-through`, `WARN downgraded`, `verdict_before after the guard`, `codes dropped`.
27. `TestWriteCleanStdout_NilFilterSkips_ErrorIsACodeNeverAnError` — incl. the D2 order `[STDOUT_FILTER_FAILED, RECONCILED]` and `len(Fields) == 1` — kills `nil filter dereferenced`, `wrong code`, `workspace dropped`, `a second (path) key` (the review fold: sharpened red on the build's `path` field, then the field went).
28. `TestClassifiedArtifact_VerifiedBytesThenOneReadThenPaneOrEmpty`.
29. `TestForensicSnapshot_AbsentSizeTailAndDirectory` + `TestForensicCodes_JoinsWithComma` — kills `head instead of tail`, `absent on read error`.
30. `TestStatSnapshot_RequiresNonEmptyRegularFile` + `TestUnchangedSince_FailsOpenOnErrorAndNonRegular` — kills `size-only key`, `error reads as unchanged`, `Stat instead of Lstat`.
31. `TestRootsFor_CarriesCycleAndEvolveDirOnlyWithAProjectRoot` (value comparison + a source scan of the leaf for `".evolve"` — the value alone cannot tell `paths.EvolveDirOf` from an inline join) — kills `Cycle dropped`, `EvolveDir hard-coded`. 31a `TestChallengeToken_TrimsAndRejectsEmpty` MOVED VERBATIM to `phasecontract/challenge_token_test.go` with the reader (review fold F1; red first: `undefined: ChallengeToken` there) — kills `TrimSpace dropped`, `empty token accepted`. 31b `TestChallengeToken_ReadThroughPhasecontract` — a source scan: no leaf source spells the token file — kills `token read inline` (proven red with an inline literal in acsFloorRescues).
32. `TestResponseBase_CarriesExactlyTheSixFields`.
33. `TestJudge_ReplaysTheHostGoldens` — the twelve response goldens and all 17 probe rows replayed through `Judge` with equivalent fakes over real temp dirs: byte-identical JSON, identical probe/sleep counts.
34. `TestAPICover_EveryExportIsNamedAndExercised` — the 21 exports named in the leaf's own tests (`make apicover-enforce APICOVER_PKGS=./internal/phases/runner/verdict`: 21 exported, 21 covered; 22 before `ChallengeToken` moved to phasecontract, whose row is 62/62).

Step 3 — the seam, the roots and the wiring:

35. `TestVerdictEngine_OneConstructionSite` (host) — needle `verdict.New(` only in `internal/phases/runner/verdict_engine.go`; proven red with a throwaway `var _ = verdict.New(nil)` in preparation.go.
36. `TestNew_BuildsTheEngineOnceEagerly` (host; was `TestVerdictEngine_LiteralRunnerBuildsTheEngineOnce` until the review fold) — `New(...).judge != nil` and `.judge = ` assigned exactly once in the package's non-test sources (runner.go) — RED on the build's lazy accessor (`map[runner.go:1 verdict_engine.go:1]`), green once the lazy branch went — kills `a second construction`, `New leaves judge nil`. 36b `TestRun_DefaultProbeHonorsPhaseIO` (host, runner_phaseio_test.go; was `TestNew_DefaultVerifyFnHonorsPhaseIO` reading `b.verifyFn`) — through `Run`: at PhaseIO=enforce a build FAIL-without-block report is the verdict source, the guard turns the hook's PASS into FAIL and `deliverable contract violation [failure_context_missing]` is the one diagnostic; at off, PASS with no diagnostic — kills `stage dropped from the default probe` (proven red with `stage := config.StageOff`). 36c `TestRun_DefaultProbeResolvesTheMergedCatalog` (host, same file; was `TestNew_DefaultVerifyFnIsCatalogAware` in runner_reconcile_test.go) — through `Run`: a user phase's on-disk report, not the pane, reaches Classify — kills `BuiltinResolver-only default` (proven red: Classify got the pane). Both pins now observe the probe the ENGINE judges with (the review's concrete failure: a wrapped or re-resolved probe kept the field pins green).
37. `TestDispatchOf_IsTheOneProjection` (host) — kills `ModelSource dropped`.
38. `TestNew_SignalsResolveExplicitThenBridgeThenNull` (host; names `SignalsWired` for the host apicover row) — kills `bridge accessor ignored`, `explicit override lost`, `Center snapshotted at New`.
39. `TestNew_NoCenterIsTheNullObject` (host).
40. `TestRun_StreamIsEmptyOnTheHappyPathAndCarriesOneCodePerFaultPath` (host) — the ordered `{module, kind, code}` per scenario: `[]` (substantive, clean OK, uncontracted, the intent-delta SKIPPED pass-through), `[RUNNER_RECONCILED via=verify|acs_floor]`, `[RUNNER_TEARDOWN_FAIL cause=malformed|stale_leftover]`, `[RUNNER_OPTIONAL_PHASE_DEGRADED]` ×2, `[RUNNER_DELIVERABLE_UNVERIFIED downgraded=true]`, and `[STDOUT_FILTER_FAILED, RECONCILED]` with a failing filter — every event stamped with the request's cycle/run/phase.
41. `TestPreparation_ChallengeTokenAndSnapshotProjectTheLeaf` (host) — the consumer pins for the two moved beliefs (`phasecontract.ChallengeToken` since the fold, plus a source scan: no runner-package source spells the token file — proven red with an inline read in preparation.go).
42. `TestAdapter_SignalsReturnsTheInjectedCenter` (adapters/bridge) — incl. the nil receiver — kills `accessor returns a fresh Center`.
43. `TestSwarmDecorator_ForwardsSignalsWired` (swarmrunner) — kills `narrow the capability`.
44. `TestWireOrchestratorDeps_SignalCenterReachesEveryPhaseRunner` (cmd/evolve) — `d.Bridge.Signals() == d.Signals`; every `d.Runners` entry except ship/retro answers `SignalsWired()` true (scout/build through the Decorator, buildplanner through `.BaseRunner()`, the spec runners) — kills `Runners unset`.
45. `TestWireSimulateOrchestrator_RunnerWarningRenders` (cmd/evolve) — the console line `[runner] runner.warning WARN RUNNER_STDOUT_FILTER_FAILED cycle=7 phase=audit` and `"code":"RUNNER_STDOUT_FILTER_FAILED"` in the cycle workspace's signals.ndjson.
46. `TestSignalsCodes_RepoDocIsInSyncWithTheLinkedRegistry` — green after the regen (92 → 97).

Host tests KEPT with zero edits under their names (the byte-identity harness): runner_reconcile_test.go ×9, _transient ×6, _stale ×4, _ctxcancel ×3, _settlewindow ×1, _cleanexit ×3, _single_read ×4, _verdict_source ×4, _teardown_acsfloor ×4, _optional ×3, _amplify ×3, stdout_filter_test.go ×4; plus runner_verify_roots_cycle_test.go, runner_phaseio_test.go, runner_challengetoken_test.go ×3, worktree_fence_test.go ×3, apicover_named_test.go. Tests moved: none. Tests deleted: none — test 5's stderr half was retargeted (its intent moved verbatim to leaf test 22 + host test 40; the captured line stays in testdata as the record).

## 7. Why it is better than the original

| Dimension | Before | After |
|---|---|---|
| Observability | module `runner` reserved with nothing behind it; the teardown false-FAIL forensic an unpinned INFO stderr line, the ACS-floor rescue and the reconcile printed as two INFO lines for one belief, the ship-guard downgrade and the contracted-malformed FAIL silent, the optional degrade a response diagnostic only; a runner FAIL's reason unreachable from the C1 record (cycles 1634/1636) | five WARN codes under module `runner` on one kind, `origin` = the emitting method, discriminating fields (`cause`, `via`, `downgraded`, `settle_attempts`, `stale_leftover`); `grep '"module":"runner"' signals.ndjson` → one line per decision: `code` the step class, `origin` the function, `fields.cause|via|downgraded` the branch, `settle_attempts` never-flushed vs malformed, `codes` the contract violation, `report/acs` the on-disk truth — the failing step in one line, the response diagnostics (unchanged) the second, the C1 `phase.outcome` the third |
| Isolation | 358 lines of verdict logic in two 170–190-line functions (nesting 6) plus nine helpers, testable only through `Run` with a fake bridge | a leaf over core/deliverable/phasecontract/coherence/paths/signalcenter types with five injected collaborators (probe, hook closure, sleep, filter, Center accessor); 100 % statements directly; the pure rules (`applyShipGuard`, `violationDiagnostics`, `responseBase`, `teardownKind`, `cause`, `staleGate`, `rootsFor`, `forensicSnapshot`, `forensicCodes`, `classifiedArtifact`, `unchangedSince`) are the mutant seam; the leaf structurally cannot import the host, `internal/log`, the bridge or the env |
| Changeability | the challenge-token rule spelled twice, the `.evolve` join inline, the settle bound in code and five test comments, six base fields in four literals, arm order and cancellation policies as comments | each belief has ONE declaration with a projection AND a consumer pin; ONE `responseBase`; the arm order and both cancellation policies are table-tested (21, 18) and probe-golden'd (7, 33) |
| Measurement | a slice of the host's apicover floor; no per-condition counts | `.cover-strict` 100 and apicover 21/21 for the package (22/22 before the token reader moved to phasecontract); per-cycle counts by `RUNNER_*` code — the first quantitative view of how often teardowns reconcile, degrade or FAIL, and why |
| Callers | 15 production construction sites, ten embedders, the Decorator, phaseregistrar, 48 host tests through `Run`, three tests reading consts, three reading `b.verifyFn` (two tests; retargeted through `Run` at the review fold) | zero production spelling changes; four in-package token edits in preparation.go; additive `Signals()`/`SignalsWired()`/`orchDeps.Runners`; runner.go 719 → 500; two files deleted; host net −440 lines (tracked files +245/−683 across 19 files incl. the docs) |

## 8. Risks and byte-identity guarantees

Byte identity (pinned by tests 1-9, 33, 40 and the 48 kept host tests):

- DISK — the leaf persists nothing; the only write on the path is `<workspace>/<phase>-stdout.clean.txt` through the SAME injected filter at the same point (after verdict-bytes selection, before Classify). READ sequence identical: the verify-probe and sleep counts (probes golden), the single artifact read, the ONE late-bytes read in the ACS-floor arm — still BEFORE the stale check, the two coherence reads + the token read, the two forensic Stat+ReadFile on the FAIL arm (unconditional, as before — the fields are built before the Null-Object check), the Lstat of the stale gate.
- PhaseResponse — every field of all four literals, every diagnostic's severity literal and message bytes, their ORDER, `Reconciled`, `NextPhase` untouched by the ship guard, `ModelSource`/`ResolvedModel`/`BootMS` — twelve goldens captured on 8e8f080f (test 1) and replayed through `Judge` (test 33).
- ERRORS — `fmt.Errorf("%s: bridge: %w", phase, bridgeErr)` at both FAIL arms; `errors.Is` on both sentinels (tests 1, 6, 20); core's dispatch-loop suite unchanged (`go test ./internal/core/` green).
- PROMPT BYTES — the challenge-token tail unchanged through `phasecontract.ChallengeToken` (runner_challengetoken_test.go ×3; consumer pin test 41).
- STDERR — the 20 emit sites outside the leaf verbatim through the same three sinks.
- STREAM — module `runner` still emits nothing on the happy path, the uncontracted path and the substantive-error path (test 40; leaf 19); no existing producer's sequence changes.
- ACS — no predicate greps a moved token (zero hits in acs/ for `reconciliation.go`, `classification.go`, `VERDICT-FORENSIC`, `ACS-FLOOR`, `RECONCILED phase` or any moved helper — only comments in deliverable.go:221, runner_cleanexit_test.go:155 name `verifyReconcileDeliverable`); the runner.go `FileNotContains` greps (cycle4/24/45/46) stay vacuously green; cycle46's reflect on `Options.DisableStdoutFilter` stays green.
- HOST EXPORTS — nothing removed; added `Options.Signals` (a field) and `BaseRunner.SignalsWired()` (named by test 38); the host apicover row stays green (20/20), adapters/bridge 16/16, swarmrunner 7/7.

Declared deviations (each with its pin):

- D1 reconciliation.go:170-172 `[VERDICT-FORENSIC] teardown-FAIL …` (INFO via `log.Diag()`) → `RUNNER_TEARDOWN_FAIL` WARN through the root StderrSink — the console line changes format and grep handle; every former value survives as a field (`roots` folds `ws/wt/evolve` into one) — pins: the golden line in testdata (the record), leaf test 22 (the fields reproduce it), host test 5 retargeted to the diagnostic.
- D2 classification.go:184 `[runner] RECONCILED …` + reconciliation.go:151 `[runner] ACS-FLOOR …` (INFO ×2) → ONE `RUNNER_RECONCILED` WARN emitted after `RUNNER_STDOUT_FILTER_FAILED` when both fire — pins: leaf tests 25/27, host test 40.
- D3 classification.go:101 `[runner] WARN stdout filter …` → `RUNNER_STDOUT_FILTER_FAILED` (fields: `workspace` only — the old line printed phase + err; the build's `path` field was a duplicated belief and went at the review fold) — pins: leaf test 27, render twin test 45; stdout_filter_test.go keeps proving the phase is not blocked.
- D4 STREAM + STDERR ADDITION: the optional-degrade arm emits `RUNNER_OPTIONAL_PHASE_DEGRADED` — pins: leaf test 23, host test 40.
- D5 STREAM + STDERR ADDITION: `RUNNER_DELIVERABLE_UNVERIFIED`, fault-only — pins: leaf test 26, host test 40 (the SKIPPED pass-through row emits nothing).
- D6 CONSOLE FORMAT for D1–D3: three INFO lines reached stderr unfiltered; as WARN codes they pass the root's `SeverityWarn` filter and render through the sink — visible, different bytes. A runner built WITHOUT a Center — fake bridges, the nine registry-factory roots — now prints NOTHING where the four lines printed (the trade unit 03b's review fold (e) accepted); pinned by `TestNilSignalBridgeRootsAreExplicit`; operator question 1. Both reviews (architecture MEDIUM-adjacent note, Go MAJOR) ask that this be an EXPLICIT operator decision, so the exact blast radius is spelled out: the nine `bridge.NewDefault(root, nil)` sites are the registry factories scout.go:187, intent.go:160, triage.go:406, audit/audit.go:656, retro/retro.go:410, debugger/debugger.go:248, build/build.go:142, tdd/tdd.go:136 and cmd_campaign.go:404 — consumed by `phasecmd` (`evolve phase run|serve`), `cmd_compose`, `cmd_plan_and_execute`, `looppreflight` and `evolve campaign`, NOT by the production cycle root (`wireOrchestratorDeps` builds every runner over the Center-carrying Adapter — test 44). On those standalone CLIs the teardown forensic, the reconcile trail, the optional degrade and the stdout-filter WARN reach the response diagnostics (ledger/report) but no longer stderr in real time. The two remedies are both root-wiring policy, not this unit's code: (a) a Center + stderr sink at those nine roots — contradicts the S3 pin that non-root `NewDefault` calls pass an explicit nil, so the pin and the S3 rule change together; (b) a process-wide fallback Center — an implicit global the program forbids. The leaf must not grow a stderr fallback (`TestNoStderrNoEnv_InTheLeaf`). Until the operator decides, F7 stands and nothing prints there.
- D7 In-package edits with consumer pins: preparation.go:26/:97/:121-122 (test 41), :47 `skipVerdict`; classification.go:203's join → `paths.EvolveDirOf` (leaf test 31). Additive: `Adapter.Signals()`, `Decorator.SignalsWired()`, `BaseRunner.SignalsWired()`, `Options.Signals`, `orchDeps.Runners` — tests 38/42/43/44. Run's unreachable `err != nil && early == nil` reconcile branch removed with the fold.
- D8 Run's godoc (runner.go:506-518, attached to `type artifactSnapshot` by a misplacement) re-attached to `func Run` with its stale steps corrected; the `Options.SleepFn` comment re-pointed at the engine's ladder — comment-only.
- D9 The host harness's `unverifiedReport` is 246 bytes (the design's shape was 62): the `tail 160 → 200` mutant SURVIVED with a report shorter than the tail — the stderr golden was recaptured on the base with the longer report before any code moved.

Preserved quirks (named, not fixed): Q1 the sleep seam twice — `Options.SleepFn` AND the package var `settleSleep` flipped by runner_cleanexit_test.go:18 — stays in the host. Q2 `forensicSnapshot` discards the ReadFile error — a directory renders `size=N tail=""`; kept so the `report=`/`acs=` field bytes match the golden. Q3 the late-bytes read in the ACS-floor arm runs even when the snapshot is stale (a read with no effect) — order kept. Q4 `classify` never returns a non-nil error — `Judge`'s error is only the reconcile arms'. Q5 the dead store `req.WorktreeVerified = verified` (dispatch.go:155) and the trigger-exit set spelled three ways — the moved comment is kept verbatim; the leaf never restates the set. Q6 the mis-attached `InlinePromptProvider` doc (runner.go:98-107) stays (D8 fixed only Run's). Q7 audit's `Classify` hook performs its own I/O inside the leaf's classify step — the Strategy boundary; the leaf's tests use a fake hook, audit's real classification stays covered by the audit package and the host's Run-level tests. Q8 `settle_attempts` counts RE-PROBES (probes − 1): a ctx cancelled after the sleep but before the re-probe counts one sleep and zero attempts — the probes golden pins both numbers. Q9 the forensic fields (two Stat+ReadFile) are built even when no Center is wired — the base performed the same reads for the stderr line; kept so the read sequence stays identical.

Follow-ups (named, not this unit): F1 DONE at the review fold (`phasecontract.ChallengeToken`); F1b consolidate the four inline token readers outside the runner (deliverable.go:331, bridge/completion.go:171, bridge/driver_common.go:63 are drop-in; bridge/report.go:82 keeps a file-present-but-empty distinction the signature folds, so it needs its own shape) and the three writers onto one `phasecontract` spelling. F2 fold `settleSleep` into `Options.SleepFn` once the host suite's init flip is replaced by per-test injection. F3 split the rest of `New`'s ladder (51 lines after the fold moved the engine's three seams into `wiredVerdictEngine`) into `Options.withDefaults()` (no host limits guard today). F4 DONE at the review fold (the five fields and `signals` are gone; the engine is the seams' only home; the two `b.verifyFn` pins run through `Run`). F5 unit 11b (the dispatch chain: routing/dispatch/cli_health/fence, the 20 remaining stderr sites, the two set-once globals, the `phase.dispatched` producer with `Event.Attempt`, the escalation-report schema twin, the dead store dispatch.go:155, the manifest rows :128-129). F6 retro.go:192-224's hand-rolled runner — fold into BaseRunner in 11b or leave as a retro-unit concern. F7 a root Center for the nine Center-less CLIs (operator question 1). F8 `TestLeafTestsNeverSleep` if the leaf suite ever exceeds 2 s. F9 acs/cycle25's standing red (operator question 3). F10 the stale comment deliverable.go:221 ("runner.go verifyReconcileDeliverable") — a one-word edit outside this unit's surface.

Risks:

- The arm order and the two cancellation policies survive only if the split is extracted BY HAND, never regenerated — tests 7, 18, 21, 33 and the four host suites are the defence.
- Dual-return contract: a leaf that re-wraps or returns a zero response on a FAIL arm silently changes core's retry/quota-abort classification — tests 1, 6, 20; the substantive arm must never emit (leaf test 19).
- The grep-handle change (D1/D2) invalidates any external tooling keyed on `[VERDICT-FORENSIC]` / `[runner] RECONCILED` — the module-internal grep is clean and no docs/skills/knowledge-base file names them (runtime-reference.md gained a triage line for the new codes instead), but retro dossiers and the console's shell history are outside this tree (operator question 2).
- New WARN console volume (D4/D5): fault-only keeps the intent-delta SKIPPED cycle silent, but every coherent deliverable-production FAIL and every optional-phase teardown now prints a WARN line — watch the first batch.
- The bridge-carried Center is an implicit channel: a future bridge implementation that carries a Center but does not implement `Signals()` silently yields Null-Object runners — test 44 and `TestNilSignalBridgeRootsAreExplicit` are the guards; a NEW production root is caught only by adding it to test 44.
- Two ProtectedSurface-guarded areas are edited — runner.go (row :128, `requiresExplanationSandbox` kept) and adapters/bridge/ (row :131, +11 lines) — both rationales stay true; the new leaf/seam rows land in the SAME change. dispatch.go (row :129) is NOT touched. The tree ships only via `evolve ship --class manual` from an operator session outside any cycle, like units 01-03b.
- acs/cycle25 `TestC25_NEG1_ProfileInteractivePolicyHonored` is RED on 8e8f080f independently of this unit (it greps runner.go for `prof.InteractivePolicy`, a token that exists nowhere in the package since #549); the whole-module floor will show it; this unit never re-adds the token.
- The host's line coverage is unmeasured here (the whole-module floor is the orchestrator's); the untested host behaviours outside the leaf stay unpinned until 11b.

## 9. Migration

Landing plan — ONE PR, ONE ship commit (`evolve ship --class manual` from an operator session outside any cycle after the reviewer fleet — simplifier → architecture ∥ Go, CRITICAL/HIGH block — and the orchestrator's whole-module floor), built in three verified steps, each green before the next, every file left UNCOMMITTED in `dev/decompose-phaserunner`:

1. Step 1 (host pins + goldens; zero production edits): tests 1-9 (green on 8e8f080f, each proven red by its named mutant — §10); the throwaway writer (10) captured `go/internal/phases/runner/verdict/testdata/{response_<scenario>.golden.json ×12, stderr_forensic.golden.txt, probes.golden.json, stream_sequences.golden.txt}` with temp paths templated, then was deleted. Gate: `go test -count=1 ./internal/phases/runner/` (171 → 181 top-level tests).
2. Step 2 (the leaf, the closed set, the gates): event.go `KindRunnerWarning` + `knownKinds`, event_test.go `all` (test 12 red → green); tests 11, 13-34 red first (`undefined: Snapshot`); `verdict/{verdict.go, settle.go, reconcile.go, classify.go, artifact.go}` + `limits_test.go` (copied) + `importgraph_test.go` (+ the no-stderr scan); `.cover-strict` + `./internal/phases/runner/verdict 100`; `.apicover-enforce` block + `./internal/phases/runner/verdict`. Gates: `make -C go cover-strict` (100.0), `make -C go apicover-enforce APICOVER_PKGS=./internal/phases/runner/verdict` (22 exports named), `go test -count=1 -race ./internal/phases/runner/verdict/`.
3. Step 3 (the seam, the wiring, the docs — as built; the review folds in §10 then dropped `BaseRunner.signals` with the five seam fields and the lazy accessor): tests 35-46 red first; runner.go — the deletions of §2, `Options.Signals`, `BaseRunner.signals/judge`, `New` +3 lines, `Run` tail → the one `Judge` call, imports pruned, Run's godoc re-attached (D8); reconciliation.go and classification.go DELETED; preparation.go ×4; NEW `verdict_engine.go`; adapters/bridge `Signals()`; swarmrunner `SignalsWired()`; cmd_cycle.go `orchDeps.Runners`; ProtectedSurface +2 rows after integrity_surface.go:129; test 5 retargeted (D1); `make -C go build && ./go/bin/evolve signals codes generate && … check` (92 → 97); docs — this file, the ADR-0103 units-table row + note, signal-center-design.md §5.1 parenthetical / §5.2 row / §12 note, runtime-reference.md's triage addition. Then the mutants (§10), gofmt on both toolchains, `go vet ./...`, `go vet -tags integration ./internal/core/ ./cmd/evolve/`, the touched packages + `./internal/core/` + `./cmd/evolve/`, `-race` on the leaf and host, cover-strict, apicover. Not run here: the whole-module suite, ACS, integration — the orchestrator's floor; acs/cycle25 will show RED there regardless (pre-existing).

Call-site changes: ZERO production spelling changes — `runner.New(runner.Options{…})` at cmd_cycle.go:498-518/:569/:1003, registrar.go:194, the ten embedding packages and every registry `init()` factory; `swarmrunner.New(...)`; `.BaseRunner()`; the `core.PhaseRunner`/`core.PersonaProber` ports; `bridge.NewDefault(root, signals|nil)` — all unchanged; every host test compiles unchanged. Declared in-package edits: preparation.go ×4 (D7). Declared additive edits: `Adapter.Signals()`, `Decorator.SignalsWired()`, `BaseRunner.SignalsWired()`, `Options.Signals`, `orchDeps.Runners`, the closed-set lines, the gate rows, the manifest rows, the docs. runner_dispatch_dedup_test.go UNTOUCHED.

ACS by-name couplings kept green with no edit: acs/cycle276 (the host package subprocess), cycle534 (`go vet` of the runner dir; `BaseCycleContext`/`StaticPrefix` bytes) / cycle646, cycle463/476/440/672/986 (`runnerPkg` by import path), cycle943 (`FormatSkillOverlayLog`), cycle1460 (digest-shadow), cycle426/1647 (real `Run` through dispatch — untouched), cycle4/24/45/46 (runner.go `FileNotContains` + the Options reflect); acs/cycle25 stays RED exactly as on 8e8f080f.

Decisions (the open points, each taken once): winner = design A (byte-identity) as the base — single `Judge`, package `verdict`, no FS port, Center from the bridge — with B's code vocabulary (fold ACS_FLOOR into RECONCILED `via=`, `cause`/`teardown`/`exit`/`downgraded` fields, fault-only DELIVERABLE_UNVERIFIED, the 11b draft) and its per-runner root proof, and C's per-call bound `Classify` closure, invariants table, probe goldens and quirk list. Five codes (the optional degrade is its own belief) not four. Origin = the emitting method, no `fields.step`. No `Event.Attempt` stamp (dispatch.go is protected; 11b). `ChallengeToken` single-sourced now (F1 re-home later). Accessor pair kept. `signalSource` unexported. `dispatchOf` keyed, the positional guard in the leaf test. `orchDeps.Runners` accepted as an additive root field — operator question 4. Host pins are green characterization tests, not "RED today". No core-package RunCycle ledger test. Goldens live in `verdict/testdata`, read by host and leaf. `StatSnapshot` over `Stat`; `Snapshot` fields unexported; `SettleRetries`/`SettleInterval` exported for the projections; `settleSleep` stays.

**Critic folds (completeness critic on the synthesis, before implementing — all applied).** (1) The `MaxFields = 12` cap: `ws`/`wt`/`evolve` folded into ONE `roots` field and empty values omitted by `warn`, so `RUNNER_TEARDOWN_FAIL` carries ≤ 11 keys; leaf test 22 asserts no `truncated` and ≤ `MaxFields-1` keys. (2) Test 5's step-3 fate declared: retargeted to the diagnostic; the stderr half's intent lives in leaf test 22 + host test 40; the captured line is the record. (3) The directory-at-acs-verdict arm is never byte-goldened (a regex on `size=`; leaf test 29 asserts the shape). (4) `settled` gains `stale` and `refused`; `cause` derives from the flags, never from the :83 message text. (5) `RUNNER_DELIVERABLE_UNVERIFIED`'s origin is `Engine.unverifiedSignal` — one spelling. (6) Test 19's no-probe half scoped to `reconcile`; through `Judge` the clean path probes once. (7) `EvolveDir-hard-coded` is a source scan (`".evolve"` absent from the leaf's non-test sources), not a value comparison. (8) `nonTestSourcesMentioning` copied with `../../..` and the `internal/phases/runner/verdict/` skip prefix. (9) The runtime-reference edit is an ADDITION (a triage line for the five codes) — no old token existed there. (10) D2 states the order `[STDOUT_FILTER_FAILED, RECONCILED]`. (11) preparation.go:47's local renamed `skipVerdict`; `Adapter.Signals()` guards a nil receiver. (12) The import allowlist is DIRECT imports only, said so. (13) The console example renders values unquoted (`report=size=246 tail="…"`); the swarmrunner anchor is :304-309. Corrections folded from tree verification: `TestRun_CancelledCtx_TeardownReconcileStillSettles` already pinned the cancel-immune half (test 7 adds the count golden); acs/cycle534 vets the runner DIRECTORY, not `...`; 9 Center-less `bridge.NewDefault(…, nil)` roots, not 5; 10 packages embed `*runner.BaseRunner` (retro and ship do not); zero host tests name any moved symbol; the 4 stderr lines and the moved helpers have zero test/ACS binders.

Facade removal plan: `reconcileSettleRetries`/`Interval` go when the three settle tests read `verdict.SettleRetries` directly (the five BaseRunner fields already went — F4 at the review fold); `signalSource` goes if a later unit threads `Options.Signals` through the phase Configs (then the bridge derivation is dead and test 38's override rule is the only path); `classifyWith` goes when the hook signature drops the request.

## 10. Verification (landed)

Red first on every surface: the nine host pins were green on 8e8f080f and each proven red against its named mutant before any code moved; the leaf's tests failed on the missing package (`undefined: Snapshot`), the closed-set test on the missing kind, the seam tests on the missing accessor/projection, the root tests on the missing `Signals()`/`SignalsWired()`/`orchDeps.Runners`; after the fold exactly one host test was red — test 5's stderr half, retargeted as declared.

Counts (2026-09-14, `dev/decompose-phaserunner`, base 8e8f080f): `./internal/phases/runner/verdict` 100.0 % statements (`make cover-strict`, the row `./internal/phases/runner/verdict 100`) and 21/21 exports named (`make apicover-enforce APICOVER_PKGS=./internal/phases/runner/verdict`: 21 exported, 21 covered, 0 false-green — 22/22 before the review fold moved `ChallengeToken` to `./internal/phasecontract`, 62/62 there); the host row `./internal/phases/runner` 20/20, `./internal/adapters/bridge` 16/16, `./internal/phases/swarmrunner` 7/7. The leaf: 33 top-level tests (5 files, 812 production lines, every function < 50 lines, nesting ≤ 3 — `limits_test.go`), 0.4 s. The host: 171 → 188 top-level tests (+17: 10 pins/goldens — one retargeted at the fold — and 7 seam), all 48 tests of the reconcile/verdict cluster green with zero edits. Every touched package green: `go test -count=1 ./internal/phases/runner/verdict/ ./internal/phases/runner/ ./internal/adapters/bridge/ ./internal/phases/swarmrunner/ ./internal/signalcenter/ ./internal/guards/ ./internal/core/ ./cmd/evolve/` — 8/8 ok; `go test -count=1 -race ./internal/phases/runner/verdict/ ./internal/phases/runner/` ok; `gofmt -l ./internal ./cmd` empty on Go 1.27.1 and `gofmt -d -s .` empty on the Go 1.23.12 toolchain; `go vet ./...` and `go vet -tags integration ./internal/core/ ./cmd/evolve/` clean; `signal-codes.md` regenerated 92 → 97 codes, `evolve signals codes check` in sync. runner.go 719 → 500 lines; reconciliation.go (210) and classification.go (206) deleted; tracked files net +245/−683 across 19 files (incl. the docs), plus the new leaf, seam, tests and goldens.

Codes added (5): `RUNNER_TEARDOWN_FAIL`, `RUNNER_OPTIONAL_PHASE_DEGRADED`, `RUNNER_RECONCILED`, `RUNNER_DELIVERABLE_UNVERIFIED`, `RUNNER_STDOUT_FILTER_FAILED`. Stderr lines replaced (4): reconciliation.go:170-172 → `RUNNER_TEARDOWN_FAIL`; reconciliation.go:151 + classification.go:184 → `RUNNER_RECONCILED`; classification.go:101 → `RUNNER_STDOUT_FILTER_FAILED`. Declared deviations D1–D9 (§8). Tests moved: one at the review fold — leaf test 31a (`TestChallengeToken_TrimsAndRejectsEmpty`) verbatim to `phasecontract/challenge_token_test.go` with its function.

Mutants, hand-applied one at a time (cp → perl → vet → `go test -run <Name>` → restore), 68 build-confirmed kills by name — on the base against the host pins (step 1, 20): `%w → %v` on the teardown arm (1); ArtifactsDir from Worktree (1); BootMS dropped from the substantive literal (1); nextPhase cleared on downgrade (4); WARN removed from the guard (4); violation severity warning (3); violations dropped (3); fence after violations (2); reconcile diag before Classify (2); late read dropped (8); filter after Classify (9); WithoutCancel removed (7); pre-sleep ctx dropped (7); bound off-by-one (7); tail 160 → 200 (5 — survived at 62 bytes, killed after the report was lengthened, D9); tail from the head (5); Violations[0] → [1] (5); roots.EvolveDir empty (5); zero response on the teardown FAIL (6); verify on the substantive arm (6) — on the leaf (step 2, 41): stale gate disarmed (21); floor when stale (21); `&& res.OK` dropped from the gate (21); optional before OK (21); WithoutCancel removed (18); WithoutCancel on the clean path (18); pre-sleep ctx dropped (17); post-sleep ctx dropped (17); bound off-by-one (16); verify error retried (16); attempts miscounted (16); interval 250ms (16); `%w → %v` on the forensic arm (20); cause mapping swapped (22); origin misnamed (22); teardownKind swapped (22); codes joined by `;` (22); tail from the head (29); absent on a read error (29); an event on the OK arm (22); the unverifiable suffix dropped (23); an error returned on the WARN arm (23); the audit gate dropped (24); TrimSpace dropped (31/24); late read dropped (24); ArtifactPath not adopted (24); two events per rescue (25); the override diag without codes (25); Reconciled unset (25); an event on the pass-through (26); WARN downgraded (26); verdict_before after the guard (26); codes dropped from the unverified event (26); a nil filter called (27); the wrong code on a filter failure (27); the filter path dropped (27); a size-only key (30); an error reading as unchanged (30); Stat instead of Lstat (30); EvolveDir hard-coded (31); Cycle dropped from the roots (31); the Center snapshotted (15); a nil-verify guard added (15); a RUNNER_ code on the substantive arm (19); the optional arm on a substantive error (19) — on the seam, the roots and the closed set (step 3, 7 + 2): the bridge accessor ignored (38); the explicit override lost (38); the Center snapshotted at New (38); a second `verdict.New(` caller (35); dispatchOf drops ModelSource (37); the disable-filter flag ignored (stdout_filter_test); a second lazy construction (36); the Decorator not forwarding (43); `Adapter.Signals` returning a fresh Center (42); `orchDeps.Runners` unset (44); the kind marked terminal (12); the kind left out of the closed set (12). Equivalents recorded, not padded: the optional-vs-floor order (unobservable — audit is never optional); `auditRan ignored` (`ReadCycleVerdicts` returns `audit == ""` when `auditRan` is false, so `audit != "PASS"` already declines — the guard is defensive); ArtifactPath-not-adopted THROUGH THE HOST (a redundant re-read yields identical bytes; killed in the leaf by test 24's directory swap); override-without-codes THROUGH THE HOST (a not-OK probe always carries a violation in the host fakes; killed in the leaf by test 25); the removed INFO lines (they ARE the declared replacements); a `Fields` key rename (caught by test 22).

Review folds (2026-09-14, the reviewer fleet's architecture Warning/FIX_THEN_MERGE and Go Warning/Approve verdicts, folded by the unit's folder before landing; red-first where a behaviour is pinned, every gate re-run — `gofmt` on both toolchains empty, `go vet ./...` and `-tags integration` rc=0, the leaf 100.0 % / 21 of 21, phasecontract 62/62, host rows 20/20 · 16/16 · 7/7, `signals codes check` 97 in sync, `go test -count=1` on verdict · runner · phasecontract · adapters/bridge · swarmrunner · signalcenter · guards · core · cmd/evolve 9/9 ok, `-race` on verdict · runner · phasecontract 3/3 ok):

- HIGH (architecture) — the leaf re-derived logfilter's companion filename for `RUNNER_STDOUT_FILTER_FAILED`'s `path` field (classify.go:96 ≡ logfilter.go:28, a belief the engine never writes). FOLDED: leaf test 27 sharpened to `len(Fields) == 1` (RED on the build), the key dropped, the code's registration and `WithStdoutFilter`'s doc reworded, signal-codes.md regenerated (97, in sync), §5 row + D3 updated. Event.Phase + `fields.workspace` identify the companion.
- MEDIUM (architecture) — `verdict.ChallengeToken` claimed to be "the ONE reader" while four production readers spell the read inline, and the pre-dispatch stage imported the post-dispatch leaf for a prompt concern. FOLDED as F1 now: `phasecontract.ChallengeToken` (`challenge_token.go` beside `Contract.RequireChallengeToken`; test 31a moved verbatim, RED first on `undefined: ChallengeToken`; the phasecontract apicover row 62/62), the leaf's ACS floor and preparation.go:122 read through it, test 41 retargeted; two source-scan pins (leaf 31b, host 41) forbid the token filename in either package (each proven red with an inline literal); the function's doc NAMES the four sibling readers and the writers instead of claiming them (F1b). Leaf exports 22 → 21.
- MEDIUM (architecture) + MINOR (Go) — the five resolved seams lived twice (BaseRunner fields and the engine's copies) with two construction moments, and the lazy `verdictEngine()` branch was dead in production (New sets `judge` eagerly), its only client its own literal-runner test, an unsynchronised write. FOLDED as F4 + the lazy half dropped: `wiredVerdictEngine(opts)` is the ONE construction and the ONE resolution (VerifyFn | catalog-aware over PhaseIO, SleepFn | settleSleep, StdoutFilter | logfilter.Process | nil when disabled, Optional, the Center accessor); `BaseRunner` lost `verifyFn`, `sleepFn`, `optional`, `stdoutFilter`, `disableStdoutFilter` and `signals`; `Run` and `SignalsWired` read `b.judge`; test 36 became `TestNew_BuildsTheEngineOnceEagerly` (RED on the build: `.judge = ` assigned in two files); the two `b.verifyFn` pins became `TestRun_DefaultProbeHonorsPhaseIO` and `TestRun_DefaultProbeResolvesTheMergedCatalog`, driven through `Run` so they observe the probe the engine judges with (each proven red on the build with `stage := config.StageOff` / a BuiltinResolver-only default, then again against the restructured construction; the other seams re-killed there: optional dropped ×2 tests, disable ignored ×2, signals dropped ×1; `settleSleep → time.Sleep` is EQUIVALENT in production and recorded as such). A deliberate departure from the protocol's `x()`/`wiredX()` accessor-pair idiom, on both reviews' instruction: no production literal exists to serve the lazy half.
- LOW (architecture) — reconcile.go:164 spells `"acs-verdict.json"` while `acssuite.VerdictFilename` exists. DECLINED, noted for the 11b/acs cleanup as the review itself proposed: the leaf's import allowlist (importgraph_test.go — stdlib plus six named packages) would grow `acssuite`, which imports `policy`, `verifylock`, `changedpkgs`, `gopkgpattern`, `ipcenv`; the spelling pre-dates the move (byte-identical), and `coherence.ReadCycleVerdicts` (coherence.go:133), which the leaf already reads through, spells it inline too.
- MAJOR (Go) — the nine Center-less registry-factory roots print nothing where the four forensic lines printed. DECLINED as code (both reviews route it to the operator): D6 above now spells out the nine sites, their consumers (`evolve phase run|serve`, compose, plan-and-execute, looppreflight, campaign — never the production cycle root, test 44) and the two remedies, both root-wiring policy — a Center at those roots contradicts the S3 pin `TestNilSignalBridgeRootsAreExplicit` (cmd_cycle_signal_center_test.go:183-222, which REQUIRES the explicit nil), a process-wide fallback is an implicit global the program forbids, and a stderr fallback in the leaf is what `TestNoStderrNoEnv_InTheLeaf` exists to refuse. F7 stands as operator question 1.

Counts after the folds: tracked diff 21 files, +338/−796 (runner.go 500 → 467, verdict_engine.go 93 → 113, the leaf 812 → 804 production lines, still every function < 50 and nesting ≤ 3); host top-level tests 173 (base, recounted — the build's "171" was a miscount) → 190; leaf 33; phasecontract +1. The whole-module floor stays the orchestrator's.
