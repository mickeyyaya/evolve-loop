# Unit 08 — Config resolution (`internal/config`, decomposed in place)

- **Program:** [ADR-0103](../adr/0103-component-breakdown-program.md) · units table row 08 (`config` removed from the `…` row) · design §12 row 7 (signal-center-design.md: `config/config.go (962)`, module `config`, "typed policy structs; retire env flags as a by-product")
- **Subject:** `go/internal/config/config.go` (962 lines on the base) — NOT an extraction: the package was already the leaf (stdlib-only; `go list -deps ./internal/config` names zero internal packages) and 58 non-test files import it. The unit decomposes the file in place around ONE new `Loader` with explicit DI, gives module `config` its first kind and six codes, and closes the two silent kill-paths the readers found: the swallowed registry read/parse errors (config.go:655-665) and the composition root's warning-free copies of the stage ladders (cmd_cycle.go:955-977).
- **Module tag:** `config` (already in the closed set: event.go:84, `knownModules` :100, event_test.go:41 — the 26-module literal at event_test.go:49-50 does NOT move) · **Kind:** `config.warning` (NEW, non-terminal) · **Status:** landed 2026-09-14 (one ship commit — see §9); design synthesized from the judged three-lens panel (three designs, two judges), winner "Seam and testability first" with the grafts both judges named; the completeness critic's seven folds applied (§9 "Critic folds").

Base: `8e8f080f` (the worktree `dev/decompose-config`, `HEAD == origin/main`). Every `file:line` below is against that tree unless it names a new file.

## 1. Context

`config.Load` (config.go:518-534) was the ONE exported entry of config resolution: `defaults()` → `[env["EVOLVE_USE_PHASE_REGISTRY"] != "0"]` `readRegistry` + `applyRegistry` → `applyEnv` → `validateSpine` → `validateInertEnables`, precedence env > registry > default (:515-517), env an injected map (the package never calls `os.Getenv`). Its only diagnostic channel was the returned `[]Warning{Code, Message}` (:483-486). Thirteen sites appended one: `"unknown-value"` ×10 (:699, :710, :753, :766, :774, :841, :860, :872, :890, :904), `"weak-spine"` (:798-800), `"spine-order"` (:818-820), `"inert-phase-enable"` (:583-585). The :484 comment advertised two codes nothing produces (`unknown-key`, `deprecated-flag` — config_legacyflags_test.go:41-43 pins the latter's ABSENCE) and omitted `spine-order`. The package wrote nothing to stderr, disk or the ledger; the :482 "and ledger" claim had no producer.

Five callers rendered or dropped the slice five ways: cmd_cycle.go:407-410 (the ONE production root, reached by `evolve cycle` :226 and `evolve loop` via `wireOrchestratorDepsFn`) printed `[config] WARN %s: %s` to `os.Stderr` — past the Signal Center `wireOrchestratorDeps` built 31 lines earlier at :376 (`newRootSignalCenter` :347-357), and past the root's `console` writer, so `captureConsole` (cmd_cycle_signal_center_test.go:126-130) could not see it and no test pinned it (grep over `*_test.go`: 0); cmd_solution.go:47-50 printed a second spelling `evolve solution: registry warning [%s]: %s` (no Center in that root); router/policy.go:88 (`PolicyForProject`, re-run PER PHASE by buildplanner/triage/swarmplan/tdd) and phase_verify.go:194 dropped them (`cfg, _ :=`); kerneltest/fixture.go:45 dropped them (a test helper). A grep for `config\.Load(` over non-test sources found those four Center-less sites plus the root; the fifth hit, cmd_campaign.go:394, is `phaseconfig.Load(`.

The component's blindest failure: `readRegistry` (:655-665) returned `(registryDoc{}, false)` for ANY `os.ReadFile` error (:656-659) and ANY `json.Unmarshal` error (:661-663), and `Load` :524 treated `ok=false` as "no registry". A trailing comma in `docs/architecture/phase-registry.json` (17 phases, `mandatory_phases` = scout/triage/build/audit/ship, `model_routing: auto`, `dynamic_routing: advisory`) silently ran every cycle on the compiled baseline that deliberately omits triage (:615-619, :634), with empty `Order` (the router falls back to `canonicalOrder`, :411-412), no registry `enabled`/`routing` blocks, no `goal_recipes`, no `deliverable_kinds` — nothing on stderr, nothing in signals.ndjson, and no test fed a malformed registry (config_test.go:29 covered absence only). Absence is the ORDINARY silent case (config_legacyflags_test.go:36 and eleven other tests load `/nonexistent/phase-registry.json`), so the leaf must split `fs.ErrNotExist` from every other error — the split domain.go:32-41 already performed for `.evolve/domain.json`. A sibling parser of the same file, phaseorder.go:28-47, FAILS LOUDLY (`parse %s: %w`), and the production root's own third parser `phasespec.Load` (cmd_cycle.go:527-533) prints `[phases] WARN builtin registry load failed` — three contracts for one file.

The second silent kill-path lived in the root: `parseGateStage` (cmd_cycle.go:955-964, shadow|enforce else Off) and `parseRouterStage` (:966-977, shadow|advisory|enforce else Off) duplicated config's `parseEvidenceStage` (:851-863) and `parseStage` (:830-844) MINUS the `unknown-value` warning that config.go:827-829 says a kill-path dial must never lack. They were applied unconditionally at :442-449 and :451-455 to thirteen `RoutingConfig` fields from `pol.GatesConfig()` (routing_gates.go:108-125), `pol.RecoveryConfig()` (budgets_recovery.go:151), `pol.RouterConfig()` (routing_gates.go:42) and `pol.ParallelEvaluateConfig()` (merge_catalog.go:27-38 — the one accessor that sanitises its stage first), which pass raw strings; so `.evolve/policy.json` `gates.eval_gate: "enfroce"` turned the eval gate OFF with no trace. `TestParseGateStage` (cmd_cycle_test.go:22-34) pins `"unknown" → StageOff` by name; `parseGateStage` is also used at cmd_cycle.go:700 (`ReportSizeGate`) and cmd_loop_preflight.go:32 (`NestedFallbackStage`). The same fold made the "immutable, loaded once" story (config.go:3-4, :382-383) false: the root MUTATED the value (:427, :436, :442-455), and the `[policy] WARN` branch (:423-425) resets `pol = policy.Policy{}` so every gate dial resolves from policy's compiled defaults with no code. `router.PolicyForProject` (router/policy.go:86-100) composes a THIRD, partial overlay (Mandatory merge + AuditFail only).

Coverage was 95.7 % with eight uncovered blocks (coverprofile at 8e8f080f): config.go:124 (the `DefaultTddRule` panic), :578 (`continue`), :583-585 (the ONLY `inert-phase-enable` emission — inert_enable_test.go:22-50 are four ABSENCE pins), :662-663 (the malformed-registry branch), :715 (`continue`), :766-767 (the `EVOLVE_CONDITIONAL_MANDATORY` parse-error warning), :872-873 (`parseMode`'s unknown arm), domain.go:36-37 (the non-ENOENT read error). Seven of eight are the failure branches a triage reads. Three functions failed the < 50-line bar: `defaults` :589-653 (65, ~45 of them comment prose inside the literal), `applyRegistry` :667-725 (59, four concerns), `applyEnv` :727-777 (51, nine arms in two shapes); the file failed the < 800 bar. Module `config` had zero producers, zero codes (grep `CONFIG_[A-Z]` over `*.go`: none), no kind (event.go:128-152), and no `### config` section in signal-codes.md (92 codes). Import graph (`go list -deps`, 2026-09-14): config → ∅; signalcenter → `internal/log` only; paths → `internal/ipcenv` only; policy → config (overlays.go:26) — so config can NEVER import policy; core → config in 17 non-test files.

## 2. Boundary

| Moves (base lines, config.go unless noted) | Destination | Stays / facade | Why |
|---|---|---|---|
| `Stage`/`Mode`/`ModelRouting`/`Enable` + `String` (:21-108), `DeliverableKindCode/Document` (:132-135), `SignalDeliverableKind/GoalType` (:194-197), `SandboxMode*` (:476-480) | `vocabulary.go` verbatim | — | pure vocabulary; the 58 importers consume only this |
| `DefaultTddRuleExpr`, `DefaultTddRule` (:117-127), `CondRule`, `Clauses` (:165-179), `parseCondRule`, `parseCondClause` (:909-951) | `condrule.go`; the :124 panic moved into NEW `mustCondRule(expr string) CondRule` so the line is reachable; `DefaultTddRule()` = `mustCondRule(DefaultTddRuleExpr)` | `DefaultTddRule()` spelling kept (routingeval.go:70, routingtest/engine.go:64); `parseCondRule` name + signature kept (condrule_and_test.go:15/:25/:29, config_coverage_test.go:39/:44, config_test.go:194) | the grammar is pure; the package's only panic must be coverable |
| `DeliverableKindSpec` + `DocumentSpec` (:143-158), `Condition` (:184-188), `RoutingBlock` (:200-210), `RoutingConfig` (:384-472) | `routing_config.go` verbatim; the :388-389 comment corrected: `model_routing` comes from phase-registry.json `config.model_routing` (registryDoc :493, applyRegistry :675-677), not policy.json; the :382-383 "immutable, loaded once" sentence rewritten to name `Loader.Load` and `Loader.ApplyPolicyStages` as the two steps | — | data only; two wrong provenance comments send a triage to the wrong file |
| `RolloutStages` (:217-380, 164 lines, ~150 of doc prose) | `rollout.go` verbatim | — | the bulk that pushed config.go past 800 |
| `defaults()` (:589-653, 65 lines) | STAYS in `config.go` as a 15-line composer over NEW `defaultRollout() RolloutStages` (the :613 literal, one field per line, no trailing comments — gofmt 1.23/1.27 parity), `defaultMandatory() []string` (:634) and `defaultPhaseEnable() map[string]Enable` (:645-650), all in config.go; the :591-596 routing-history prose is `defaults()`'s doc comment, the :599-612 SpineFloor rationale is `defaultRollout()`'s, and the :615-633 Mandatory/`phasecontract` divergence block is `defaultMandatory()`'s doc comment verbatim | `defaults()` spelling kept (condrule_and_test.go:83, config_test.go:290, mergegate_stage_test.go:11, parallel_evaluate_stage_test.go:16/:40) | limits_test.go counts comments INSIDE a function but not its doc comment, so the prose moved onto doc comments beside the values it explains; acs/cycle1141/predicates_test.go:240-252 reads config.go BY PATH and `hasDivergenceComment` (:411-425) needs a `//` line with `phasecontract` + `mandatory`/`diverg`/`deliberately` — honoured on its intent (no pointer comment written only for a predicate; the doc comment says in place why the family stays in config.go); cycle1141 is a green-floor predicate, not a manifest row |
| `registryDoc` (:489-513), `readRegistry` (:655-665), `applyRegistry` (:667-725, 59 lines) | `registry.go`: the anonymous struct types become `registryConfig`, `registryWorkflow`, `registryPhase` (identical JSON shape — the four steps need named parameter types); `(*Loader).readRegistry(path string, ws *[]Warning) (registryDoc, bool)` through the injected reader, classifying `fs.ErrNotExist` (silent) / any other read error (`CONFIG_REGISTRY_UNREADABLE`) / a parse error (`CONFIG_REGISTRY_MALFORMED`); `applyRegistry` is a 4-line coordinator over NEW `applyRegistryDials` (:668-692 + :703-705), `applyDeliverableKinds` (:693-702, keys SORTED), `applyConditional` (:706-712, keys SORTED), `applyPhases` (:713-724, stamps `key=phases[<name>].enabled` on the range its `parseEnable` call appended) | `applyRegistry(cfg, doc, ws)` spelling kept | four independent concerns; each is a mutant seam and a `fields.step` triage landing; map iteration made deterministic (D5) |
| `applyEnv` (:727-777, 51 lines) | `env.go`: `var envDials = []envDial{{key, apply}}` in the :728-776 order (Strategy per key; the arms are named functions `envDynamicRouting` … `envMaxOptionalInsertions` so every table element is one line — the gofmt-parity-safe shape); the loop captures `from := len(*ws)` before each `apply` and stamps `key=<d.key>` on `(*ws)[from:]` after it (so an env-sourced `unknown-value` carries `key=EVOLVE_DYNAMIC_ROUTING` while the :729 varName `"dynamic_routing"` stays in the Message — attached at the arm, never by widening a parser); NEW `parseSandboxMode(v, current string, ws *[]Warning) string` lifted from :749-755 with the :754 message fed by `cfg.SandboxMode`; the :762 `strings.Cut` miss gains `CONFIG_UNKNOWN_VALUE` (D3); the :734-736 model_routing note is the file's header | `applyEnv(cfg, env, ws)` spelling kept | nine `if v := env[K]` arms in two shapes; deterministic warning order equals today's |
| `staticSpinePhases` (:543-553), `validateInertEnables` (:563-587), `containsPhase` (:780-787), `validateSpine` (:789-823) | `validate.go` verbatim (the Warning literals became `warn(...)` calls with Fields: `missing`; `audit_pos`/`ship_pos`; `phase`/`stage`) | `validateSpine(cfg, &ws)` spelling kept (spine_order_validation_test.go:14/:23); `staticSpinePhases` kept (export_test.go:7, spine_contract_test.go:20-40) | pure validators |
| `parseStage`/`parseEvidenceStage`/`parseMode`/`parseModelRouting`/`parseEnable` (:830-907), `splitCSV` (:953-962) | `parse.go`: `parseStage`/`parseEvidenceStage` became `s, ok := X(v); if !ok { warn(...) }` over NEW exported pure ladders `RouterStage(v) (Stage, bool)` (the 4-value ladder :831-839) and `GateStage(v) (Stage, bool)` (the 3-value ladder :852-858) | `parseStage(in, name, &ws)` 3-arg signature kept (mergegate_stage_test.go:27, parallel_evaluate_stage_test.go:32); `parseMode(v, ws)` and `parseEnable(v, ws)` keep hard-coding `routing_mode=` / `enabled=` in the Message (:872, :904 — preserved quirk Q4; the origin rides in `fields.key`/`source`) | the two ladders the root re-implemented silently have ONE spelling |
| `Warning` (:483-486), `Load` (:518-534) | `config.go` (the engine file): `Warning{Code, Message, Fields}` (ONE new field), `Loader`, `Option`, `New`, `WithReadFile`, `WithSignals`, `SignalsWired`, `(*Loader).Load` (the :522-533 pipeline + range stamping + ONE emit) with its registry step `(*Loader).applyRegistryFile`, the package-level `Load` facade, `RegistryPath` | `config.Load(path, env)` package func kept = `New().Load(path, env)` — the silent facade for router/policy.go:88 (per phase), phase_verify.go:194, cmd_solution.go:47, kerneltest/fixture.go:45 and the cross-package tests | Null Object for Center-less sites; an emitting `Load` would fire every registry warning once per buildplanner/triage/swarmplan/tdd dispatch |
| NEW codes, `init()`, the code table, the appender, the stamper, the producer | `signals.go` (99 lines): six `Code*` consts, `init()` with `signalcenter.RegisterCode(signalcenter.ModuleConfig, …)`, the six unexported kebab consts, `signalCodes map[string]signalcenter.Code`, `warn`, `stamp`, `(*Loader).emit`, `center()` | — | one file to read for "what can this module say" |
| NEW `PolicyStages` + `(*Loader).ApplyPolicyStages` | `policy_stages.go` (58 lines) | the root's thirteen assignments cmd_cycle.go:442-449/:451-455 became ONE call; `parseGateStage`/`parseRouterStage` (:955-977) moved to the seam file as forwarders `s, _ := config.GateStage(v); return s` so `TestParseGateStage` (cmd_cycle_test.go:22-34), cmd_cycle.go:700 and cmd_loop_preflight.go:32 keep their spelling and their silent mapping (Q6) | config cannot import policy (overlays.go:26); strings/ints/bools cross; the typo hole is closed where the dials are assigned |
| domain.go:30 `filepath.Join(projectRoot, ".evolve", "domain.json")` | `filepath.Join(paths.EvolveDirOf(projectRoot), "domain.json")` (paths.go:198 — a byte-identical join; the 03b landing-fold precedent) | `LoadDomain`, `Domain`, `DefaultDeliverableKind` spellings and the error texts `read %s: %w` / `parse %s: %w` (:36/:40) unchanged; the `[orchestrator] WARN .evolve/domain.json unreadable` line (core/dispatch_signals.go:89) STAYS verbatim — handed to unit 05 by name (F3: `domainDefaultKind` :86-97 is a free function with no Center in scope) | the `.evolve` layout has ONE spelling |
| the registry path join at cmd_cycle.go:406, cmd_solution.go:43, router/policy.go:87, phase_verify.go:193 | NEW `config.RegistryPath(projectRoot string) string` (D7 — byte-identical path; cmd_cycle.go keeps the `registryPath` local because :527 reuses it for `phasespec.Load`; `path/filepath` stays imported in all three re-pointed files for their other joins) | the six remaining four-segment joins (cmd_cycle_outputs.go:98, phaseinventory.go:121, phases_create.go:242, phase_order.go:45, skillcheck.go:399, kerneltest/fixture.go:33 — the last keeps its `runtime.Caller` root and re-points only the suffix; the join Cleans to the same bytes) were re-pointed at the architecture-review fold, pinned by `TestRegistryPathSpelling_HasOneNonTestHome` (test 45: the `"docs", "architecture", "phase-registry.json"` join has ZERO non-test homes outside the leaf); phasespec/mergedcatalog.go:21 `registryRelPath` (a relative constant inside the sibling parser) stays — it goes with the READER (F4), which is a separate follow-up from the spelling | single-source-with-projection + consumer pin; config is the leaf, so every reader can import it cycle-free |
| — | — | STAYS in cmd_cycle.go: `policy.Load` + the `[policy] WARN` line + `pol = policy.Policy{}` (:422-425), `cfg.Mandatory = pol.MergeMandatory` (:427), `shipFloor` (:430-433), `cfg.AuditFailRoutesTo = router.FailureRouteFromPolicy(pol)` (:436), the `gatesCfg`/`routerCfg` locals (:439-440, reused at :514/:590/:700) and `recoveryCfg` (:441 — after the fold its sole use is the `policyStagesOf(...)` argument, said so in place), `phasespec.Load` and its `[phases] WARN` (:527-533) | need `policy.Policy` / `router` / `phasespec`; the policy-load belief has no module (F1); unit 05's composition-root series |
| — | — | STAYS: `router.PolicyForProject`'s partial overlay (router/policy.go:94-97 — Mandatory + AuditFail, no gate dials) on the silent facade | unifying it changes phase self-skip decisions (a behaviour change); named as a replicated belief with its :89-93/:96-99 rationale (Q5) |
| — | — | STAYS: cmd_solution.go:48-50 hand-rendered (no Center in that root) — pinned by name in the Center-less list; its stderr now ALSO shows the two registry codes as `registry warning [registry-malformed]: …` (D2b, test 43) | a Center for `evolve solution` is operator question 7 |
| — | — | OUT OF SCOPE (declared): "retire env flags as a by-product" (signal-center-design.md §12 row 7) — the nine env keys are prose-contracted in the PROTECTED flagregistry/registry_table.go:14-34 (integrity_surface.go:57) under the ceiling ratchet (:58) | a protected-surface change with its own slice (F8); only the §12 row note is revised |

The leaf imports stdlib (`encoding/json`, `errors`, `fmt`, `io/fs`, `maps`, `os`, `path/filepath`, `slices`, `sort`, `strconv`, `strings`) plus `internal/signalcenter` and `internal/paths` — both cycle-free (§1). An import-allowlist test (`importgraph_test.go`, the failurelearning shape, allowlist {signalcenter, paths}) replaces the implicit "stdlib-only" promise at config.go:6 and dynamic-phase-routing.md:166 with an explicit leaf declaration; the compiler stays the cycle guard (core → config in 17 files, policy → config). Collision check: `config.New(` — zero hits in non-test sources before the seam; `phaseconfig.New(` — zero hits; `phaseconfig.Load(` — ONE hit (cmd_campaign.go:394), which is why every source-scan needle in §6 carries `\b`. `internal/continuation` has its own `RegistryPath` — a different package, no collision at the `config.` spelling. The ProtectedSurface fragment `/go/internal/config/` is not a substring of `/go/internal/phaseconfig/` (checked), and the seam file has its own row.

Line projection, measured at landing: config.go 962 → 229 (loader + defaults family with the prose on doc comments); signals.go 99; vocabulary.go 120; condrule.go 100; routing_config.go 145; rollout.go 174; registry.go 179; env.go 120; validate.go 108; parse.go 133; policy_stages.go 58; domain.go 57 (+2: the paths import); every function < 50 (limits guard); cmd_cycle.go −47 +14 (numstat); the seam file 56.

## 3. API

```go
package config // go/internal/config — stdlib + internal/signalcenter + internal/paths (importgraph_test allowlist)

// signals.go — codes registered in init() via signalcenter.RegisterCode(signalcenter.ModuleConfig, …) with the §5 docs.
const (
    CodeUnknownValue       signalcenter.Code = "CONFIG_UNKNOWN_VALUE"
    CodeWeakSpine          signalcenter.Code = "CONFIG_WEAK_SPINE"
    CodeSpineOrder         signalcenter.Code = "CONFIG_SPINE_ORDER"
    CodeInertPhaseEnable   signalcenter.Code = "CONFIG_INERT_PHASE_ENABLE"
    CodeRegistryUnreadable signalcenter.Code = "CONFIG_REGISTRY_UNREADABLE"
    CodeRegistryMalformed  signalcenter.Code = "CONFIG_REGISTRY_MALFORMED"
)

// The legacy vocabulary the 18 in-package pins and cmd_solution.go:49 read — unexported typed literals,
// the ONE spelling each producer site passes to warn.
const (
    codeUnknownValue       = "unknown-value"
    codeWeakSpine          = "weak-spine"
    codeSpineOrder         = "spine-order"
    codeInertPhaseEnable   = "inert-phase-enable"
    codeRegistryUnreadable = "registry-unreadable"
    codeRegistryMalformed  = "registry-malformed"
)

// signalCodes is the ONE projection from a Warning.Code onto its registered signal code — six explicit
// pairs (data, not a string transform), walked by TestSignalCodes_TableIsTheOneProjectionWithNoPhantoms.
// emit reads it; a Code absent from it would reach the Center empty and be stamped SIGNALCENTER_MISSING_CODE
// (normalize.go:19; checkCode :68-71 — raised, never dropped), which the stream golden asserts never happens.
var signalCodes = map[string]signalcenter.Code{ /* six entries */ }

// Warning is one non-fatal resolution diagnostic (Result Object). Code is the closed set
// "unknown-value" | "weak-spine" | "spine-order" | "inert-phase-enable" | "registry-unreadable" | "registry-malformed";
// Message is the operator sentence (today's text verbatim; it becomes Event.Reason); Fields is what a triage reads.
type Warning struct {
    Code    string
    Message string
    Fields  map[string]string // step, source, key, value, default, path, err, missing, audit_pos, ship_pos, phase, stage, root, min_options
}

// Loader resolves the effective RoutingConfig. Collaborators are explicit at construction: the file reader
// (os.ReadFile by default; tests inject bytes/errors — no disk) and the Center through an accessor read at
// every use (nil accessor or nil Center = Null Object: (*Center)(nil).Emit is a no-op, center.go:130-133).
type Loader struct{ /* readFile func(string) ([]byte, error); signals func() *signalcenter.Center */ }
type Option func(*Loader)
func New(opts ...Option) *Loader                               // Factory Method; defaults: os.ReadFile, no Center (the 01-03b `New` spelling)
func WithReadFile(fn func(name string) ([]byte, error)) Option  // the filesystem port
func WithSignals(c func() *signalcenter.Center) Option          // late-bound accessor (03b idiom, failurelearning.go:88-90)
func (l *Loader) SignalsWired() bool

// Load — config.go:518-534 verbatim in shape: defaults → [EVOLVE_USE_PHASE_REGISTRY != "0": readRegistry →
// applyRegistry] → applyEnv → validateSpine → validateInertEnables. After each step the warnings that step
// appended (a slice range captured by index) are stamped step/source/path; then every Warning is emitted ONCE,
// in order, Origin "Loader.Load", Cycle 0. Never fails; returns the value and the slice.
func (l *Loader) Load(registryPath string, env map[string]string) (RoutingConfig, []Warning)

// ApplyPolicyStages — the thirteen assignments cmd_cycle.go:442-449/:451-455: cfg by VALUE in, a new value out.
// The seven gate dials through GateStage, the two router dials through RouterStage, the ints/bools copied;
// a word outside its ladder → CONFIG_UNKNOWN_VALUE {key: "gates.eval_gate", value, default: "off", step: "policy",
// source: "policy"} and StageOff (the value the silent root produced). Origin "Loader.ApplyPolicyStages".
func (l *Loader) ApplyPolicyStages(cfg RoutingConfig, ps PolicyStages) (RoutingConfig, []Warning)

// PolicyStages is the strings/ints/bools Parameter Object the composition root projects from
// policy.GatesConfig / RecoveryConfig / RouterConfig / ParallelEvaluateConfig (config never imports policy).
// Field order = the root's assignment order; constructed POSITIONALLY by the in-package tests so a new field
// breaks the build (the cmd/evolve consumer pin uses keyed fields — vet's composites check).
type PolicyStages struct {
    ContractGate, EvalGate, TriageCapGate, TopNGate, ReviewGate string // gates.* (GateStage)
    PhaseRecovery, SpineFloor                                   string // recovery.* (GateStage)
    RouterReplan                                                string // router.router_replan (RouterStage)
    ParallelEvaluate                                            string // parallel_evaluate.stage (RouterStage; pre-sanitised by policy, Q7)
    ParallelEvaluateConcurrency                                 int
    RoutingJudge, ReconDigest                                   bool
    RePlanMaxDepth                                              int
}

// Load is the Strangler Fig facade: New().Load — silent (no Center). Kept for router/policy.go:88 (per phase),
// phase_verify.go:194, cmd_solution.go:47, kerneltest/fixture.go:45 and every cross-package test.
func Load(registryPath string, env map[string]string) (RoutingConfig, []Warning)

// RegistryPath is the ONE spelling of the registry's location: <root>/docs/architecture/phase-registry.json.
func RegistryPath(projectRoot string) string

// The two pure ladders (exported so the root's forwarders keep TestParseGateStage's mapping without a copy).
func GateStage(v string) (Stage, bool)   // "0"|"off"→Off, "shadow", "enforce"; else (Off, false)               — config.go:852-858
func RouterStage(v string) (Stage, bool) // "0"|"off"→Off, "shadow", "advisory", "enforce"; else (Off, false)   — config.go:831-839

// unexported — spellings kept for the in-package tests that name them
func defaults() RoutingConfig                                                   // 15-line composer (config.go)
func defaultRollout() RolloutStages                                             // the :613 literal, one field per line
func defaultMandatory() []string                                                // :634; doc comment = the :615-633 divergence block
func defaultPhaseEnable() map[string]Enable                                     // :645-650
func mustCondRule(expr string) CondRule                                         // the :124 panic, reachable
func (l *Loader) applyRegistryFile(cfg *RoutingConfig, registryPath string, ws *[]Warning) // the registry step + its range stamp
func (l *Loader) readRegistry(path string, ws *[]Warning) (registryDoc, bool)   // ENOENT silent | UNREADABLE | MALFORMED
func applyRegistry(cfg *RoutingConfig, doc registryDoc, ws *[]Warning)          // coordinator (4 lines)
func applyRegistryDials(cfg *RoutingConfig, c registryConfig, ws *[]Warning)    // :668-692 + :703-705
func applyDeliverableKinds(cfg *RoutingConfig, kinds map[string]DeliverableKindSpec, ws *[]Warning) // :693-702, sorted keys
func applyConditional(cfg *RoutingConfig, rules map[string]string, ws *[]Warning)                   // :706-712, sorted keys
func applyPhases(cfg *RoutingConfig, phases []registryPhase, ws *[]Warning)     // :713-724; stamps key=phases[<name>].enabled
func applyEnv(cfg *RoutingConfig, env map[string]string, ws *[]Warning)         // for _, d := range envDials { from := len(*ws); d.apply(…); stamp((*ws)[from:], "key", d.key) }
type envDial struct{ key string; apply func(*RoutingConfig, string, *[]Warning) }
var envDials []envDial                                                           // declared order = the :728-776 order; arms envDynamicRouting … envMaxOptionalInsertions
func parseSandboxMode(v, current string, ws *[]Warning) string                   // :749-755 lifted; the :754 message names current
func parseStage(v, varName string, ws *[]Warning) Stage                          // signature kept; Fields{key: varName, value, default: "off"}
func parseEvidenceStage(v, varName string, ws *[]Warning) Stage
func parseMode(v string, ws *[]Warning) Mode                                     // Fields{key: "routing_mode", value, default: "llm"}
func parseModelRouting(v, varName string, ws *[]Warning) ModelRouting
func parseEnable(v string, ws *[]Warning) Enable                                 // Fields{key: "enabled" (overridden by applyPhases), value, default: "content"}
func validateSpine(cfg RoutingConfig, ws *[]Warning)                             // signature kept
func validateInertEnables(cfg RoutingConfig, ws *[]Warning)
func warn(ws *[]Warning, code, msg string, fields map[string]string)             // the ONE appender — copies fields into the Warning's own map (always non-nil, no nil branch anywhere); no bare Warning{} literal survives outside it
func stamp(ws []Warning, kv ...string)                                           // sets each pair on every Warning in the range
func (l *Loader) emit(origin string, ws []Warning)                               // the ONE producer: Event{Cycle 0, Module config, Origin, Kind config.warning, WARN, Code: signalCodes[w.Code], Reason: w.Message, Fields: w.Fields}
func (l *Loader) center() *signalcenter.Center
```

```go
package main // go/cmd/evolve/cmd_cycle_config.go — the unit-08 seam (56 lines)

// wiredRoutingConfigLoader is the ONE construction (TestRoutingConfigLoader_OneConstructionSite, needle
// `\bconfig\.New\(`, onlySite this file). The root's Center exists 31 lines before Load (cmd_cycle.go:376 vs :407),
// so there is no lazy accessor pair: the host is a function (wireOrchestratorDeps), not a literal-buildable struct.
func wiredRoutingConfigLoader(signals *signalcenter.Center) *config.Loader {
    return config.New(config.WithSignals(func() *signalcenter.Center { return signals }))
}
// policyStagesOf is the ONE projection of the four policy accessors onto the leaf's Parameter Object
// (TestPolicyStagesOf_ProjectsThePolicyAccessors is the consumer pin).
func policyStagesOf(g policy.GatesConfig, r policy.RecoveryPolicy, rt policy.RouterPolicy, pe policy.ParallelEvaluateConfig) config.PolicyStages
// forwarders — cmd_cycle.go:955-977 moved verbatim in meaning: silent, for :700 and cmd_loop_preflight.go:32 (Q6)
func parseGateStage(stage string) config.Stage   { s, _ := config.GateStage(stage); return s }
func parseRouterStage(stage string) config.Stage { s, _ := config.RouterStage(stage); return s }
```

cmd_cycle.go edits (the only production spelling changes, all at the root): :406-410 → `registryPath := config.RegistryPath(projectRoot)` (kept as a local: :527 reuses it) / `loader := wiredRoutingConfigLoader(signals)` / `cfg, _ := loader.Load(registryPath, filterEvolveEnv(os.Environ()))` with the seam comment "every warning the Loader resolves rides the Center as config.warning (the root StderrSink renders WARN; the cycle-less durable sink files it); the discarded slice is the same data — nothing to print twice" (the rule-12 defence); the `[config] WARN` loop deleted (D1); the `strings` import pruned. :442-455 → `cfg, _ = loader.ApplyPolicyStages(cfg, policyStagesOf(gatesCfg, recoveryCfg, routerCfg, pol.ParallelEvaluateConfig()))` (the `peCfg` local folds in). :955-977 deleted (moved to the seam). cmd_solution.go:43, router/policy.go:87-88, phase_verify.go:193-194 → `config.RegistryPath(…)` (D7); at the architecture-review fold also cmd_cycle_outputs.go:98, phaseinventory.go:121, phases_create.go:242, phase_order.go:45 (its `path/filepath` import pruned — the join was its only use), skillcheck.go:399 and kerneltest/fixture.go:33 (`config.RegistryPath(filepath.Join(filepath.Dir(self), "..", "..", ".."))` — same bytes after Clean) → `config.RegistryPath(…)`; phaseinventory and skillcheck gain the `config` import (cycle-free: the leaf imports only signalcenter, paths and their stdlib-plus-ipcenv/log closure).

## 4. Patterns

| Pattern | Force it answers |
|---|---|
| **Extract Class** (`Loader`) + **explicit DI via functional options** (`New(opts…)`, `WithReadFile`, `WithSignals`) — the 03b shape (failurelearning.go:66-90) minus positional required deps (config has none: the reader defaults to `os.ReadFile`) | the ENOENT-vs-unreadable-vs-malformed branch (config.go:655-665) must be testable in memory with no chmod/tmp tricks; the Center is built before Load in the ONE production root (cmd_cycle.go:376 vs :407) but absent in four others |
| **Null Object** — nil accessor / nil Center → `(*Center)(nil).Emit` no-op (center.go:130-133); the package-level `Load` = `New().Load` | `router.PolicyForProject` re-loads PER PHASE (router/policy.go:86-100; four phase callers) — an emitting Load would fire every registry warning four extra times per cycle; cmd_solution.go, phase_verify.go, kerneltest have no Center |
| **Strangler Fig facades** at THREE levels: the exported `Load(path, env)`; the unexported spellings `defaults()`, `parseStage(v, name, &ws)`, `parseCondRule`, `validateSpine(cfg, &ws)`, `staticSpinePhases`; the root forwarders `parseGateStage`/`parseRouterStage` onto `GateStage`/`RouterStage` | 58 importers, 18 in-package pins, the cross-package tests, `TestParseGateStage` (cmd_cycle_test.go:22), cmd_cycle.go:700 and cmd_loop_preflight.go:32 never learn the unit exists; zero undeclared call-site changes |
| **Parameter Object** (`PolicyStages`, strings + ints + bools) + **Result Object** (`Warning{Code, Message, Fields}`) | config can NEVER import internal/policy (overlays.go:26); the thirteen dials cross as plain values and `policyStagesOf` gets a consumer pin; one Warning value serves three consumers (the event, the legacy print in cmd_solution.go, the 18 pins) with one belief per field — the panel's objection to a stored `Signal` beside `Code` |
| **Single-source-with-projection + consumer pin** ×5: the six kebab codes → `signalCodes` table (pin: exactly six entries, every value registered under `ModuleConfig`, no phantoms; the stream golden asserts no `SIGNALCENTER_MISSING_CODE`); `RegistryPath` (pins: the spelling, test 31; ONE non-test home outside the leaf, test 45 — ten re-pointed sites after the review fold); the nine policy-key spellings `ApplyPolicyStages` names (`gates.contract_gate` … `parallel_evaluate.stage`) ↔ policy.json's own `json` tags (pin: `TestPolicyStages_KeysMatchPolicyJSONTags`, test 46 — reflection over `policy.Policy`'s section tags and the four block structs, so a policy rename that leaves config's triage label behind goes red); `paths.EvolveDirOf` for domain.json (pin: `TestLoadDomain_ReadsUnderTheEvolveDir`); config's gate defaults ↔ policy's compiled accessors (pin: `TestDefaults_GateDialsMatchPolicyCompiledDefaults`, package `config_test` importing policy — the spine_contract_test.go idiom) | never_duplicate: the code vocabulary, the registry path and the `.evolve` join each had no single home; the two-source gate defaults (config.go:613 vs routing_gates.go:108-125 / budgets_recovery.go:151 / routing_gates.go:42 / merge_catalog.go:27) stay two-source (F6) but become a PINNED belief, not prose |
| **Strategy table** (`envDials` in declared order, one named arm per var) + **Extract Method** (`parseSandboxMode`, `mustCondRule`, `defaultRollout`/`defaultMandatory`/`defaultPhaseEnable`, `applyRegistryDials`/`applyDeliverableKinds`/`applyConditional`/`applyPhases`, `applyRegistryFile`) | limits_test.go (< 50 lines, nesting ≤ 4, file < 800); nine env arms independently table-testable with a deterministic warning order equal to today's :728-776 sequence; each registry step is a `fields.step` landing; the :124 panic is reachable |
| **Range stamping** (`stamp(ws[from:], …)` after each pipeline step in `Load`, at each env dial, and after `parseEnable` in `applyPhases`) | the triage fields (step/source/path/key) attach without changing any pinned parser signature and without threading a context through twelve helpers; the parsers stamp what they know (value, default, the registry key), the callers stamp what only they know (the env var, the phase name, the source) |
| **Factory Method + ONE-construction source-scan guard** (`wiredRoutingConfigLoader`; `TestRoutingConfigLoader_OneConstructionSite`, needle `\bconfig\.New\(`, onlySite cmd_cycle_config.go, a regexp walker `nonTestSourcesMatching` skipping `internal/config/` — the `TestNilSignalBridgeRootsAreExplicit` regexp shape, because `nonTestSourcesMentioning` in carryover_lifecycle_test.go:117-153 is a substring scan that cannot honour `\b`) | `New` is exported; visibility no longer proves one wired Loader; the `--simulate` root (cmd_cycle_simulate.go) never calls Load, so its twin constructs the Loader directly like 02/03/03b (cmd_cycle_signal_center_test.go:266-332) |
| **Import allowlist + limits guards as tests** (importgraph_test.go allowlist {signalcenter, paths}; limits_test.go copied from failurelearning) | the "stdlib-only" promise (config.go:6, dynamic-phase-routing.md:166) becomes an EXPLICIT leaf declaration; the compiler stays the cycle guard |
| **Immutability** (`ApplyPolicyStages(cfg RoutingConfig, …) (RoutingConfig, …)`) | the root's in-place mutation (cmd_cycle.go:442-455) becomes value-in/value-out per the operator's immutability rule; the `*RoutingConfig` mutation inside Load's private steps is kept (a local under construction, never shared) |
| **Characterization goldens** (the FROZEN registry copy's RoutingConfig, the compiled defaults, the kitchen-sink `{Code, Message}` sequence — captured on 8e8f080f BEFORE any move; the stream sequence golden captured after) | ADR-0103 item 4: the resolved value and the warning text are the on-wire contracts every phase, the router and cmd_solution read; the live registry is a cycle-edited file (8 commits since 2026-08-01, 3 autonomous) and acs/cycle1168:195 runs this package as a green-floor predicate, so the golden's input is a frozen copy and the live file keeps its property-style guard (`TestLoad_RealRegistry`) — critic fold 1 |
| Deliberately NOT used: a **resolver package above config+policy** (two 100 % gates, a router import, and a behaviour change for phase self-skip decisions); **`config.ApplyPolicy(pol policy.Policy)`** (an import cycle); a **`Signal` field on Warning** (a stored projection beside its source); **`DisallowUnknownFields`** on the registry (the live file carries four `_*_note` keys — F5 is an allow-list warning); **`CONFIG_POLICY_LOAD_FAILED`** (a policy.json belief the leaf never observes; both judges rejected it — F1); **`CONFIG_DOMAIN_UNREADABLE`** (no Center in scope at dispatch_signals.go:86 — F3); a positive **`CONFIG_RESOLVED` INFO** (a stream addition on every boot; units 01-03b add exactly one WARN kind); **`CONFIG_DELIVERABLE_KIND_INCOMPLETE` / `CONFIG_ENV_VALUE_IGNORED` / `CONFIG_STAGE_UNKNOWN`** (second names for `UNKNOWN_VALUE`; `fields.key`/`source` carry the distinction and the 1:1 Code→signal projection keeps the 18 pins untouched); a **nil-Fields branch in `stamp`** (every producer passes fields and `warn` copies them into a fresh map, so the branch would be dead code under `.cover-strict` — critic fold 6) | |

## 5. Module contract

| Item | Value |
|---|---|
| Module | `config` — ALREADY in the closed set (event.go:84; `knownModules` :100; `TestModule_ClosedSet` event_test.go:39-41). The 26-module literal (event_test.go:49-50) and `Modules()` are UNTOUCHED — a unit that bumps 26 → 27 is wrong. Code prefix `CONFIG_` (`codePrefix` event.go:119-121). `RegisterCode` (registry.go:54-79) refuses a code whose prefix does not belong to its module and records a `Conflict`; `TestSignalCenterRegistry_EveryLinkedModuleRegistersCleanly` (cmd_cycle_signal_center_test.go:135) asserts the linked registry is clean |
| Kind | `config.warning` — NEW, non-terminal: `KindConfigWarning Kind = "config.warning"` as a sibling const after event.go:145 (`KindFailureLearningWarning`), added to `knownKinds` (:155-161), NOT to `terminalKinds` (:165-168), appended to the `all` slice of `TestKind_ClosedSetAndTerminal` (event_test.go:69-72; the kind count is derived at :84-86 — no literal). §5.2 row after signal-center-design.md:187 (wording in that file). Rejected: a second INFO kind |
| Common fields | `Event.Cycle = 0` (the loader is batch-level: `wireOrchestratorDeps` runs before any cycle number exists; cycle-less events land in `<evolveDir>/signals.ndjson` per `newRootSignalCenter` cmd_cycle.go:349-354, like `quota.paused`), `Event.Phase = ""`, `Severity = WARN`, `Reason = Warning.Message` verbatim, `Origin` = the exported method that produced it (`Loader.Load` for codes 1-6 via Load; `Loader.ApplyPolicyStages` for the policy-sourced code 1); `fields.step` on every event ∈ {`registry`, `env`, `spine`, `inert`, `policy`} — the name of the split file/function a triage opens (registry.go, env.go, validate.go, validate.go, policy_stages.go); `fields.source` ∈ {`registry`, `env`, `policy`} on `CONFIG_UNKNOWN_VALUE` and the two registry codes |
| `CONFIG_UNKNOWN_VALUE` WARN | a routing dial's value is outside its closed vocabulary, or a contract is incomplete, and the documented fail-safe was taken (never a kill-path enable): the ten `unknown-value` sites — :699 (`key=deliverable_kinds[<kind>]`, `root`, `min_options`), :710 (`key=conditional_mandatory[<phase>]`, `value`, `err`), :753-754 (`key=EVOLVE_SANDBOX`, `value`, `default=<current>`), :766 (`key=EVOLVE_CONDITIONAL_MANDATORY`, `value`, `err`), :774 (`key=EVOLVE_MAX_OPTIONAL_INSERTIONS`, `value`), :841 (`key=dynamic_routing` from the registry / `EVOLVE_DYNAMIC_ROUTING` or `EVOLVE_PHASE_IO` from env, `value`, `default=off`), :860 (`key=EVOLVE_COMMIT_EVIDENCE`, `default=off`), :872 (`key=routing_mode` / `EVOLVE_ROUTING_MODE`, `default=llm`), :890 (`key=model_routing`, `default=static`), :904 (`key=phases[<name>].enabled`, `default=content`) — PLUS the two silent branches closed: the :762 `strings.Cut` miss (NEW message `EVOLVE_CONDITIONAL_MANDATORY=%q: missing ':' (want phase:expr)`, `key`, `value`, `err`; the value stays ignored — D3) and the policy-dial typos (message = `parseEvidenceStage`'s / `parseStage`'s existing format with the policy key as varName, e.g. `gates.eval_gate="enfroce" unknown (want off\|shadow\|enforce), defaulting to off`; `key` ∈ {`gates.contract_gate`, `gates.eval_gate`, `gates.triage_cap_gate`, `gates.topn_gate`, `gates.review_gate`, `recovery.phase_recovery`, `recovery.spine_floor`, `router.router_replan`, `parallel_evaluate.stage`} — the policy.json tag spellings, routing_gates.go:64-71, budgets_recovery.go:139-144, routing_gates.go:31, merge_catalog.go:8; `default=off`, `step=policy`, `source=policy` — D4). Replaces the cmd_cycle.go:409 rendering of all ten; `parallel_evaluate.stage` typos never reach it from policy (merge_catalog.go:33-38 sanitises first — Q7) |
| `CONFIG_WEAK_SPINE` WARN | `mandatory_phases` (registry or `EVOLVE_MANDATORY_PHASES`) omits audit and/or ship (config.go:790-802); reason = the :800 sentence; `fields.missing` ∈ {`audit`, `ship`, `audit+ship`}, `step=spine`. Replaces :409 |
| `CONFIG_SPINE_ORDER` WARN | the phases[] walk (`cfg.Order`, NOT `spine_order` — spine_order_validation_test.go:14/:23) places ship before audit (:808-822); reason = the :820 sentence; `fields.audit_pos`, `ship_pos`, `step=spine`. Replaces :409 |
| `CONFIG_INERT_PHASE_ENABLE` WARN | `PhaseEnable[p]=on` while `dynamic_routing<advisory` and p is neither mandatory nor in the static spine (:563-587; the cycle-120 class); reason = the :584 sentence; `fields.phase`, `stage` (`cfg.Stage.String()` — "0" for Off, Q3), `step=inert`. Replaces :409; the :583-585 emission had coverprofile count 0 before — test 2 is its first positive pin |
| `CONFIG_REGISTRY_UNREADABLE` WARN | NEW: `docs/architecture/phase-registry.json` exists but the read failed — any error other than `fs.ErrNotExist` (EACCES, EISDIR — the directory-at-path idiom); the loop proceeds on the compiled baseline (Mandatory scout/build/audit/ship, no triage, empty Order); reason `phase registry unreadable (running on the built-in baseline — no triage, no registry order): read <path>: <err>`; `fields.path`, `err`, `step=registry`, `source=registry`; legacy Code `registry-unreadable`. Was silent (:656-659) — D2 |
| `CONFIG_REGISTRY_MALFORMED` WARN | NEW: the registry read but `json.Unmarshal` failed (:661-663); same fallback; reason `phase registry malformed (running on the built-in baseline — no triage, no registry order): parse <path>: <err>`; `fields.path`, `err`, `step=registry`, `source=registry`; legacy Code `registry-malformed`. Was silent — D2. Severity WARN per the units 01-03b precedent (schema-1.0 fixes severity per code; INCIDENT is operator question 2) |
| Console rendering (verified at landing) | root `StderrSink` (sinks.go:123-146, `FormatLine`; WARN-filtered at cmd_cycle.go:355; cycle omitted when 0; fields sorted by key): `[config] config.warning WARN CONFIG_REGISTRY_MALFORMED seq=1 origin=Loader.Load — phase registry malformed (running on the built-in baseline — no triage, no registry order): parse /r/docs/architecture/phase-registry.json: unexpected end of JSON input err=unexpected end of JSON input path=/r/docs/architecture/phase-registry.json source=registry step=registry` · `[config] config.warning WARN CONFIG_UNKNOWN_VALUE seq=4 origin=Loader.ApplyPolicyStages — gates.eval_gate="enfroce" unknown (want off\|shadow\|enforce), defaulting to off default=off key=gates.eval_gate source=policy step=policy value=enfroce`. On a malformed registry the console carries TWO lines from two channels until F4: the sibling parser's raw `[phases] WARN builtin registry load failed (…)` on `os.Stderr` (cmd_cycle.go:527-533) and this Center-rendered line on the root's console writer |
| Removed lines | ONE: cmd_cycle.go:409 `[config] WARN %s: %s\n` on `os.Stderr` (no test pinned it — grep over `*_test.go`: 0; the production console IS stderr, cmd_cycle.go:226, so the operator still sees one line per warning, now module-tagged and durable). KEPT verbatim (declared): cmd_solution.go:49 (Center-less root; pinned by name; now ALSO prints the two registry codes — D2b), cmd_cycle.go:424 `[policy] WARN …` (policy has no module — F1), cmd_cycle.go:533 `[phases] WARN builtin registry load failed` (the sibling parser — F4), core/dispatch_signals.go:89 `[orchestrator] WARN .evolve/domain.json unreadable` (unit 05 — F3) |
| Phantom codes | `unknown-key` and `deprecated-flag` (config.go:484) have no producer anywhere; NOT registered; deleted from the comment (D6); config_legacyflags_test.go:41-43 stays green (an absence pin) |
| Registry facts honoured | codes match `^[A-Z][A-Z0-9]*(_[A-Z0-9]+)+$` (event.go:190) and carry the `CONFIG_` prefix; `TestRegistryConflicts_RealRegistryIsClean` and `TestCode_FormatAndModulePrefix` stay green; `TestSignalsCodes_RepoDocIsInSyncWithTheLinkedRegistry` (cmd_signals_test.go:17-33) turned cmd/evolve RED the moment the codes were registered until `make -C go build && ./go/bin/evolve signals codes generate` regenerated signal-codes.md (92 → 98; `### config` sorts between `### carryover` and `### failurediag`) — no CI step runs `codes check`, the TEST is the gate; the six docs avoid a `\|` inside a table cell (the generator does not escape one); emission volume: one event per Warning per root Load; a clean load of the live registry emits ZERO events (config_realregistry_test.go:74 asserts no weak-spine; the live file's dials are all in-vocabulary) so the happy-path stream is byte-identical |

## 6. Tests (red first)

Every test was written before the code it names; step 1's pins were GREEN on 8e8f080f and each proven RED by hand-applying its named mutant once (§10); step 2's tests were red because the API, the kind and the codes did not exist. `go test -count=1` always (config_realregistry_test.go reads `../../../docs` — the cache is blind to it); no `t.Skip`. Package `config` unless noted. The fixture halves live in `internal/config/testdata/`: `frozen_registry.json` (the live registry at 8e8f080f — critic fold 1), `kitchen_sink_registry.json` (dynamic_routing/model_routing/routing_mode bogus, ONE deliverable_kinds hole, ONE bad conditional, phases `[ship, audit, plan-review]` with an unknown `enabled` on ship and `plan-review` enabled `on` — the non-spine phase critic fold 2 added so the inert branch fires), and the goldens `real_registry_config.golden.json`, `defaults.golden.json`, `kitchen_sink_warnings.golden.json` (a `{Code, Message}` projection — critic fold 4), `kitchen_sink_signals.golden.json`.

Step 1 — pins and goldens on 8e8f080f (nothing moves; `loader_pins_test.go`):

1. `TestLoad_WarningOrderIsRegistryThenEnvThenSpineThenInert` — fixture registry `{dynamic_routing:"bogus", phases:[{name:plan-review, enabled:on}]}` + env `{EVOLVE_PHASE_IO:"bogus", EVOLVE_DYNAMIC_ROUTING:"off", EVOLVE_MANDATORY_PHASES:"scout,build"}` → codes exactly `[unknown-value(dynamic_routing), unknown-value(EVOLVE_PHASE_IO), weak-spine, inert-phase-enable]` with the four literal messages — kills `swap validateSpine/validateInertEnables (:532-533)`.
2. `TestValidateInertEnables_FiresForForceEnabledNonSpinePhasesInSortedOrder` (covers :578, :583-585 — the first POSITIVE pin) — registry `plan-review` and `zz-custom` enabled `on`, `triage` (spine) `on`, `custom-mandatory` `on` AND in `mandatory_phases` (so the mandatory guard's `continue` counts), `build-planner` default Off, env `EVOLVE_DYNAMIC_ROUTING=shadow` → exactly two `inert-phase-enable` in the order `[plan-review, zz-custom]` with the :584 literal; `=advisory` → none — kills `>= → >` at :564, `sort.Strings removed` :572 (the EnableOn guard is :574; :577 is the mandatory guard).
3. `TestApplyEnv_ConditionalMandatoryParseErrorWarns` (covers :766-767) — `EVOLVE_CONDITIONAL_MANDATORY="tdd:nonsense"` → `unknown-value` `EVOLVE_CONDITIONAL_MANDATORY="tdd:nonsense": no comparison operator in "nonsense"`, `Conditional[tdd]` still the default rule — kills `else branch deleted`.
4. `TestParseMode_UnknownWarnsAndDefaultsToLLM` (covers :872-873) — `EVOLVE_ROUTING_MODE=bogus` → `ModeDynamicLLM` + `routing_mode="bogus" unknown, defaulting to llm` — kills `default → ModeStaticPreset`.
5. `TestApplyRegistry_PhaseWithoutNameIsSkipped` (covers :715) — phases `[{name:"", enabled:on}, {name:scout}]` → `Order == [scout]`, no `PhaseEnable[""]` — kills `continue removed`.
6. `TestLoad_RegistryConditionalMergesOverTheTddDefault` — `conditional_mandatory {plan-review: "triage.unified_size!="}` → `Conditional` has BOTH `tdd == DefaultTddRule()` and `plan-review {triage.unified_size, !=, ""}` (the live registry's empty-RHS rule, accepted by :931) — kills `cfg.Conditional replaced before the loop`.
7. `TestLoad_MalformedRegistryFallsBackToTheCompiledBaseline` (characterisation; covers :662-663) — a temp file `{` → cfg deep-equals `Load("/nonexistent", …)` (Mandatory scout/build/audit/ship, Order nil, no DeliverableKinds). Written directly in its step-2 shape — the design's `ws == nil` half (to be deleted in step 2) was never added, so the one declared test edit is void. The design's mutant `return doc, true on a parse error` is value-EQUIVALENT on both trees (`json.Unmarshal` validates the whole input before decoding, so a syntax error leaves `doc` zero and applying it is a no-op); the mutant that kills the malformed branch is test 20's `the malformed-branch warn dropped`.
8. `TestLoad_RealRegistry_MatchesTheGolden` (G1) — `json.MarshalIndent(cfg)` for `testdata/frozen_registry.json` == `testdata/real_registry_config.golden.json`, warnings empty; the live file keeps `TestLoad_RealRegistry` as its property-style guard — kills `Triggers unset in applyPhases`, `SpineOrder dropped`.
9. `TestLoad_BuiltinDefaults_MatchTheGoldenThroughTheDefaultReader` (G2) — `Load("/nonexistent/phase-registry.json", nil)` through the DEFAULT reader on the real filesystem == `testdata/defaults.golden.json` AND `len(ws) == 0` — kills `ParallelEvaluateConcurrency 3 → 2`, `ENOENT reported`.
10. `TestLoad_KitchenSink_WarningsMatchTheGolden` (G3) — the kitchen sink + env `{EVOLVE_SANDBOX:bogus, EVOLVE_MAX_OPTIONAL_INSERTIONS:x, EVOLVE_COMMIT_EVIDENCE:advisory, EVOLVE_MANDATORY_PHASES:scout, EVOLVE_DYNAMIC_ROUTING:off, EVOLVE_USE_PHASE_REGISTRY:false}` → the ordered `{Code, Message}` JSON == `testdata/kitchen_sink_warnings.golden.json` (twelve entries; `false` keeps the registry ON — Q1 characterised) — kills any append reorder (an env arm moved), any message edit.
11. `TestLoadDomain_ReadErrorOtherThanAbsenceIsReturned` (covers domain.go:36-37) — a DIRECTORY at `<root>/.evolve/domain.json` → `ok false`, `err` starts `read <path>:` — kills `nil on any error`.
12. `TestApplyRegistry_DeliverableKindHoleWarnsButKeepsTheSpec` — `deliverable_kinds {document:{root:"", min_options:0}}` → the :699 message AND `cfg.DeliverableKinds[document]` present — kills `assignment skipped on a hole`.
13. `TestLoad_UsePhaseRegistry_OnlyTheLiteralZeroDisables` — `"0"` → Order empty; `"false"`/`"off"`/`""` → Order non-empty (Q1) — kills `!= "0" → == ""`.
14. (cmd/evolve, cmd_cycle_test.go) `TestParseRouterStage_LadderAndUnknown` — `"advisory"` → Advisory, `"unknown"` → Off, `" shadow "` → Shadow — the missing twin of `TestParseGateStage` — kills `RouterStage drops advisory` after the move.
15. `TestLimits_FunctionsFilesAndNestingStayWithinTheBar` (limits_test.go, copied from failurelearning) — RED on the unsplit tree (`config.go: 962 lines ≥ 800; defaults is 65 lines; applyRegistry is 59; applyEnv is 51`) — proves the split is mandatory; green only after step 2.

Step 2 — the leaf (`loader_test.go`, `signals_test.go`, `policy_stages_test.go`, `importgraph_test.go`, `defaults_parity_test.go`; red: the API, the kind and the codes did not exist):

16. `TestNew_DefaultsToDiskAndNoCenter` — `New().SignalsWired() == false`; `New().Load(<temp file>)` reads through `os.ReadFile` — kills `readFile default nil`.
17. `TestLoader_UseRegistryZeroNeverCallsTheReader` — a counting reader + `EVOLVE_USE_PHASE_REGISTRY=0` → no call, no event; the `=false` row → the reader IS called — kills `!= "0" → == "0"`.
18. `TestLoader_Load_RegistryAbsentIsSilent` — `WithReadFile` returning `&fs.PathError{Err: fs.ErrNotExist}` → defaults, zero warnings, zero events — kills `errors.Is(err, fs.ErrNotExist) → false`.
19. `TestLoader_Load_RegistryUnreadableWarnsAndRunsTheBaseline` — `WithReadFile` returning `syscall.EACCES` → exactly one `Warning{Code "registry-unreadable", Fields{step:registry, source:registry, path, err}}`; the Center holds one `Event{Module config, Kind config.warning, Severity WARN, Origin "Loader.Load", Cycle 0, Code CONFIG_REGISTRY_UNREADABLE, Reason == Message, Fields == Fields}`; cfg == defaults — kills `errors.Is widened`, `MALFORMED at the read branch`, `Origin "Load"`, `Cycle 1`.
20. `TestLoader_Load_RegistryMalformedWarnsAndRunsTheBaseline` — `WithReadFile` returning `[]byte("{")` → `CONFIG_REGISTRY_MALFORMED` with `unexpected end of JSON input` in Message and `err`; `Mandatory == [scout build audit ship]`, `Order nil` — kills `the malformed-branch warn dropped`.
21. `TestLoader_Load_StampsStepSourceKeyAndPathPerRange` — the kitchen sink + `EVOLVE_CONDITIONAL_MANDATORY="noColon"` (thirteen warnings) → registry-range warnings carry `step=registry`, `source=registry`, `path=<registryPath>` and the keys `dynamic_routing`, `routing_mode`, `model_routing`, `deliverable_kinds[document]`, `conditional_mandatory[plan-review]`, `phases[ship].enabled`; env-range warnings carry `step=env`, `source=env`, no path, and the keys `EVOLVE_COMMIT_EVIDENCE`, `EVOLVE_SANDBOX`, `EVOLVE_CONDITIONAL_MANDATORY` (the NEW missing-':' `unknown-value` with `value` and `err`), `EVOLVE_MAX_OPTIONAL_INSERTIONS`; `weak-spine` `step=spine`, `missing=audit+ship`; `spine-order` `audit_pos=1`/`ship_pos=0`; `inert-phase-enable` `phase=plan-review`/`stage=0`; a second load with `EVOLVE_DYNAMIC_ROUTING=bogus`, `EVOLVE_ROUTING_MODE=bogus` → `key=EVOLVE_DYNAMIC_ROUTING` / `EVOLVE_ROUTING_MODE` while the Messages still read `dynamic_routing=…` / `routing_mode=…` (D9) — kills `range off-by-one`, `stamp("env") on the registry range`, `Cut-miss warning dropped`, `env key not overridden at the arm`.
22. `TestLoader_Load_EmitsEveryWarningOnceInOrderAndMatchesTheStreamGolden` — `len(events) == len(ws) == 12`; each event carries its warning's Message and Fields verbatim; the `{module, kind, code, origin, step}` sequence == `testdata/kitchen_sink_signals.golden.json` (captured after the leaf went green, then frozen — the NEW stream contract, D8); no event carries `SIGNALCENTER_MISSING_CODE`, `SIGNALCENTER_UNREGISTERED_CODE` or a `drift` field — kills `emit inside the registry step` (double count), `a signalCodes entry removed`.
23. `TestLoad_FacadeReturnsTheLoaderValuesAndEmitsNothing` — `Load(kitchen sink) == New().Load(kitchen sink)` (cfg and warnings deep-equal) — kills `facade with its own defaults`; the facade-with-a-Center mutant cannot be built (`New()` takes no Center) — recorded EQUIVALENT.
24. `TestSignalCodes_TableIsTheOneProjectionWithNoPhantoms` — `signalCodes` has exactly six entries; keys == the six kebab literals; every value resolves in `signalcenter.IsRegistered` to `ModuleConfig` and its doc (from `signalcenter.RegisteredCodes()[ModuleConfig]` — `IsRegistered` returns only the owner) mentions `fields.step`; `RegistryConflicts()` empty; neither `unknown-key` nor `deprecated-flag` nor `CONFIG_UNKNOWN_KEY` — kills `a code registered under another module`, `spine-order missing from the table`, `a phantom registered`.
25. `TestApplyRegistry_MapWarningsAreSortedByKey` — eight `deliverable_kinds` holes `k1..k8` and eight bad conditional rules, declared out of order → sixteen warnings in key order (P(pass without sort) ≈ 1/8! per map) — kills `sort removed` in `applyDeliverableKinds` (D5).
26. `TestApplyRegistry_FourStepsAreIndependent` — `applyRegistryDials` distinguishes `MaxOptionalInsertions` nil vs 0 (the `*int` at :688-690); `applyDeliverableKinds`/`applyConditional` on empty maps are no-ops; `applyPhases` sets `Triggers` only when `Routing != nil` (:721-723) and skips the empty name — kills `*int guard dropped`.
27. `TestGateStageAndRouterStage_LaddersTrimAndReject` — `GateStage("advisory") == (Off, false)`, `RouterStage("advisory") == (Advisory, true)`; both accept `"0"`/`"off"`/`" shadow "`/`"enforce"` and reject `""`/`"enfroce"`/`"on"` — kills `GateStage accepts advisory`, `RouterStage drops advisory`.
28. `TestApplyPolicyStages_ProjectsEveryDialAndWarnsOnTypos` — the INPUT cfg is `sentinelStages()` (every gate/recovery dial `StageAdvisory`, `RouterReplan` Off, `ParallelEvaluate` Enforce, ints −1, bools false — each differs from what the fixture resolves to, critic fold 3), `PolicyStages{enforce, "enfroce", shadow, off, enforce, shadow, enforce, advisory, shadow, 5, true, true, 2}` (positional) → every cfg field as listed, `EvalGate == Off`, ONE `CONFIG_UNKNOWN_VALUE {key gates.eval_gate, value enfroce, default off, step policy, source policy}` with the byte-identical `parseEvidenceStage` message and Origin `Loader.ApplyPolicyStages` on the event, and the INPUT cfg unchanged — kills each of the thirteen assignments dropped (thirteen one-line mutants), `source=env`.
29. `TestApplyPolicyStages_GateLadderRejectsAdvisoryRouterLadderAcceptsIt` — `gates.contract_gate: advisory` → Off + the one warning; `router.router_replan: advisory` → Advisory, no warning — kills `GateStage accepts advisory` at the seam.
30. `TestMustCondRule_PanicsWithTheDocumentedPrefix` (covers :124) — `mustCondRule("garbage")` panics with the `config: DefaultTddRuleExpr does not parse:` prefix; `DefaultTddRule() == mustCondRule(DefaultTddRuleExpr)` — kills `panic text changed`.
31. `TestRegistryPath_IsTheDocsArchitectureSpelling` — `RegistryPath("/r") == filepath.Join("/r", "docs", "architecture", "phase-registry.json")` — kills a segment typo.
32. `TestParseSandboxMode_TrimsAndWarnsNamingTheCurrentDefault` — `" on "` → `on`; `"bogus"` with current `auto` → `auto` and the :754 message `EVOLVE_SANDBOX="bogus" unknown (want auto|on|off), defaulting to "auto"` with `default=auto`, `key=EVOLVE_SANDBOX` — kills `TrimSpace dropped`.
33. `TestLoadDomain_ReadsUnderTheEvolveDir` — a domain.json under `<root>/.evolve` is read; `DefaultDeliverableKind` for `writing` → document; absence → `ok false, err nil` — kills `join drift`.
34. `TestImportGraph_LeafImportsOnlyTheDeclaredPackages` — allowlist {`internal/signalcenter`, `internal/paths`} — kills `import internal/policy` (also a compile cycle), any core import.
35. `TestNoStderrOrWritesInTheLeaf` + `TestNoBareWarningLiteralOutsideTheAppender` + `TestWarning_FieldsMarshalBesideCodeAndMessage` — grep `os.Stderr`/`fmt.Fprint`/`os.WriteFile`/`os.Create`/`os.MkdirAll` over non-test files == 0; `Warning{` appears in non-test sources exactly once, in signals.go (`warn`); a Warning's JSON carries `Fields` (why the goldens are projections).
36. (`config_test`, importing policy — the spine_contract_test.go idiom) `TestDefaults_GateDialsMatchPolicyCompiledDefaults` — for the seven gate/recovery dials `Load(absent, nil).<field> == GateStage(policy.Policy{}.GatesConfig().<x>)` etc., `RouterReplan`/`ParallelEvaluate` via `RouterStage`, `ParallelEvaluateConcurrency == 3 == pe.Concurrency`, `RePlanMaxDepth == router.ReplanDepth` — the consumer pin on the two-source belief acs/cycle34:21-23 records — kills a one-sided default change in either package.
37. (signalcenter) `TestKind_ClosedSetAndTerminal` — `all` gains `KindConfigWarning`, `Terminal()` false; `TestModule_ClosedSet` UNCHANGED (26) — red until the const + `knownKinds` existed — kills `KindConfigWarning in terminalKinds`.
38. (cmd/evolve, cmd_cycle_config_test.go) `TestRoutingConfigLoader_OneConstructionSite` — non-test files matching `\bconfig\.New\(` == {`cmd/evolve/cmd_cycle_config.go`} (the walker skips `internal/config/` and `testdata`) — kills `a second config.New( in cmd_cycle.go`.
39. (cmd/evolve) `TestWireOrchestratorDeps_ConfigWarningRendersAndIsDurable` + `TestWireSimulateOrchestrator_CenterTopologyRendersConfigWarnings` (renamed at the Go-review fold: the old name `…_ConfigWarningRenders` overclaimed — `wireSimulateOrchestrator` resolves no RoutingConfig and never loads the registry, so no CONFIG_* warning originates from `--simulate` today; the twin now FIRST asserts the freshly wired simulate console carries no `[config]` line with a read-fault registry in place, then proves the Center TOPOLOGY renders a Loader's warning) — a `{` file at `config.RegistryPath(root)`; `captureConsole(func(c io.Writer) { wireOrchestratorDeps(root, evolveDir, c) })`: the console contains `[config] config.warning WARN CONFIG_REGISTRY_MALFORMED`, `origin=Loader.Load`, `step=registry`, `phase registry malformed` and NOT `[config] WARN registry-malformed:` nor `cycle=`; `<evolveDir>/signals.ndjson` contains `"code":"CONFIG_REGISTRY_MALFORMED"` (the malformed file also trips `phasespec.Load` → the `[phases] WARN` at :533 on real stderr, non-fatal, tolerated); the simulate twin (the :314-332 shape) constructs `config.New(config.WithSignals(func() *signalcenter.Center { return d.Signals }))` with a DIRECTORY at the registry path → `CONFIG_REGISTRY_UNREADABLE` rendered and durable — kills `root still on the silent facade`, `WithSignals not passed`, `the simulate root loads the registry` (M61).
40. (cmd/evolve) `TestPolicyStagesOf_ProjectsThePolicyAccessors` — a `policy.Policy` with every gate/recovery/router/parallel_evaluate value set → `PolicyStages` (KEYED literal — vet's composites check, critic fold 5) equals the accessors' values field by field — kills any dropped or crossed field (`TopNGate ← ReviewGate`).
41. (cmd/evolve) `TestCenterlessConfigLoadSitesArePinned` — non-test files matching `\bconfig\.Load\(` == exactly {`internal/router/policy.go`, `internal/cli/phasecmd/phase_verify.go`, `cmd/evolve/cmd_solution.go`, `internal/kerneltest/fixture.go`}; cmd_cycle.go must NOT match; `phaseconfig.Load(` at cmd_campaign.go:394 is excluded by the boundary — kills `root reverted to config.Load(`, `a fifth silent site`.
42. (cmd/evolve) `TestCmdCycle_NoHandWrittenConfigWarnLineAndLaddersLiveInTheSeam` — `"[config] WARN` absent from cmd/evolve non-test sources; `func parseGateStage`/`func parseRouterStage` defined only in cmd_cycle_config.go and their bodies contain `config.GateStage(`/`config.RouterStage(` — kills `the old print loop kept beside the emit` (double render), `a ladder copy re-inlined`.
43. (cmd/evolve) `TestSolutionCmd_MalformedRegistryPrintsTheRegistryWarning` — `evolve solution check <slug> --project-root <root>` with a `{` registry: stderr contains `evolve solution: registry warning [registry-malformed]: phase registry malformed` and `declares no config.deliverable_kinds.document`, exit 2 (D2b pinned).
44. (cmd/evolve, existing) `TestSignalsCodes_RepoDocIsInSyncWithTheLinkedRegistry` (cmd_signals_test.go:17) — RED after the six `RegisterCode` calls, GREEN after `evolve signals codes generate` (92 → 98, `### config` present) — kills a forgotten regen.
45. (cmd/evolve, cmd_cycle_config_test.go — architecture-review fold, MEDIUM) `TestRegistryPathSpelling_HasOneNonTestHome` — non-test files outside `internal/config/` matching `"docs",\s*"architecture",\s*"phase-registry\.json"` == none. RED first: listed the six re-inlined joins (cmd_cycle_outputs.go, phase_order.go, phases_create.go, kerneltest/fixture.go, phaseinventory.go, skillcheck.go); GREEN after the six re-points — kills any re-inlined join (the red run is the evidence).
46. (`config_test`, defaults_parity_test.go — architecture-review fold, HIGH) `TestPolicyStages_KeysMatchPolicyJSONTags` — for each of the nine stage dials, a `PolicyStages` with every word `off` and the one under test `typo` through `New().ApplyPolicyStages` → exactly one warning whose `fields.key` and Message prefix equal `<json tag of the policy.Policy section field>.<json tag of the block field>` read by reflection (`gates`+`topn_gate`, `recovery`+`spine_floor`, `router`+`router_replan`, `parallel_evaluate`+`stage`, …) — kills M59 `gates.topn_gate → gates.top_n_gate`, M60 `recovery.spine_floor → gates.spine_floor` (a section cross), M60b `parallel_evaluate.stage → parallel_evaluate.Stage` (the Go field name).

Kept with zero edits under their names: all 58 existing `config` tests (the 18 code-string pins read `w.Code` unchanged; `hasWarning` config_test.go:22; the 3-arg `parseStage` calls; `defaults()`; `validateSpine(cfg, &ws)`; `parseCondRule`; `StaticSpinePhasesForTesting`), `TestParseGateStage` (cmd_cycle_test.go:22-34, through the forwarder), the `TestWireOrchestratorDeps_*` fixtures (absence is silent — I4), the cross-package `config.Load` callers, and the ACS predicates cycle34 (C34_NEG1 :213, C34_004 :117-129 — absence of gate env names in config.go), cycle37 (:155-175), cycle39 (:162-215), cycle413 (:127), cycle1141 (:209-217, :240-252), cycle1168 (:195 runs `./internal/config/`), cycle1633 (:278), and `acs/regression/flagreaders` `TestEveryProductionReaderHasRegistryRow` (:121 — the nine env names keep their flagregistry rows; the leaf reads no new env; `testdata/` is skipped by the scan). Declared test edit: none (test 7 was written in its final shape — see item 7).

Coverage bar: 100 % statements for `./internal/config` (`.cover-strict` row; baseline 95.7 %, the eight blocks covered by tests 2, 3, 4, 5, 20, 11, 30 and the test-2 mandatory guard); every export named in package-local tests (apicover, Makefile:143-147 counts package-local `_test.go` files — the external `config_test` files in the same directory count): `Loader`, `Option`, `New`, `WithReadFile`, `WithSignals`, `SignalsWired`, `Load` (method + func), `ApplyPolicyStages`, `PolicyStages`, `RegistryPath`, `GateStage`, `RouterStage`, `Warning.Fields`, the six `Code*` — plus every pre-existing export apicover_named_test.go:20-88 already binds (60/60 at landing).

## 7. Why it is better than the original

| Dimension | Before | After |
|---|---|---|
| Observability | one hand-written `[config] WARN <code>: <msg>` line on raw stderr at ONE root (invisible to `captureConsole`, absent from signals.ndjson), a second spelling at `evolve solution`, three sites dropping the slice; a corrupt phase-registry.json and a typo'd policy gate dial each resolved SILENTLY to a weaker configuration | six `CONFIG_*` WARN codes under module `config`, `origin` naming the method, `fields.step` naming the file, `key`/`source`/`path` naming the dial or file: "why is triage missing / why did the eval gate not fire" is `grep '"module":"config"' .evolve/signals.ndjson` → the code names the class → `step`+`key` name the function and the dial — three lines into a < 230-line file; the happy-path stream is unchanged (zero events on the live registry) |
| Isolation | one 962-line file; the reader was `os.ReadFile` inline (:656) so the malformed/unreadable branches were untestable except by chmod; the three giants mixed nine concerns; the stage ladders existed twice (config vs the root) | a `Loader` with two injected collaborators (reader, Center accessor); twelve files each under 230 lines, every function under 50, guarded by tests; the ladders have ONE spelling (`GateStage`/`RouterStage`) the root forwards to; the pure rules (the parsers, validators, the registry steps) are the mutant seam |
| Changeability | the code vocabulary lived in a stale comment (two phantoms, one omission); the registry path was spelled at four loader call sites; the `.evolve` join twice; the gate defaults replicated across config and policy with only prose (acs/cycle34:21-23) recording it; `model_routing`'s provenance comment pointed at the wrong file | each belief has ONE home with a consumer pin: `signalCodes` (test 24), `RegistryPath` (test 31 + four sites), `paths.EvolveDirOf` (test 33), the gate-default parity pin (test 36); the provenance and immutability comments corrected (D6) |
| Measurement | 95.7 % with the failure branches uncovered; no `.cover-strict` row; apicover in a 2026-06 tier | `.cover-strict` 100; apicover 60/60 exports; per-boot counts by `CONFIG_*` code — the first quantitative view of how often the registry, an env dial or a policy dial is mis-set |
| Callers | 58 importers, 6 `config.Load(`-shaped sites (one a false positive), 21 ACS files, the busiest root in the tree | zero production spelling changes outside the root's own lines (:406-410, :442-455, :955-977) and three `RegistryPath` re-points; every unexported test spelling preserved; `TestParseGateStage` untouched |

## 8. Risks and byte-identity guarantees

Byte identity (each with its pin):

- B1 The resolved `RoutingConfig` VALUE for the frozen registry copy — golden `testdata/real_registry_config.golden.json` (json.MarshalIndent, sorted keys) captured on 8e8f080f before any move, replayed through `Load` and `New().Load` (tests 8, 23). B2 The compiled-defaults VALUE — `testdata/defaults.golden.json` (test 9). B3 Every `Warning.Code` and `Warning.Message` for the twelve-warning kitchen sink — `testdata/kitchen_sink_warnings.golden.json` (test 10; the fixture registry lives in testdata/, env is an in-test map, so nothing is path-templated). B4 On disk: the package writes nothing before and after (test 35). B5 The four facade sites return byte-equal values (B1 through the facade, test 23) and cmd_solution.go:49's stderr bytes for the four legacy codes are unchanged (it prints `w.Code`/`w.Message`). B6 The registry path bytes at the ten re-pointed sites — the four at the build and the six at the review fold (test 31 for the spelling, test 45 for the one-home property; the kerneltest join Cleans to the same bytes, checked by a throwaway `filepath.Join` comparison). B7 `TestParseGateStage`'s mapping and the silent `StageOff` at cmd_cycle.go:700 / cmd_loop_preflight.go:32 (the forwarders; test 14 adds the router twin). B8 acs/cycle1141's by-path predicate stays green — config.go keeps `//` lines with `phasecontract` + `deliberately` on `defaultMandatory()`'s doc comment (and `derived := strings.Contains(text, "phasecontract")` :248 alone satisfies it). B9 The 26-module literal and `TestModule_ClosedSet` untouched. B10 The signal stream on a clean load of the live registry: ZERO `config` events (test 22's fixture is where the twelve fire; config_realregistry_test.go:74). B11 `staticSpinePhases` == core's state machine (spine_contract_test.go:20-40, untouched).

Declared deviations (each with its pin): D1 cmd_cycle.go:409 `[config] WARN <code>: <msg>\n` on `os.Stderr` → the `StderrSink` line `[config] config.warning WARN CONFIG_… seq=s origin=Loader.Load — <msg> …fields` on the root's console writer (production console == stderr, cmd_cycle.go:226); no test pinned the old line (pins: tests 39, 42). D2 A malformed or unreadable (non-ENOENT) registry: silent → `CONFIG_REGISTRY_MALFORMED` / `CONFIG_REGISTRY_UNREADABLE` WARN, the fallback VALUE unchanged (tests 19, 20). D2b `evolve solution` (Center-less) now prints `registry warning [registry-malformed]: …` where it printed nothing (test 43). D3 `EVOLVE_CONDITIONAL_MANDATORY` without ':' — silently ignored → `CONFIG_UNKNOWN_VALUE`, the value still ignored (test 21; the protected flagregistry prose registry_table.go:15 says only `phase:expr predicate`, so no drift). D4 A policy stage word outside its ladder (`gates.*`, `recovery.*`, `router.router_replan`): silent `StageOff` → `CONFIG_UNKNOWN_VALUE` + `StageOff` (tests 28, 29); an EMPTY policy word also warns (it is outside the ladder; every policy accessor fills a default, so production never passes one); `parallel_evaluate.stage` typos still never reach it (merge_catalog.go:33-38 sanitises first — Q7). D5 Warning ORDER for ≥ 2 deliverable_kinds holes / ≥ 2 bad conditional rules: random (map iteration :694, :706) → sorted by key (test 25; no golden could pin the old order). D6 Comments: the :484 code list (`unknown-key`/`deprecated-flag` deleted; `spine-order`, `registry-unreadable`, `registry-malformed` added), the :3-4/:382-383 "immutable, loaded once" prose (now names the two Loader steps), the :388-389/:734-736 `model_routing` provenance (→ phase-registry.json), the :6 "imports only stdlib" (→ stdlib + signalcenter + paths, allowlist-tested), the root's :397-398 comment naming `config.Load`. D7 Ten registry-path spellings → `config.RegistryPath` (four at the build, six at the architecture-review fold; B6). D8 The signal stream gains module `config` events on faults only (a pure addition; the stream golden is NEW, captured after the move — test 22). D9 The `fields.key` on an env-sourced `unknown-value` names the env var while the Message keeps the registry key name (:729 passes `"dynamic_routing"` as varName) — the Message is byte-identical, the field is the correction (test 21). Nothing else moves a byte.

Preserved quirks, deliberately NOT fixed (each a behaviour change with its own commit): Q1 `EVOLVE_USE_PHASE_REGISTRY` disables only on the literal `"0"` (:523); `false`/`off` keep the registry on silently (test 13; flagregistry row :34 documents only `0`). Q2 Unknown registry keys are ignored (no `DisallowUnknownFields`; the live file carries four `_*_note` keys) — F5 is an allow-list warning, not a strict decoder. Q3 `Stage.String()` returns `"0"` for Off (:40) while the parsers accept `"off"` — `fields.stage` carries `"0"`. Q4 `parseMode`/`parseEnable` hard-code the key in the Message (:872, :904) so env-vs-registry origin rides only in `fields.key`/`source`. Q5 `router.PolicyForProject` applies Mandatory + AuditFail but no gate dials (router/policy.go:94-97) — a replicated belief with its own rationale, unit 05's. Q6 cmd_cycle.go:700 (`ReportSizeGate`) and cmd_loop_preflight.go:32 (`NestedFallbackStage`) stay on the silent forwarders — neither is a `RoutingConfig` field (F2). Q7 `parallel_evaluate.stage` is sanitised by policy before it reaches the loader, so its typo never warns from the policy path (test 28 covers the ladder). Q8 `[policy] WARN` (cmd_cycle.go:424) resets every gate dial to policy's compiled defaults with no code (F1). Q9 The two-source gate defaults (config.go:613 vs the policy accessors), policy applied unconditionally — now PINNED by test 36, unification F6. Q10 The compiled baseline omits triage (:615-619) — the reason the registry codes matter; unchanged. Q11 The malformed-registry DEGRADE (never a halt) — operator question 1.

Risks:

- **The ACS by-path pin** (acs/cycle1141/predicates_test.go:240-252): `defaults()` and the divergence prose stay in config.go on `defaultMandatory()`'s doc comment; a later "tidy" that moves the defaults family to its own file reds the green floor — the doc comment on `defaultMandatory` says so in place.
- **The limits guard counts comments inside a function** (limits_test.go): the 45 lines of prose inside the :589-653 literal became doc comments (excluded) — moving them was byte-neutral on every artifact.
- **Per-phase duplication**: if a future change gives `router.PolicyForProject` a Center-bearing Loader, every registry warning fires once per buildplanner/triage/swarmplan/tdd dispatch per cycle; the facade is silent by construction and test 41 names the four sites; the fix is a cached resolved config, never an emitting per-phase Load.
- **Two-source gate defaults stay two-source** (Q9): a reader of `defaultRollout()` can still believe config's value is live for a cycle; the doc comment names the override and test 36 pins the parity.
- **The stream golden is NEW** (D8): captured after step 2 went green, then frozen — there is no pre-move stream for module `config` (zero producers before), so D8 is a contract creation, not a replay.
- **`TestSignalsCodes_RepoDocIsInSyncWithTheLinkedRegistry`** reds cmd/evolve between registering the codes and regenerating signal-codes.md; regen from the DEV worktree (`make -C go build && ./go/bin/evolve signals codes generate` from the project root), never the runtime plane, never the tracked `go/evolve` (a bare `go build ./cmd/evolve/` from `go/` overwrites that tracked binary — restore it with `git show HEAD:go/evolve > go/evolve`).
- **gofmt parity** on `defaultRollout()`'s literal: one field per line with NO trailing comments; on `envDials`: one named-function arm per element so no element spans lines (a long inline closure made the two gofmt versions' alignment diverge); on `PolicyStages`: group doc comments above the fields instead of trailing comments. Both gofmt binaries agree at landing.
- **100 % lines needs BOTH reader branches**: the default `os.ReadFile` path (test 16 through a temp file, test 9 through an absent path) and the injected faults (tests 18-20); the mandatory guard in `validateInertEnables` needs a force-enabled MANDATORY non-spine phase (test 2's `custom-mandatory`).
- **D4 is a stderr/stream ADDITION** for operators whose `.evolve/policy.json` has silently carried a typo'd dial — desirable, but the CHANGELOG entry says so (operator question 8).
- **The seam edits touch cmd_cycle.go**, the busiest root (also `evolve loop` via `wireOrchestratorDepsFn`, cmd_loop.go); the diff there is −47 +14 lines (numstat), and every existing `TestWireOrchestratorDeps_*` fixture now runs Load through the Loader — green because absence is silent (I4).
- **`\b` on every source-scan needle**: `phaseconfig.Load(` (cmd_campaign.go:394) and `phaseconfig.New(` (none today) would otherwise false-positive tests 38 and 41; the seam tests use a regexp walker for that reason.
- **Protected surface — an ongoing cost** (critic fold 7): neither cmd_cycle.go nor `internal/config/` was on `ProtectedSurfaceManifest` before (integrity_surface.go:53-126); after landing both the leaf dir and the seam file are, so the unit ships only via `evolve ship --class manual` from an operator session outside any cycle, like 01-03b — AND every future cycle edit of the package goes console-manual: 3 of the 8 commits touching go/internal/config since 2026-07-01 were autonomous `evolve-cycle:` landings, and cycles add `RoutingConfig` dials (cycle413 CompactPrompts, cycle436 ModelRouting). The trade is deliberate (the loader every routing consumer is injected from must not be edited by the loop it configures) and is operator question 9. `.evolve/policy.json` (:67) and flagregistry (:57-58) are protected and untouched.
- **`sort` is still imported** (validate.go); registry.go's key sorts use `slices.Sorted(maps.Keys(…))` (Go 1.23 stdlib); `errors`/`io/fs` moved from domain.go's import set into registry.go's (both stdlib, both still in domain.go).
- **acs/cycle1638 `TestC1638_010_ExplanationNamesEveryLoadBearingFileTheBuildChanged`** is red in this worktree INDEPENDENT of the unit: it diffs the working tree against the cycle-1647 explanation dossier's `Base SHA` (23f1073d) and lists 79 files at HEAD already (unit 03b's among them) — a pre-existing red the orchestrator's floor owns.

Follow-ups, in order: F1 a `policy` module decision — cmd_cycle.go:424 `[policy] WARN` (which resets every gate dial with no code), overlays.go:177, the `pol, _ :=` drops at cmd_cycle.go:822/:827 and cmd_loop_preflight.go:25, and the cmd_loop*.go sites (a 26 → 27 closed-set edit — operator question 3); F2 fold `ReportSizeGate` (cmd_cycle.go:700) and `NestedFallbackStage` (cmd_loop_preflight.go:32) into `PolicyStages` once they are typed fields; F3 `CONFIG_DOMAIN_UNREADABLE` via an orchestrator-held Loader (unit 05; dispatch_signals.go:86-97); F4 ONE reader of phase-registry.json — the two OTHER parsers (`phasespec.Load` at cmd_cycle.go:527 with its `[phases] WARN` :533, `phaseorder.go:28-47` which fails loudly) adopting the Loader's contract (operator question 6) — until then a malformed registry prints two console lines from two channels; the path SPELLING is a separate, now-finished follow-up (test 45 — every four-segment join outside the leaf is gone; only phasespec's own relative `registryRelPath`, mergedcatalog.go:21, remains, and it goes with the reader); F5 unknown registry keys — an allow-list warning (`unknown-key` revived with a producer) that tolerates `_*_note`; F6 unify the two-source gate defaults once the operator picks the winner (question 4); F7 `evolve solution` gets a Center or stays hand-rendered (question 7); F8 the "retire env flags" by-product (§12 row 7) as its own protected-surface slice; F9 the signal-codes generator escaping `|` inside a doc cell (the failurelearning `RECURRENCE_LEDGER_FAILED` row already carries one raw).

Mutants (each a one-line edit; the named test goes red; hand-applied per the protocol and recorded by name in §10): M1 swap :532/:533 (t1) · M2 `>=` → `>` :564 (t2) · M3 `sort.Strings` removed :572 (t2) · M4 else deleted :765-767 (t3) · M5 `parseMode` default → StaticPreset (t4) · M6 empty-name `continue` removed (t5) · M7 `Conditional` map replaced (t6) · M8 `return doc, true` on a parse error (t7 — EQUIVALENT, see item 7) · M8' the malformed-branch warn dropped (t20) · M9 `Triggers` unset (t8) · M10 `SpineOrder` dropped (t8) · M11 `ParallelEvaluateConcurrency` 3 → 2 (t9) · M12 ENOENT reported (t9, t18) · M13 an env append reordered (t10) · M14 `LoadDomain` nil on error (t11) · M15 assignment skipped on a hole (t12) · M16 `!= "0"` → `== ""` (t13) · M17 `RouterStage` drops advisory (t27; t14 after the move) · M18 `readFile` default nil (t16) · M19 `errors.Is` widened (t19) · M20 `MALFORMED` at the read branch (t19) · M21 Origin `"Load"` (t19) · M22 Cycle 1 (t19) · M23 range off-by-one in the registry stamp (t21) · M24 env `key` not overridden at the arm (t21) · M25 Cut-miss warning dropped (t21) · M26 `emit` inside the registry step (t22) · M27 a `signalCodes` entry removed (t22, t24) · M28 `sort` removed in `applyDeliverableKinds` (t25) · M29 `*int` guard dropped (t26) · M30 `GateStage` accepts advisory (t27, t29) · M31-M43 each of the thirteen `ApplyPolicyStages` assignments dropped (t28) · M44 `source=env` in `ApplyPolicyStages` (t28) · M45 cfg mutated in place (t28 — NOT APPLICABLE: `cfg` is a value parameter, the mutant cannot be written) · M46 `mustCondRule` panic text changed (t30) · M47 `RegistryPath` segment typo (t31) · M48 `TrimSpace` dropped in `parseSandboxMode` (t32) · M49 `KindConfigWarning` in `terminalKinds` (t37) · M50 a second `config.New(` in the root (t38) · M51 the old print loop kept (t42) · M52 `TopNGate ← ReviewGate` in `policyStagesOf` (t40) · M53 `step=env` stamped on the registry range (t21) · M54 the root reverted to the silent facade (t41, t39) · M55 `WithSignals` not passed at the seam (t39) · M56 the seam forwarder re-inlines the gate ladder (t42, `TestParseGateStage`) · M57 domain.json join drift (t33) · M58 a leaf import of internal/policy (t34) · M59 `gates.topn_gate` → `gates.top_n_gate` (t46) · M60 `recovery.spine_floor` → `gates.spine_floor` (t46) · M60b `parallel_evaluate.stage` → `parallel_evaluate.Stage` (t46) · M61 `wireSimulateOrchestrator` loads the registry through the seam Loader (t39 twin — applied WITH the `config` import so it compiles; the kill is the new first assertion, not a build error). Equivalents recorded, not padded: the facade built WITH a Center (t23 — unreachable: `New()` takes no Center); `readRegistry` taking `ws` by slice instead of pointer (identical output); `Fields` nil vs empty map on a clean load (no event → unobservable); M8 (value-equivalent).

## 9. Migration

Landing plan — ONE PR, ONE ship commit (a squash; the 01-03b precedent), built in three verified steps in `dev/decompose-config`, each green before the next; shipped via `evolve ship --class manual` from an operator session outside any cycle after the reviewer fleet (simplifier → architecture ∥ Go; CRITICAL/HIGH block) and the whole-module floor:

1. Step 1 (pins and goldens on 8e8f080f — nothing moves): `testdata/frozen_registry.json` copied from the live registry; a throwaway test wrote `testdata/real_registry_config.golden.json`, `testdata/defaults.golden.json`, `testdata/kitchen_sink_warnings.golden.json` (+ the hand-written `testdata/kitchen_sink_registry.json`) and was deleted; tests 1-14 written and run (green), each proven red by its named mutant once (§10); test 15 (limits) RED for the right reason. Byte-identical on every artifact and on stderr.
2. Step 2 (the leaf, the seam, the closed set, the gates, the docs — tests 16-44 red first): (a) signalcenter — `KindConfigWarning` after event.go:145, `knownKinds` :159, event_test.go `all` :69-72 (module count 26 untouched); (b) the split — `vocabulary.go`, `condrule.go` (+ `mustCondRule`), `routing_config.go` (+ the D6 comment fixes), `rollout.go`, `registry.go` (the Loader method + four steps, sorted), `env.go` (`envDials` + `parseSandboxMode`), `validate.go`, `parse.go` (+ `GateStage`/`RouterStage`), `policy_stages.go`, `signals.go`, and `config.go` rewritten around `Loader` with the defaults family and its prose on doc comments; `domain.go:30` → `paths.EvolveDirOf`; `limits_test.go` + `importgraph_test.go` copied from failurelearning with the allowlist {signalcenter, paths}; the stream golden captured once green, then frozen; (c) the seam `cmd/evolve/cmd_cycle_config.go` (`wiredRoutingConfigLoader`, `policyStagesOf`, the two forwarders) + cmd_cycle.go edits at :406-410 (+ the `strings` import pruned and the :397-398 comment corrected), :442-455, :955-977 + the three `config.RegistryPath` re-points (cmd_solution.go:43, router/policy.go:87-88, phase_verify.go:193-194); (d) gates — `.cover-strict` += `./internal/config 100` after :11; `.apicover-enforce` row :213 STAYS in its tier (no duplicate row) and a comment joins the ADR-0103 block after :587; `ProtectedSurfaceManifest` (integrity_surface.go, after :118): `{Fragment: "/go/internal/config/", …}` and `{Fragment: "/go/cmd/evolve/cmd_cycle_config.go", …}`; (e) `make -C go build && (cd .. && ./go/bin/evolve signals codes generate && ./go/bin/evolve signals codes check)` → signal-codes.md 92 → 98 with `### config`; (f) docs — this file; ADR-0103 units table row 08 with `config` removed from the `…` row; signal-center-design.md §5.1 parenthetical, §5.2 row, §12 row 7; dynamic-phase-routing.md :5, :110, :121, :166; CHANGELOG unreleased entry naming D1-D4.
3. Step 3 (verification from `go/`): `gofmt -l ./internal ./cmd` and `$(GOTOOLCHAIN=go1.23.12 go env GOROOT)/bin/gofmt -d -s .`; `go vet ./...`; `go vet -tags integration ./internal/core/ ./cmd/evolve/`; `go test -count=1 ./internal/config/ ./internal/signalcenter/ ./internal/router/ ./internal/cli/phasecmd/ ./internal/kerneltest/ ./internal/guards/ ./internal/policy/ ./internal/core/ ./cmd/evolve/`; `go test -count=1 -race ./internal/config/ ./cmd/evolve/`; `make cover-strict` (config 100); `make apicover-enforce APICOVER_PKGS=./internal/config`; the mutants by name; `go test -count=1 -tags acs ./acs/cycle34/ ./acs/cycle37/ ./acs/cycle39/ ./acs/cycle413/ ./acs/cycle1141/ ./acs/cycle1168/ ./acs/cycle1633/ ./acs/cycle1638/ ./acs/regression/flagreaders/` (the by-path and env-reader predicates; the orchestrator runs the whole ACS floor). §10 records the build-confirmed mutants by name, the coverage figures, the export count, the code count and the review folds.

Call-site changes: ZERO undeclared — the root's own lines (:406-410, :442-455, :955-977), the three `RegistryPath` re-points at the build and the six at the architecture-review fold (cmd_cycle_outputs.go:98, phaseinventory.go:121, phases_create.go:242, phase_order.go:45, skillcheck.go:399, kerneltest/fixture.go:33 — byte-identical joins, pinned by test 45) are the declared call-site edits; every other `config.` spelling in 58 importers, 151 test files and 21 ACS files is untouched; every unexported test spelling (`parseStage` 3-arg, `defaults()`, `validateSpine`, `parseCondRule`, `staticSpinePhases`) compiles unchanged. Tests moved: none. Tests edited: none (test 7 was written in its final shape). Facade removal plan: the package-level `Load` goes when unit 05 gives `router.PolicyForProject` and `phase_verify` a Loader (or a cached resolved config) — then the Center-less list shrinks to `cmd_solution.go` and `kerneltest`; the forwarders go when `ReportSizeGate`/`NestedFallbackStage` become `PolicyStages` fields (F2).

**Panel folds (before implementing).** (1) Base = "Seam and testability first" (both judges). (2) `defaults()` and its :591-633 prose stay in config.go — on doc comments, because the limits guard counts in-function comments; no pointer comment written only for a predicate. (3) `Warning{Code, Message, Fields}` + the `signalCodes` table replaces the winner's `Signal` field + `legacyCodeOf`: one belief per field, the pairing is explicit data with a consumer pin, `emit` has no miss branch because the Center's `SIGNALCENTER_MISSING_CODE` stamp (normalize.go:19; checkCode :68-71) is the fail-loud path and test 22 asserts it never fires. (4) `parseSandboxMode(v, current, ws) string` so the :754 message is built inside the helper. (5) `fields.key` for env-sourced warnings stamped at the `envDials` arm — no parser signature widens. (6) `.apicover-enforce` row :213 stays; a comment joins the ADR-0103 block. (7) Grafted tests: 9 (real-FS absent path), 17 (reader never called), 26 (four steps), 36 (gate-default parity), 43 (`evolve solution`). (8) `CONFIG_POLICY_LOAD_FAILED` not registered (both judges); `[policy] WARN` stays (F1). (9) Thirteen dials, not fourteen (cmd_cycle.go:450 is a local). (10) `\b` on every source-scan needle (cmd_campaign.go:394). (11) The `[phases] WARN` at cmd_cycle.go:533 is a sibling parser's line, kept, named under F4. (12) `ModuleConfig` already counted: the 26 literal never moves.

**Critic folds (completeness critic on the synthesis, before implementing).** (1) The G1 golden's input is a FROZEN copy of the registry under testdata/, never the live file (cycles edit it; acs/cycle1168:195 runs the package as a floor predicate); `TestLoad_RealRegistry` stays the live-file guard. (2) The kitchen sink carries a non-spine `plan-review` enabled `on`, so the inert branch fires and the count is twelve. (3) Test 28's input is a sentinel cfg (every field differs from the fixture's resolution), so all thirteen assignment drops are observable — against `defaults()` three were equivalent. (4) The warning golden is a `{Code, Message}` projection: `Fields` lands in step 2 and a map is never `omitempty`. (5) The cmd/evolve consumer pin builds `PolicyStages` KEYED (vet's composites check rejects unkeyed literals of imported structs); positional construction is the in-package tests' privilege. (6) No nil-Fields branch in `stamp`: `warn` copies every producer's fields into the Warning's own map, always non-nil. (7) The protected-surface cost for a cycle-edited package is named (§8) and is operator question 9. Plus the line-reference corrections: `SIGNALCENTER_MISSING_CODE` is normalize.go:19 and the stamping rule is `checkCode` :68-71; the code regexp is event.go:190; `nonTestSourcesMentioning` (carryover_lifecycle_test.go:117-153) is a substring scan, so the seam tests use the regexp shape of `TestNilSignalBridgeRootsAreExplicit`; test 2's EnableOn guard is :574 (:577 is the mandatory guard); `hasDivergenceComment` is acs/cycle1141:411-425.

**Builder deviations from the synthesis (declared).** The env arms are named functions (`envDynamicRouting` … `envMaxOptionalInsertions`) rather than inline closures — one table element per line is the gofmt-parity-safe shape (an inline closure over 100 columns made gofmt 1.27 re-wrap and re-align the table). `registryDoc`'s anonymous struct types became named (`registryConfig`, `registryWorkflow`, `registryPhase`) so the four steps take typed parameters; the JSON shape is identical (goldens G1/G2 replay). The registry step's Loader method is `applyRegistryFile` (the read + overlay + range stamp) beside `readRegistry` (the read alone). `slices.Sorted(maps.Keys(…))` (Go 1.23 stdlib) sorts the two registry maps instead of `sort.Strings`. `PolicyStages` documents its groups above the fields rather than in trailing comments (gofmt parity). Test 7 was written in its step-2 shape, so the design's one declared test edit never happened. `warn` copies the producer's map (always allocating) — the design's "Fields allocated when nil" branch would have been dead. M8 is value-equivalent on both trees and M45 cannot be written; the malformed branch is killed by M8' instead.

## 10. Verification (landed)

Red first on every surface: the fourteen step-1 pins (tests 1-14) were green on the unsplit code and each proven red against its named mutant before the split (M1-M7, M9-M11, M13-M16 killed; M8 value-equivalent); the limits guard (test 15) was red on the unsplit tree for the right reason (`config.go: 962 lines ≥ 800; defaults 65; applyRegistry 59; applyEnv 51`); the leaf's tests (16-36) failed to compile against the package before the Loader, the codes and the kind existed (`undefined: Option`, `undefined: Loader`, `undefined: New`, `undefined: signalcenter.KindConfigWarning`, `undefined: CodeRegistryUnreadable`); the closed-set test (37) failed on the missing kind; the seam tests (38-43) failed on the missing `policyStagesOf`; the signal-codes doc test (44) failed after the six `RegisterCode` calls until the regen.

`internal/config` 100.0 % statements (`make cover-strict`: `./internal/config: 100.0% >= 100% ok`; baseline 95.7 %) and 60/60 exports named and executed (`make apicover-enforce APICOVER_PKGS=./internal/config`: `60 exported, 60 covered, 0 uncovered, 0 false-green, 0 ignored`); the limits guard (every function < 50 lines, nesting ≤ 4, every file < 800) and the import allowlist green; `signal-codes.md` regenerated 92 → 98 codes (`### config`, six rows; `evolve signals codes check`: `98 codes in sync`). The goldens (the frozen registry's RoutingConfig, the compiled defaults, the twelve `{Code, Message}` warnings) captured on 8e8f080f before the code moved and replayed byte-for-byte through `Load` and `New().Load`; the stream golden (twelve `{config, config.warning, CONFIG_*, Loader.Load, step}` rows) captured once the leaf went green, then frozen. The production root renders `[config] config.warning WARN CONFIG_REGISTRY_MALFORMED seq=1 origin=Loader.Load — phase registry malformed (…) err=… path=… source=registry step=registry` on the console writer and files the record under `<evolveDir>/signals.ndjson` (cycle-less); a Loader handed the `--simulate` root's Center renders `CONFIG_REGISTRY_UNREADABLE` the same way — the simulate root itself never loads the registry (it resolves no RoutingConfig), which test 39's twin now pins before proving the topology.

Sixty-one build-confirmed mutants killed by name (fifty-seven at the build, four at the review folds — listed at the end of the seam group) — the pre-move pins (step 1, on 8e8f080f): M1 the validators swapped; M2 `>=` → `>` at the advisory guard; M3 `sort.Strings` removed; M4 the conditional env else-branch deleted; M5 `parseMode` defaulting to StaticPreset; M6 the empty-name `continue` removed; M7 the Conditional map replaced before the loop; M9 `Triggers` unset; M10 `SpineOrder` dropped; M11 `ParallelEvaluateConcurrency` 3 → 2; M13 an env append reordered; M14 `LoadDomain` nil on any error; M15 the deliverable-kind assignment skipped on a hole; M16 `!= "0"` → `== ""` — the leaf (step 2): M8' the malformed-branch warn dropped; M12 ENOENT reported; M17 `RouterStage` drops advisory; M18 `readFile` default nil; M19 `errors.Is` widened to any error; M20 MALFORMED at the read branch; M21 Origin `"Load"`; M22 Cycle 1; M23 the registry range stamp off by one; M24 the env key not overridden at the arm; M25 the Cut-miss warning dropped; M26 `emit` inside the registry step; M27 a `signalCodes` entry removed; M28 the sort removed in `applyDeliverableKinds`; M29 the `*int` guard dropped; M30 `GateStage` accepts advisory; M31-M43 each of the thirteen `ApplyPolicyStages` assignments dropped (ContractGate, EvalGate, TriageCapGate, TopNGate, ReviewGate, PhaseRecovery, SpineFloor, RouterReplan, ParallelEvaluate, ParallelEvaluateConcurrency, RoutingJudge, ReconDigest, RePlanMaxDepth); M44 `source=env` in `ApplyPolicyStages`; M46 the `mustCondRule` panic text changed; M47 a `RegistryPath` segment typo; M48 `TrimSpace` dropped in `parseSandboxMode`; M53 `step=env` stamped on the registry range; M57 the domain.json join drifted; M58 a leaf import of internal/policy — the closed set and the seam: M49 `KindConfigWarning` in `terminalKinds`; M50 a second `config.New(` in the root; M51 the old print loop kept beside the emit; M52 `TopNGate ← ReviewGate` in `policyStagesOf`; M54 the root reverted to the silent facade; M55 `WithSignals` not passed at the seam; M56 the seam forwarder re-inlining the gate ladder — the review folds: M59 `gates.topn_gate` → `gates.top_n_gate`, M60 `recovery.spine_floor` → `gates.spine_floor`, M60b `parallel_evaluate.stage` → `parallel_evaluate.Stage` (all three by `TestPolicyStages_KeysMatchPolicyJSONTags`); M61 `wireSimulateOrchestrator` loading the registry through the seam Loader (by the renamed simulate twin's first assertion; applied with the `config` import so the mutant compiles). Recorded, not padded: M8 (`return doc, true` on a parse error — value-equivalent: a syntax error leaves the decoded doc zero); M45 (cfg mutated in place — a value parameter, the mutant cannot be written); the facade built with a Center (`New()` takes none); `readRegistry` taking the slice by value; nil-vs-empty Fields on a clean load.

Verification run at landing (from `go/`, 2026-09-14): `gofmt -l ./internal ./cmd` clean and the Go 1.23.12 `gofmt -d -s .` empty; `go vet ./...` clean; `go vet -tags integration ./internal/core/ ./cmd/evolve/` clean; `go test -count=1` ok for `./internal/config/`, `./internal/signalcenter/`, `./internal/router/`, `./internal/cli/phasecmd/`, `./internal/kerneltest/`, `./internal/guards/`, `./internal/policy/`, `./internal/core/`, `./cmd/evolve/`; `go test -count=1 -race ./internal/config/ ./cmd/evolve/` ok; `go test -count=1 -tags acs` ok for acs/cycle34, cycle37, cycle39, cycle413, cycle1141, cycle1168, cycle1633 and acs/regression/flagreaders; acs/cycle1638 red on `TestC1638_010_ExplanationNamesEveryLoadBearingFileTheBuildChanged` independent of this unit (it diffs the tree against the cycle-1647 dossier's base SHA and already lists 79 files at HEAD).

**Review folds (2026-09-14, the reviewer fleet — architecture Warning/FIX_THEN_MERGE, Go Warning/ship-worthy; every finding folded or declined by the folder, nothing committed).** (1) Architecture HIGH — the nine policy-key spellings `ApplyPolicyStages` names had become a second home of policy.json's tags with no consumer pin: `TestPolicyStages_KeysMatchPolicyJSONTags` (test 46, `config_test`, beside the gate-defaults parity pin) reflects the `json` tags of `policy.Policy`'s four section fields and of `GatesPolicy`/`RecoveryPolicy`/`RouterPolicy`/`ParallelEvaluatePolicy`, drives each dial through `ApplyPolicyStages` with a typo and asserts `fields.key` and the message prefix equal `<section>.<field>`; green on the tree, then red against M59 (`gates.topn_gate` → `gates.top_n_gate`), M60 (`recovery.spine_floor` → `gates.spine_floor`) and M60b (`parallel_evaluate.stage` → `parallel_evaluate.Stage`), each restored. The reviewer's alternative (`PolicyStages` carrying `{Value, Key}` pairs filled in the root) was not taken: the root would still hand-spell the keys, only one package further from policy's tags. (2) Architecture MEDIUM — the registry-path spelling was single-sourced in full: the source-scan pin `TestRegistryPathSpelling_HasOneNonTestHome` (test 45) was written first and went RED naming the six re-inlined joins, then cmd_cycle_outputs.go:98, phaseinventory.go:121, phases_create.go:242, phase_order.go:45 (`path/filepath` pruned), skillcheck.go:399 and kerneltest/fixture.go:33 (the `runtime.Caller` root kept; `config.RegistryPath(filepath.Join(filepath.Dir(self), "..", "..", ".."))` Cleans to the same bytes — checked) were re-pointed and the pin went green; phaseinventory and skillcheck import the leaf (cycle-free — the leaf's closure is signalcenter, paths, log, ipcenv); F4 now says the spelling and the reader are two follow-ups, with only phasespec's relative `registryRelPath` left, going with the reader; B6/D7 count ten re-points. (3) Go MAJOR — `TestWireSimulateOrchestrator_ConfigWarningRenders` overclaimed: renamed `TestWireSimulateOrchestrator_CenterTopologyRendersConfigWarnings` with the comment stating `wireSimulateOrchestrator` never loads the registry, and sharpened with a FIRST assertion that the freshly wired simulate console carries no `[config]` line while a read-fault registry is in place — red against M61 (`wiredRoutingConfigLoader(signals).Load(config.RegistryPath(projectRoot), nil)` inserted into `wireSimulateOrchestrator` WITH the `config` import, vet-clean, so the kill is the assertion and not a build error), restored. Wiring a Loader into the simulate root was not done: `--simulate` resolves no RoutingConfig, so a registry read there would be an undeclared behaviour change. (4) Architecture LOW — the two forwarders' doc comments now name F2 as their removal condition and say the silent mapping must not be "fixed" in place. (5) Architecture LOW (two renderings of one `Warning` — cmd_solution.go:49 vs the root `StderrSink`) DECLINED as not mechanical: the second rendering exists because `evolve solution` has no Center (F7, operator question 7); giving that CLI a signal topology is a root-wiring decision, and the line is pinned by name in the Center-less list (test 41) and by test 43. Re-verified after the folds (from `go/`): `gofmt -l ./internal ./cmd` clean and the Go 1.23.12 `gofmt -d -s .` empty; `go vet ./...` and `go vet -tags integration ./internal/core/ ./cmd/evolve/` clean; `go test -count=1` ok for `./internal/config/` (96/96 PASS, 0 FAIL), `./internal/signalcenter/`, `./internal/router/`, `./internal/cli/phasecmd/`, `./internal/kerneltest/`, `./internal/guards/`, `./internal/policy/`, `./internal/phaseinventory/`, `./internal/skillcheck/`, `./cmd/evolve/`, `./internal/core/`; `go test -count=1 -race` ok for the leaf, cmd/evolve, signalcenter, router, phasecmd, guards, phaseinventory, skillcheck, kerneltest; `make cover-strict` 7/7 rows ok with `./internal/config: 100.0% >= 100% ok`; `make apicover-enforce APICOVER_PKGS=./internal/config` `60 exported, 60 covered, 0 uncovered, 0 false-green, 0 ignored`; `make build` + `evolve signals codes check` `98 codes in sync` (no code changed, no regen needed); the eight ACS predicate packages ok (acs/cycle1638 still red for its pre-existing reason). Mutants after the folds: 61 killed by name (57 + M59, M60, M60b, M61), the same equivalents recorded. Tree: 23 modified + 21 untracked, go/evolve clean, nothing committed.

`config.go`: 962 → 229 lines; the leaf 12 production files, every function < 50 lines; the seam 56 lines; cmd_cycle.go net −33 lines.
