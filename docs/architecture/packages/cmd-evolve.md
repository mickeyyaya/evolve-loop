# cmd/evolve

> This page is filled incrementally by file group as the comment-reduction workstream reaches each part of the package. It now covers groups 1–2 (`args.go` through `cmd_dashboard.go`). Operator commands, flags and exit codes: [runtime-reference.md](../../operations/runtime-reference.md). Every `EVOLVE_*` flag, `EVOLVE_CLI_HEALTH` included: [control-flags.md](../control-flags.md). The breakdown program that moves code out of this root is [ADR-0103](../adr/0103-component-breakdown-program.md), and each unit's record is in [decomposition/](../decomposition/), for example the bridge-engine roots in [10-bridgeengine.md](../decomposition/10-bridgeengine.md) and the wave engine in [internal-loopwave.md](internal-loopwave.md). This page keeps the detail those do not.

## Purpose

`cmd/evolve` is the composition root and CLI of the `evolve` binary. It is `package main`: one handler per subcommand in the `commands` table, plus the wiring that builds the orchestrator, the loop and the campaign runner from the internal packages. It owns argument parsing, output formatting and the few decisions that belong to no internal package.

The cycle's production composition root, `wireOrchestratorDeps` in `cmd_cycle.go`, lives here too, beside its `--simulate` twin. It is the only place allowed to depend on every adapter at once, so it binds the adapters to the ports `core` declares. The routing-config seam it calls is [08-config.md](../decomposition/08-config.md), and the Signal Center topology it builds is [signal-center-design.md](../signal-center-design.md) ([ADR-0101](../adr/0101-signal-center.md)).

## Design

- **Thin handlers.** Each `runX(args, stdin, stdout, stderr) int` parses its flags and delegates. Most handlers exit 10 on bad arguments; `campaign`, `audit` and `build-invocation-context` exit 2. `reorderArgs` forwards to `cmdutil.ReorderArgs`, the one definition the extracted `internal/cli/*` groups share.
- **Removed budget flags.** `loop` and `cycle run` strip `--budget-usd`, `--budget` and `--batch-cap-usd` before `flag.Parse` in every form it accepts (`-f`, `--f`, `-f=v`, `--f=v`, `-f v`), with one WARN, so an old script gets a notice instead of a "flag provided but not defined" abort. A following token is the flag's value only when it parses as a finite float: `-1` is consumed, while `--cycles` and a goal word such as `NaN` are not.
- **One pre-wave probe protocol.** `runPreWaveProbes` serves both the loop (`prepareIteration`) and the campaign runner (`BeforeWave`). It runs the CLI-health canary, then the proactive usage probe, and returns the context's error.
  - The canary gives each expired bench one `bridge.LiveSmokeTest` through `defaultLiveProbe`, whose 4-minute bound derives from the runner's context. A clean probe clears the bench. A benchable wall re-benches it with one more strike (a doubled cooldown, or the pane's own reset hint); a credential wall (`clihealth.CredentialPattern`, [ADR-0106](../adr/0106-logic-first-delivery.md) Q2) re-benches the same way and the canary prints `clihealth.OperatorAction` — the operator's login makes the next probe succeed and clear the bench. Any other failure clears it too: non-wall failure classes belong to the capability probe and the fallback chain, and a canary that kept such a bench would re-probe it every cycle. Active benches are never probed.
  - The usage probe is opt-in (`cli_health.proactive_probe`) under the `EVOLVE_CLI_HEALTH` master switch, and fails open. It benches families already at a cap before the first phase boots them, through per-family controllers isolated under `<evolve-dir>/usage-probe`.
  - `bridgeUsageProbe` is the single way to send a family's usage command and read its pane. The cap probe and the budget quota probe (`probeWaveQuota`, under `<evolve-dir>/budget-probe`) both use it.
- **Budget-aware wave sizing** (`cli_wave_budget.go`) is the host half of the two-layer sizing in [internal-loopwave.md](internal-loopwave.md). It measures quota and pace only when a `fleet.budget` block exists. Pace comes from the last `history_window` cycles. `paceBeforeNextWave` idles for the enforce-stage `PaceDelay` and an interrupt ends the idle.
- **`evolve acs run|suite`.** `run` runs `go test -json` on one package. `suite` runs the Go predicate lane ([ADR-0042](../adr/0042-egps-v11-go-native-predicates.md)) and exits 2 on any RED predicate and 1 on a hard error such as a predicate package that fails to compile. When `--root` is left at `.`, the suite root is the cycle's `active_worktree` from its `cycle-state.json`. The predicates' `EVOLVE_PROJECT_ROOT` is the parent of `--evolve-dir` when that directory holds this cycle's `cycle-state.json`, and otherwise the parent of the git common dir ([ADR-0053](../adr/0053-dual-root-source-state-resolution.md)).
- **`evolve apicover`** calls `apicover.Main`, the same entry as the standalone `cmd/apicover`, so a deployed cycle runs the API gate without building a second executable.
- **`evolve build-invocation-context`** (`runBedrock`) emits byte-identical output per role, so the prompt cache can reuse it.
- **`evolve branches audit|prune`** is the production caller of `core.PruneSupersededOrphans` and `core.CarryforwardCandidateLandable`. `audit` prints `<ref> superseded=<t|f> landable=<t|f>` for each local `cycle-*` branch. `prune` is a dry run by default (`would-prune`). With `--dry-run=false` it deletes a superseded ref only when it has no open PR, and reports `kept-open-pr` otherwise.
- **`evolve bridge`** is a thin CLI over `bridge.Engine`: `launch`, `probe`, `report`, `add-rule`, `send`, `watch`, `recipe`, `capabilities`, `introspect`, `control`, `doctor` and `billing`. It builds `bridge.NewEngine(bridge.Deps{})`, so it is a Center-less root and its bridge signals go nowhere (10-bridgeengine.md, "Where the stream is BLIND"). The `probe` JSON shape (`{os, results:[{cli,tier}]}`) is what the adapter parses. `introspect` exits 3 on catalog drift, which is non-fatal. `control` exits 3 for an unsupported family and event pair and 1 for any other failure. `watch` is read-only; `--follow` polls every 500 ms from the end of the feed until SIGINT or SIGTERM.
- **`evolve campaign study|replan|run|status`** ([ADR-0056](../adr/0056-advisor-driven-preliminary-study-cycle.md), [ADR-0059](../adr/0059-cross-session-campaign-ownership-lease.md)).
  - `run` verifies the plan, splits it into waves and runs each through `fleet.Supervisor` with `--concurrency` (default `campaign.MaxWaveWidth`) and `--cycle-timeout` (default 2 h; a cycle past it is reaped so it cannot hang the campaign). A wave's failed cycles get one batched retry, after `campaignQuotaCooldown`: the longest active quota bench, capped at `clihealth.MaxCooldown`, or zero when nothing is benched.
  - Progress is checkpointed per goal hash and bound to the plan's SHA. A rerun of an unchanged plan resumes after the last completed wave unless `--ignore-progress` is given. SIGINT or SIGTERM reaps the in-flight cycles and leaves that checkpoint intact. `status` reads the same checkpoint.
  - `study` and `replan` run the `preliminary-study` phase through a Center-less `bridge.NewDefault(projectRoot, nil)`. The phase is registered persistence-free (no `ProfilesDir`), which is why `phaseregistrar` refuses a phase with no `dispatch.cli` only when it would persist a profile.
- **`evolve carryover apply-decisions`** applies a reviewed keep/drop/cluster decisions file (`{source_count, decisions[]}`) to `state.json:carryoverTodos`. The TTL prune only removes expired entries; this command acts on a judgment. `drop` and `cluster` ids are removed, because clustered ids live on as sweep-group inbox items and would otherwise count twice. It is a dry run unless `--apply` is given. The write goes through `statemap.UpdateStateMap`: the path is symlink-resolved, the lock is taken on the resolved path, `stateRevision` is bumped and a stale write is refused. The 25-entry ceiling is reported with a WARN, never enforced.
- **`evolve compose --phases`** runs registered phases in order through the orchestrator's factories without consulting the state machine, for re-audit and mix-and-match runs. It sets `PhaseRequest.ComposePhases`, so the kernel `evolve guard phase` hook warns instead of blocking. A verdict other than PASS or SKIPPED fails the composition, and the remaining phases still run. `ship` is refused without `--ship-anyway` (exit 2); the OS-level ship gate enforces regardless, so the early refusal only gives a clearer error.
- **`evolve reachability check-pin`** is the CLI over `reachabilityprobe.BuildImportGraph` and `CheckCallSite`; see [internal-reachabilityprobe.md](internal-reachabilityprobe.md).
- **`evolve cycle run`** absolutizes `--project-root` before deriving any path, reaps dead-owner tmux sessions (not under `--simulate`), then builds its deps through `wireOrchestratorDeps`, or `wireSimulateOrchestrator` under `--simulate`. Both return `orchDeps` and build the same signal topology through `newRootSignalCenter`.
  - Every fleet lane runs `cycle run` as a subprocess, and `fleet.Result` carries neither the lane's cycle nor its workspace, so the parent wave cannot apply a lane's verdict. The lane applies it itself through `closeoutCycleOutcome`: the inbox failure walk on FAIL (from an error or from the final verdict), the planned-no-work hand-off (`cycleoutcome.ApplyNoWork`) for a lane that answered its scope without committing it, nothing otherwise. That mirrors the PASS half in `phases/ship/postship.go` and is what makes the [ADR-0072](../adr/0072-system-failure-policy-and-halt.md) retry ceiling reachable for fleet work. A closeout error is WARNed and never changes the exit code. One asymmetry is filed as `quota-pause-closeout-parity`: a resume paused by a quota wall defers the walk, while `cycle run` and the sequential loop charge it ([incident](../../incidents/2026-09-15-shipped-item-re-pinned-and-sealed-lease-blocks-refresh.md)).
  - The exit code is a lane's only channel back to its parent wave, so a halting `SystemFailure` gets its own code, and the lane writes the escalation dossier and the P0 inbox item itself through the helper the sequential path uses.
- **`evolve cycle reset`** seals through `core.SealCycle`. Liveness is decided there, from the per-run lease heartbeat plus the owner pid (`PidAlive`): a dead pid seals without `--force` even while its heartbeat is fresh, a live owner is refused with steering to `--resume` or SIGTERM (never `pkill`), and `--force` overrides a live owner with a WARN.
- **`evolve cycle timing`** renders `phase-timing.json` as a per-phase table plus the archetype roll-up (productive build, checking, planning, control), and a shadow estimate of parallelizing the independent checking phases when it saves time. With no cycle given it picks the highest-numbered cycle that has a timing log; a reset-suffixed directory parses on its leading integer. **`evolve cycle outputs`** is a thin adapter over `internal/phaseoutputs`, whose loaders the loop's post-cycle signal emitter shares, so the CLI and the signal stream cannot read a cycle differently. It resolves report names through the builtin plus user catalog, as the contract gate does.
- **The composition root `wireOrchestratorDeps`** builds in an order that matters:
  1. It pins the model-catalog dir to `evolveDir`, the `.evolve` the cycle-start refresher writes, whatever `EVOLVE_PROJECT_ROOT` says.
  2. It builds the Signal Center first and always, since the bridge takes it at construction. The ledger comes next, observed at its append chokepoint (`ledger.WithSignals`), so the bridge's stop-review callback can append `kind=stop_review` entries.
  3. It resolves routing config through the unit-08 Loader, the sole reader of routing env and registry ([08-config.md](../decomposition/08-config.md)), before the runners, so `cfg.PhaseIO` reaches the reconcile rung. `policy.json` can only add mandatory phases; it also supplies `ship_floor` and `failure_floor`. A malformed policy prints `[policy] WARN`, continues on a zero `Policy`, and fails loudly at the first dispatch, where the runner reloads it for pins.
  4. With `workflow.universal_fallback` on it sets the runner's discovery seams once, before any constructor. Discovery is a memoized `bridge.Doctor` probe under a 30-second bound: one `<family>-tmux` per installed, non-blocked family, minus `workflow.universal_fallback_exclude`. A timeout means no discovery, which fails loud, never a hang.
  5. It wraps the bridge once in `bridgechain.New`. Every consumer (phase configs, the swarm decorator, retro, debugger, spec runners, registrar, advisors) launches through that chain-walking handle. The raw adapter is only configured: `Set*` calls, `wireBridgeStages`, the catalog publisher's resolver sink, and `orchDeps.Bridge`.
  6. Every `BaseRunner` reads the contract verifier through one accessor, so the verdict engine classifies the same bytes the contract gate approves. Gate off keeps the Null-Object `deliverable.PlainVerifier`; gate on swaps in the gate's reviewer.
  7. User phases (`.evolve/phases/<name>/phase.json`) merge over the built-in catalog. Only valid specs are routed and get a spec runner, and runner registration uses the same catalog-aware validator as `phasespec.ApplyUserRouting`. `registerBuiltinSpecRunners` then gives every advisor-selectable `kind:llm` builtin without a hand-wired runner a spec runner. Control phases are skipped, and a phase with no persona file is WARNed rather than wired.
  8. The routing advisor resolves its dispatch like a phase: `claude-tmux`/`opus`, then `.evolve/profiles/router.json`, then `policy.RouterPolicy`. Plan and re-plan use `PlanModel`, propose and judge use `ProposeModel` ([ADR-0052](../adr/0052-advisor-maximization.md)). A benched family falls back to `claude-tmux`. With claude benched too it keeps the base dispatch with `ok=false`, and the advisor degrades to the static spine, since `clihealth` is the only breaker. The same advisor is the whole-cycle planner, which the orchestrator consults only at Stage ≥ Advisory in DynamicLLM mode.
  9. The failure advisor resolves its CLI from `.evolve/profiles/failure-advisor.json` with `GitRoot` pinned to the project root, because resolvellm's git fallback runs from the process cwd. The mint registrar's `RegistryPath` anchors to the project root, where the tree-diff guard reads it ([ADR-0073](../adr/0073-crosslane-mint-registry-guard-exemption.md)).
- **The deliverable reviewer chain** is one `ChainReviewers`, because `WithReviewer` takes one reviewer. Each gate is gated on its own and fails open on ambiguity, and with every gate off no reviewer is wired. The order is:
  1. The build handoff floor, when `workflow.build_floor` enforces it: `productionBuildFloorChecks` (protected surface, then the deterministic engine), chained with `SolutionFloorChecks` for a document cycle. It runs first because its rejection carries the defect list the correction ladder needs. It is the one real `go test` run per changed package, so the advisory post-build selfcheck skips its duplicate.
  2. The eval gate.
  3. The contract gate, catalog-aware so the host enforces what `evolve phase verify` derives from `phase.json`. The report-size gate rides it as its own dial.
  4. The triage capacity clamp: committed floors above ceil(1.25·K observed throughput) are rejected into the correction ladder.
  5. The `top_n` binding gate: it aborts a build whose `## Task:` is outside triage `top_n`, or a TDD that writes tests under an empty or disjoint `top_n`.

  The salvage rung gets its own breaker-neutral contract verifier with the gate's resolution, wired at every gate stage for shadow telemetry; it only executes at `EVOLVE_PHASE_RECOVERY=enforce`.
- **The `--simulate` root** runs stub runners that return PASS and write each phase's contracted report stub, with the sentinel and the explanation-docs declaration rendered by their owners. It uses the plane's real storage and ledger, the production signal topology, and no bridge. It never mutates the operator's repository: `simulateWorktrees` hands out the project root itself as every worktree and never deletes it, and `WithDossierCommit(false)` still writes the dossier but does not commit it. It proves phase sequencing and the state, cycle-state and ledger transitions, not LLM output, Builder edits or ship. `simulatePhases` covers every phase the canonical order can route to.
- **The RUNG 0 composition fast path** (`cmd_composition_wiring.go`, [ADR-0048](../adr/0048-work-conservation-fast-reland-resilient-ship.md)) binds three closures here, because `core` cannot import `adapters/ledger`, which imports `core`. `Orchestrator.CompositionFastPathWired` ANDs them. The gates re-run through the Makefile targets CI uses ([ADR-0069](../adr/0069-cycle-ci-parity-audit-gate.md)): `compile`→`build`, `test`→`test`, `acs`→`test-acs-durable`, `apicover`→`apicover-enforce`. A gate with no target stays absent and counts as not green. The snapshot takes this run's newest bound auditor entry, diffs `main...<audited HEAD>` and computes its `git patch-id`, which is offset-insensitive, so a clean rebase recomputes the same id. Every failure falls back to a full re-audit, so a bad binding can only keep the fast path dark.
- **`evolve continuation list|release`** reuses the runtime's own paths (`continuation.ListRegistryEntries`, `inboxmover.ReleaseContinuationBinding`) instead of a second copy of the release order; the operator view is in runtime-reference.md. `release` refuses, cheapest first and leaving the registry untouched: no operator authority (checked before any read), no such binding, a lane whose lease heartbeat is fresh (unless `-force`). It resolves the root from the flag or the working directory, never `$EVOLVE_PROJECT_ROOT`, which inside a lane names another tree.
- **`evolve context-fill correlate`** is the production caller of `internal/contextfillcorrelate`. `--out` writes through temp-then-rename and refuses a destination that is not a regular file, and a root with no dossier corpus exits non-zero: absent evidence must never read as a measured zero.
- **Small commands.** `consensus-dispatch` and `cycle-simulator` keep the env-var contracts of the bash scripts they replaced (`--help` is their only flag). `cycle-health <N> <workspace>` exits 0 when healthy or only warned, 1 on a fatal anomaly or an internal error, and 10 on bad arguments. `dashboard` serves the read-only board of [ADR-0095](../adr/0095-pipeline-dashboard.md) and never takes the loop's locks; `dashboardServe` is the test seam over the server.

## Invariants

- **A cancelled canary touches no bench**, before the loop and after each probe: a smoke test cut short by an interrupt reports "not a wall", which would clear a bench that is still walled. Pinned by `TestCanary_CancelledContextTouchesNoBench`.
- **Probes run on the runner's interrupt context**, and `runPreWaveProbes` returns its error, so a caller never dispatches a wave that would be cancelled at spawn.
- **One `now` per budget sizing.** The quota snapshot and the plan share it so `ObservedAt` and the reset-horizon math agree.
- **Only `cycle-state.json` proves a plane.** A bare `runs/cycle-N` directory is not proof: `evolve acs run` creates exactly that path under its own working directory, so an agent running it inside a cycle worktree would otherwise make the worktree the plane. Presence is the whole proof, so an empty `active_worktree` never demotes a real plane to the git fallback. The git common-dir walk (`mainProjectRoot`) is wrong whenever the plane is itself a linked worktree, because all linked worktrees share one common dir. Pinned by `TestSuiteProjectRoot_NestedPlaneWorktree` and `TestSuiteProjectRoot_FallbackToGitDerivation`.
- **The suite-root resolver falls back rather than guesses.** An absent, malformed, empty or non-string `active_worktree`, or another cycle's file, yields `""` and the `--root` default (`TestACSSuiteRootFallback`, `TestACSSuiteRootAutosolve_NullAndTypeMismatch_Amp`, `TestACSSuiteRootAutosolve_WrongCycle_Amp`).
- **Read-only branch walks cannot delete.** `audit` and a dry-run `prune` pass `alwaysOpenPR`, and `PruneSupersededOrphans` never deletes a ref with an open PR (`TestBranchesAudit_ReportsSupersededAndLandable`, `TestBranchesPruneDryRunDefault_KeepsSuperseded`). `remoteOpenPR` reports no open PR when the repo has no remote or `gh` is missing, since no remote PR can then protect the ref; a failing `gh` call is an error.
- **Campaign run and status agree on one progress file.** The goal hash (SHA-256 of the plan goal) keys both each cycle's `--goal-hash` and the checkpoint. `campaignEvolveDir` always absolutizes its root, because `status` passes the raw flag and a relative root would read an empty checkpoint and never see completed waves (`TestCampaignEvolveDir_AbsolutizesRelativeRoot`).
- **`--simulate` is a plumbing check, not an owned run.** It takes no ownership lease and installs no pre-wave hook, since the probes launch real CLIs (`TestCampaignRun_SimulateSkipsOwnershipLease`). A real run releases the lease on exit (`TestCampaignRun_AcquiresAndReleasesLease`). The owner record is informational; the flock is the liveness signal.
- **A rejected decisions file leaves `state.json` byte-identical.** Validation (non-empty unique ids, the keep/drop/cluster enum, a non-blank reason on every row, a `cluster_group` on every cluster row) runs in memory before any lock or write (`TestCarryoverApplyDecisions_RejectsMissingReason` and the `_amplify_` tests). A missing state file is an error, not an empty apply. The unlocked pre-read only skips a no-op write; `UpdateStateMap` re-reads under the lock. Entries that are not objects are kept. The lock and the symlink are pinned by `TestCarryoverApplyDecisions_UsesLockedRMW` and `TestCarryoverApplyDecisions_PreservesStateSymlink`.
- **Every `bridge.NewEngine` root is on record** with its call count and Signal Center wiring (`TestBridgeEngineRootsAreEnumerated`). Calls are counted through the import alias in the AST, so a comment never counts.
- **`bridge watch` never writes** the feed or the inbox, and `--follow` starts at the end of the feed so it does not repeat what the one-shot read printed.
- **Every root carries a Signal Center, and the few exceptions are pinned.**
  - Every non-test `core.NewOrchestrator(` passes `WithSignalCenter(`, counted per file, and `internal/routingtest` is the one nil root (`TestNilSignalCenterRootsArePinned`).
  - Every `bridge.NewDefault` outside the cycle root passes an explicit `nil` (`TestNilSignalBridgeRootsAreExplicit`).
  - Every `ledger.New(` passes `ledger.WithSignals(` or is on the pinned list with its reason (`cmd_cycle_ledger_roots_test.go`), and every observer adapter gets its `.Signals` accessor (`TestObserverAdapterConstructionsAreWired`).
  - The console's WARN threshold lives only in `signalcenter.ConsoleSink` (`TestConsoleSinkThresholdHasOneHome`).
  - The cycle, loop and chain roots defer `Flush`, because an `Emit` that finds a drain in progress returns before delivery (`TestSignalCenterFlush_IsWiredAtBothRoots`).
  - The `--simulate` root builds the same topology (`TestWireSimulateOrchestrator_SignalCenterWired` and the per-unit `…WarningRenders` tests).
- **One contract verifier.** The verdict engine and the deliverables gate share one verifier, so a phase is classified on the bytes the gate approves (`TestWireOrchestratorDeps_ContractVerifierReachesEveryPhaseRunner`). The declared-deliverables gate rides the contract gate, which is on by default (`cmd_cycle_deliverables_gate_test.go`).
- **The raw bridge is never launched.** `bridgechain.New` appears once in `wireOrchestratorDeps`, and every other line that names `br` must match the allow-list (`TestWireOrchestratorDeps_EveryConsumerGetsTheChainWalkingBridge`).
- **Routing and dispatch validate user phases the same way.** Runner registration and `ApplyUserRouting` both call `ValidateUserSpecWithCatalog`; the bare `ValidateUserSpec` rejects optional built-ins such as `memo`, so the router would plan a phase that never dispatches (`TestWireOrchestrator_MemoRunnerRegistered`, against the real registry and overlay).
- **A mid-cycle mint reaches the bridge's contract resolver.** `catalog.Get` is bound to the pre-mint catalog value and `Catalog.Merge` returns a new map, so `catalogPublisher` installs a fresh resolver on every publish, a second mint in the same cycle included (`TestWireOrchestrator_CatalogPublisherWired`, `TestCatalogPublisher_RebindsResolverOnEachPublish`).
- **Only a reusable audit is carried forward.** `latestAuditEntry` is run-scoped, because the ledger is host-global across fleet worktrees and records FAIL audits too (`TestLatestAuditEntry_*`). `requireReusableAudit` reads the verdict from the bound artifact, since the ledger's `exit_code` is 1 for WARN and FAIL alike. It accepts only the audit phase's own sentinel, because the parser is tail-anchored and a quoted build-report sentinel would otherwise count. There is no fallback to an older PASS, and it runs before any git work (`TestRequireReusableAudit_*`, `TestReadCompositionSnapshot_RefusesFailBeforeAnyGitWork`).
- **The composed `apicover` gate enforces.** Its Makefile recipe must pass `-enforce` and read `go/.apicover-enforce`, pinned on the recipe text so the test never mutates the tree (`TestComposedApicoverGate_TargetRecipeEnforces`).
- **`--simulate` never touches the operator's git**: no commit, no `cycle-*` branch, no worktree, and neither the host's `gofmt -w` nor leak recovery touches tracked files (`TestWireSimulateOrchestrator_NeverMutatesGit`). A `--simulate` campaign runs no live probes (`TestCampaignBeforeWave_SimulateRunsNoLiveProbes`).
- **`cycle reset` compares absolute paths and fences on the lease, not the lock.** A relative root is absolutized before SealCycle's containment check (`TestRunCycleReset_RelativeProjectRootAbsolutized`). A held `.evolve/.lock` with no fresh lease does not block a seal, because the dispatcher's lock is released between cycles (`TestRunCycleReset_HeldLockAloneDoesNotBlock`). A live owner is refused with actionable steering (`cmd_cycle_reset_test.go`).
- **A stale lease never bricks a scope.** `continuationLaneIsLive` judges by heartbeat freshness alone, and an unreadable lease WARNs but does not block, so a dead lane's leftover `.lease` cannot lock its scope forever.
- **The unit-08 seam stays single.** `cmd_cycle_config.go` holds the one Loader construction, the one policy-stages projection, the one bridge-stage forwarding (each dial on its own setter: `TestWireBridgeStages_*`) and the one build-floor composition, which is also the only reference to `core.DefaultBuildFloorChecks` (`cmd_selfcheck_test.go`). `parseGateStage` and `parseRouterStage` stay silent (`TestParseGateStage`, `TestParseRouterStage_LadderAndUnknown`) until their two readers' dials become `PolicyStages` fields (F2 in 08-config.md); a warning there would print outside the Center. The file is on the protected-surface manifest (`guards/integrity_surface.go`).

### Readers outside the files

- `go/acs/cycle1189`, `cycle1191` and `cycle1194` parse `TestRunBridgeWatchFollow_TailsNewLines` and `TestRunBridgeWatchFollow_SkipsMalformedAndEmptyLines`. They require a `context.WithTimeout` of at least 10 seconds, no `time.Sleep(… time.Millisecond)`, and a sleep only inside a poll loop.
- `go/acs/cycle1498` runs `TestCarryoverApplyDecisions_DropsConsumedFleetAlias` by name from `cmd_carryover_test.go`.
- `go/acs/cycle10` (`TestC10_005`) fails if `cmd_compose.go` contains the retired `EVOLVE_COMPOSE_PHASES` name anywhere, comments included.
- `cmd_cycle_signal_center_test.go` (`TestNilSignalBridgeRootsAreExplicit`) requires the explicit `nil` in `cmd_campaign.go`'s `bridge.NewDefault` call.
- `internal/phaseregistrar` cites `cmd/evolve/cmd_campaign.go:403` in two comments; that line number is stale.
- `cmd_cycle.go` is read as text by many tests, and several of them count matches over the whole file, comments included:
  - Required spellings: `swarmrunner.Config` (`acs/cycle33`), `topngate.NewReviewer(cfg.TopNGate)` (`acs/cycle1620`), `Signals.Flush()`, `signalcenter.ConsoleSink(`, and exactly one `audit.NewDefaultWithStageCompactSpec(` followed within 240 bytes by `audit.WithSignals(`. The last line holding `core.PhaseAudit:` must also hold `audit.WithSignals(` (`cmd_cycle_signal_center_test.go`).
  - The `core.NewPhaseAdvisor(` call must carry `core.WithAdvisorSignals(signals)` (`cmd_cycle_advisor_signals_test.go`). `acs/cycle1494` parses the AST and needs `core.WithKB`'s argument to be a constructor whose recall argument is a call.
  - Counts that include comments: `WithSignalCenter(` against `core.NewOrchestrator(`, `.Signals = ` against `observer.NewCoreAdapter(`, three six-argument `applyCycleFailureOutcome(` call lines across `cmd_cycle.go` and `cmd_loop_sequential_outcome.go`, and `signalcenter.New(` in the two process roots only (`cmd_inbox_signals_test.go`). A comment that spells one of these calls changes the count.
  - `TestWireOrchestratorDeps_EveryConsumerGetsTheChainWalkingBridge` scans `wireOrchestratorDeps` for one `bridgechain.New(` and checks every line naming `br` against its allow-list. It skips whole-line comments but not trailing ones.
  - `acs/cycle10`, `11`, `15`, `17`, `37` and `38` fail if the file spells a retired env name (`EVOLVE_POLICY_BYPASS`, `EVOLVE_OBSERVER_AUTOSPAWN`, `EVOLVE_ROUTER_CLI`, …), comments included. `acs/cycle38` also forbids `t.Setenv("EVOLVE_ROUTER_CLI"` and `_MODEL` in `cmd_cycle_test.go`.
- Module-wide source scans that also read comments: `StderrSink(` may appear in no non-test file but `internal/signalcenter/sinks.go`. `config.New(`, `config.Load(`, the `"docs", "architecture", "phase-registry.json"` join, the three bridge stage setters and `wireBridgeStages(` each have pinned homes (`cmd_cycle_config_test.go`). `cmd_cycle_config.go` must spell `func parseGateStage(`, `config.GateStage(`, `func parseRouterStage(` and `config.RouterStage(`.
- `acs/cycle1684` requires `continuation.RequireOperatorAuthority(` in `cmd_continuation.go`, and `cmd_inbox_signals_test.go` pins its `inboxmover.Options{` literal.
- The raw string `fixtureSrc` in `cmd_composition_apicover_gap_test.go` has lines that start with `//`. `commentaudit`'s line stats count them as comments, but they are code.
- Several ACS predicates and tests cite `cmd_cycle.go` by line number (`:399`, `:406`, `:482`, `:620`, `:700`, `:711`); those numbers were already stale before this batch.

## Findings

- **cycle 230**: `evolve acs suite` trusted the caller's working directory as the suite root, and the resulting topology nondeterminism false-FAILed cycles 226 and 227. The suite root now comes from the kernel-owned cycle state.
- **cycle 1434** (#449, an [ADR-0072](../adr/0072-system-failure-policy-and-halt.md) halt): the plane was itself a linked worktree, so the git common-dir walk skipped it and landed on the console checkout, whose `.evolve` had none of the cycle's run state. Three predicates went red against that root while the audit's own run was 8/8 green, and the CLI-written verdict won. The review then rejected a bare `runs/cycle-N` directory as proof of a plane. See runtime-reference.md and [egps-v10.md](../egps-v10.md).
- **cycle 644**: a frozen structural pin that closes an import cycle can never be satisfied. The probe library landed in cycle 1226 with no production caller, so nothing ran the check until `evolve reachability check-pin` wired it.
- **cycles 962 and 969**: `core.PruneSupersededOrphans` and `core.CarryforwardCandidateLandable` shipped tested but callerless; `evolve branches` became their caller.
- **cycles 997 to 1001**: the cycle-997 sweep authored the carryover decisions, but cycle 998 found the apply had never run and `carryoverTodos` still held 135 entries. Its eval fixtures had also assumed a flat top-level list instead of `{source_count, decisions}`, which `TestCarryoverApplyDecisions_RejectsFlatArraySchema` now rejects. In cycle 999 the hand-rolled temp-and-rename write replaced a worktree's `state.json` symlink with a detached file, stranding the 135-to-14 convergence in a dead copy. The write moved to `statemap` (cycles 999 and 1001). Cycle 1498 retired a consumed fleet alias through the same command.
- **cycles 1189, 1191 and 1194**: the `bridge watch --follow` tests flaked on a loaded macOS runner. The follow loop seeds its start offset asynchronously, so an append that lands first is skipped for good, and a fixed 10 ms sleep inside a 200 ms deadline lost that race. The tests now re-append on every tick and wait for the rendered line under a 30-second context.
- **F20** (2026-09-15): the loop ignored SIGINT and SIGTERM while its pre-wave probes ran, and the first fix let an interrupted canary clear a walled bench. That review also merged the loop's and the campaign's pre-wave protocols into `runPreWaveProbes`. See [verification-wave-findings-2026-09-14.md](../../research/verification-wave-findings-2026-09-14.md).
- **ADR-0059**: a second autonomous session killed the live 47-to-0 campaign three times, which produced the goal-hash ownership lease in the git common dir.
- **cycles 119 and 120**: a relative `--project-root` made worktree-phase artifact paths diverge between the agent's cwd and the bridge's (119). `cycle reset` then refused to seal because SealCycle compared `.` against an absolute `workspace_path` (120). Both roots now absolutize first.
- **cycle 395**: `cycle reset` pre-checked the dispatcher's `.evolve/.lock`, which is held per cycle and free between cycles. A sibling reset took it in the gap and sealed a running loop, so liveness moved to the per-run lease inside SealCycle. **Cycle 554** added the owner pid: a crashed owner whose heartbeat was still fresh (the 2 to 6 minutes after a crash) had forced `--force` at every batch boundary.
- **cycle 122** ([ADR-0030](../adr/0030-phase-observer-autospawn-in-evolve-loop.md)): the Go port had silently dropped the bash dispatcher's observer auto-spawn. **Cycle 440** (MR4a) added the catalog-resolvability clamp `router.ClampPlanModelRouting`, which silently does nothing unless the root injects the model-catalog lookup. **Cycle 1064** threaded `gates.manifest_gate` into ship, which until then only a code edit could activate.
- **cycle 563**: the router planned `memo` after `ship`, but runner registration validated with the bare `ValidateUserSpec`, so no runner was registered. The dispatcher's missing-runner escape hatch WARNed and moved on without recording the phase (see [internal-router.md](internal-router.md)).
- **cycle 967** (variant A2, [ADR-0073](../adr/0073-crosslane-mint-registry-guard-exemption.md)): the mint registry path moved under the project root, where the tree-diff guard reads it.
- **cycles 786, 801 and 804**: the RUNG 0 verdict writer (786) and the core seam (801) were built and tested, but no call site bound them. Every clean fleet rebase re-audited until 804 wired `compositionOptions`.
- **cycle 1571** ([incident](../../incidents/2026-08-26-audit-binding-fail-open.md), H2 and H3): the composition snapshot's "latest auditor entry" could be a sibling lane's. Run-scoping closed that half. A FAILed audit of the same run still bound, and RUNG 0 would have run the full gate set and written a verdict record certifying a rejection, hence `requireReusableAudit`.
- **the composed `apicover` gate** (`warnship-apicover-ci-gap`, six recurrences): it named the warning-only Makefile target, which exits 0 whatever it finds. The fleet-rebase carry-forward reshipped uncovered exports and repo-wide CI went red; the gate now names `apicover-enforce`. Cycle 998's live-tree reproduction created a package in-tree and regenerated `coverage.txt`, which broke CI's cover step on both platforms (2026-07-21). It is skipped for good; a rebuild must run against a throwaway copy of the module.
- **cycles 1424 and 1429**: a phase minted mid-cycle never reached the bridge's contract resolver. Dispatch fell back to the unresolved-agent path and halted on a 600-second artifact timeout (1424). The catalog publisher closed it (1429).
- **lanes 1676 and 1677** (2026-09-14): the retro launched on the raw bridge with no fallback chain, and one codex timeout sealed the cycle. Every consumer now launches through the `bridgechain` decorator.
- **research F22, cycle 1685**: the verdict engine classified the unrepaired bytes that the gate then salvaged, which led to the shared contract verifier ([factory-pipeline-findings-2026-09.md](../../research/factory-pipeline-findings-2026-09.md)).
- **cycles 1487 and 1497**: the continuation resolver wrote to `io.Discard`, so the live-scope guard's ghost-binding refusals stayed invisible for three waves ([ADR-0089](../adr/0089-continuation-retirement-and-live-scope-guard.md)). **Cycle 1507**'s audit (H2) traced a drift to a second copy of the release order, so `evolve continuation` reuses the runtime's transaction.
- **cycle 1452**: `cycle outputs` resolved report names builtin-only and reported a false `memo-report.md` gap.
- **cycle 1652**: `cycle-health` now flags a committed dossier with an empty task set and implementation phases (the `dossier_commitment` signal). The dossier is the durable record; run dirs are gitignored and pruned.
- **2026-08-26 deep-tier review**: the failure advisor hardcoded `claude-tmux`/`opus` and never read its profile. That was harmless while `PhaseRecovery` was below enforce, but it becomes wrong the moment it flips.
- **`--simulate` against the checkout** ([incident](../../incidents/2026-09-14-simulate-runs-against-the-checkout.md)): a whole-module floor ran `acs/cycle8`'s `evolve campaign run --simulate` from the checkout. It committed dossier closeouts onto the dev branch and left worktrees behind. The architecture review of the first fix found that the host's `gofmt -w` and leak-recovery `git checkout` also ran on the in-place root. Earlier, unit 01's review (HIGH-1) had found that a Center-less simulate root silenced six recorder warnings.
