# Decision Log — Modularization, Dedup & Unified Phase I/O

> Append-only chronological audit trail for the campaign chartered by **ADR-0050**. One row per
> slice. Rows are batched into a per-phase docs slice (Phase 0 → commit `56456200`; Phase 1 → PR #112 / commit `64095c18`)
> that lands separately from the row-less code slices — the code commits carry the TDD/`-race`/`apicover` evidence,
> this log carries the reasoning. (Batching the log into one docs PR per phase avoids append-conflicts across the
> parallel per-slice code PRs.) This is the single source of truth for *what
> was decided, what was rejected, and why* — the ledger hash-chain binds the commits; this log
> binds the reasoning. Detailed contract: `docs/superpowers/specs/2026-06-15-modularization-unified-phase-io-design.md`.
> Measured inventory: `docs/architecture/audit-2026-06-15-package-map.md`.

**Conventions.** `Slice` = plan section id (e.g. `0.4`). `DoD evidence` = the per-module
Definition-of-Done artifacts (`apicover` clean, `-race` green, file<800, godoc, gofmt/vet). Each
PR references ADR-0050 and carries its RED-test-first TDD evidence.

| Date | Slice | Decision | Alternatives considered | Why | Links / DoD evidence |
|------|-------|----------|-------------------------|-----|----------------------|
| 2026-06-15 | 0.1 | Isolate all campaign work in `.worktrees/modularize-unify-phaseio` (branch `modularize-campaign`) off `0f8de059`; add `.worktrees/` to `.gitignore`. | Work directly on `main`; reuse the stale `evolve-loop.orch` worktree. | Project memory records concurrent evolve-loop batches wiping main-tree work; the stale worktree is pinned to a pre-merge SHA. A fresh ignored worktree keeps the parent tree clean and the work reversible. | Baseline `make test` GREEN, 0 failures; 813 test files / 5,292 test funcs (fast tier ≈4,654, `acs` 294, integ/e2e 344). |
| 2026-06-15 | 0.2 | Stand up the traceability system: charter **ADR-0050**, design spec, this decision log — committed as a docs-only slice (PR #1). | Inline the rationale in commit messages only; one mega-doc. | A durable, reviewable, append-only trail makes every later slice traceable to a "why" (D4); separating charter / contract / log mirrors the repo's ADR + spec convention. | ADR-0050; design spec; this log. Docs-only — no Go change. |
| 2026-06-15 | 0.3 | Produce `audit-2026-06-15-package-map.md`: package inventory + fan-in/out + >800-LOC list + dedup register + seam inventory, counts verified with `wc -l`/`go list`. Committed docs-only (PR #2). | Skip the audit and refactor opportunistically. | D1 mandates the full audit first so leaf→core ordering and every dedup slice trace to a measured number, not a guess. | audit doc; counts reproducible via the commands recorded in it. |
| 2026-06-15 | 0.4 | Build `cmd/apicover` (stdlib `go/ast` only — no new dep) with a two-signal false-green check; wire `make apicover` + a **warning-only** CI step (mirrors the existing ≥85% warning-only gate). | Use `golang.org/x/tools` (new dep); a one-signal line-coverage gate; hard-fail CI from day one. | D2 targets the public-API contract; the two-signal check catches false-greens a line number hides; warning-only first matches the house rollout style (never a big-bang gate flip). | `cmd/apicover/*` TDD S0.4a–d; `Makefile` `apicover` target; `go.yml` warning step. |
| 2026-06-15 | 0.5 | Add `go/test/fixtures/StressN(t,n,k,fn)` (closed-channel barrier + WaitGroup) + document the canonical concurrency-test pattern & behavior-named test-name shape in `go/docs/testing.md`. | Each concurrency test rolls its own goroutine barrier ad-hoc. | P7: assert the invariant, not "didn't crash"; one shared helper makes Phase 2's stress backfill uniform and reviewable. | `fixtures/stress.go` + RED test; `go/docs/testing.md` update. |
| 2026-06-15 | 0.4r | **Dual-review fixes (apicover).** Replace the `basename:line` cover join with an **import-path-qualified** key (derive import path by walking to `go.mod` + module line; stdlib only) — collision-free across same-named files; add end-to-end `TestRun_WithCoverFile` proving the Covered path; harden `ParseCoverFunc` for spaces in paths; bucket malformed `//apicover:ignore` as Ignored + Enumerate-level test. | Keep basename:line + only warn on collision (defer correct keying to Phase 5). | Real 126-pkg run collides on `doc.go`/`config.go`; a *silently* wrong tool is worse than the small cost of correctness now. | go-reviewer + code-reviewer (0 CRIT); `cmd/apicover/*` -race+vet+gofmt clean; multi-pkg join verified (paths/textutil/semvercheck). |
| 2026-06-15 | 0.5r | **Dual-review fixes (StressN).** Soften the doc (barrier minimizes staggering, not a hard real-time guarantee) + warn against `t.Fatal` inside the goroutine. | — | Accuracy of the helper's contract. | LOW-only; doc change. |

| 2026-06-15 | 1.1 | New leaf `internal/gitexec` — `Git{Dir,Exec}` + `Default(dir)` constructor + `Capture/Output/Run/HEAD/DirtyPaths` + pure `PorcelainPath/PorcelainOldPath`, isolating the git CLI behind the `sysexec` seam (P2/P3). **Zero callers** (zero blast radius). | Keep git exec open-coded per package; default `Exec` to `DefaultRunner` (a nil-magic branch). | Dedup register: 25 files shell git; one faked seam makes every git call testable in the fast tier. Required (non-nil) `Exec` + a `Default` constructor removes the untestable real-git branch → 100% fast-tier public-API coverage. | apicover **9/9** public API, 0 uncovered, godoc-clean; `-race`+vet+gofmt clean; black-box `gitexec_test` (cycle-safe for Phase 4.5 when core imports it). Field named `Exec` (not the plan's `Run`) to avoid the `Run`-method clash; porcelain funcs guard short lines (public-API hardening over the call-site-guarded core). |

| 2026-06-15 | 1.2a | Migrate `internal/rollback` git calls onto `gitexec`. Extract testable cores `deleteRemoteTagWith(gitexec.Git,…)` / `revertAndShipWith(…)`; `DefaultSteps` passes `gitexec.Default(repoRoot)`. `gh` + evolve-binary execs stay (gitexec is git-only). | Leave the default git steps untested (need real git); a package-global git-runner var. | RED = "FakeExec records the git calls" — the raw `exec.Command` form couldn't. Behavior-preserving extract-and-delegate; public API (Steps/DefaultSteps/Run) unchanged. | coverage **77.4%→86.8%**; `-race`+vet+gofmt clean; PR based on `phase1-1-gitexec` (auto-retargets to main on #106 merge). |

| 2026-06-15 | 1.2b | Migrate the remaining non-hub git callers onto `gitexec`: `cycleclassify` (`gitLogMatchesCycle`), `changeloggen` (`ReadGitLogWith`/`VerifyRefWith`), `swarm` (`mergeWith` + a `newGit` factory for provision's two `-C` dirs). Shared `gitFailReason` helper. Cores are **ctx-first** `*With(ctx, g, …)`. | One PR per package (1.2b/c/d); match rollback's no-ctx core shape. | Grouped one mechanical PR (disjoint file sets, same proven pattern) for velocity. ctx-first unifies with swarm's genuinely-ctx cores (its originals used `exec.CommandContext`); rollback stays the lone no-ctx outlier, not worth churning. | go-reviewer behavior-preserving APPROVE (0 CRIT/0 HIGH; 1 LOW cosmetic `exit status 1`→`exit 1`, no logic consumer). Full fast `-race` GREEN; kernel untouched. **versionbump + preflight: NO git exec** (versionbump edits files via atomicwrite; preflight only PATH-probes git) → nothing to migrate, Phase 1.2 git-caller set complete. |

| 2026-06-15 | 1.3 | Complete `envchain` adoption in `cmd/evolve` typed env knobs: `cmd_fanout_dispatch` (`fanoutEnvConfig`, deletes local `envBoolDefault`), `cmd_phase_observer` (`observerEnvConfig`), `cmd_phase_watchdog` (`watchdogEnvConfig`), `cmd_subagent` (`readSubagentRunFlags`/`readDispatchParallelFlags`). `atoiOr(os.Getenv,d)`→`envchain.Int`; `==/!= "0/1"`→`envchain.Bool`; the `n>0` concurrency guard→`envchain.IntMin(_,2,1)`. | Migrate EVERY env read (incl. plain strings) for "no raw os.Getenv". | Scope to TYPED knobs only — `envchain.Resolve(k,nil,"","")` is byte-identical to `os.Getenv(k)`, so migrating plain string reads is churn with no dedup/behavior gain; the value + the parsing tests are in Bool/Int. Observer `StallS` KEEPS `atoiOr(envOr(...))` — its two-key fallback diverges under nested `envchain.Int` on an explicit `"0"`/invalid primary. `config.go` already injection-based (no os.Getenv); `phases/ship/native.go` DEFERRED to Phase 4 (kernel byte-identical invariant). | go-reviewer APPROVE, 0 CRIT/0 HIGH, **zero default-flips** (every `!= "0"`→`Bool(_,true)`, `== "1"`→`Bool(_,false)` verified). RED tests pin each knob's default + parsing; bridge-only B1 guard broadened to accept `envchain.*` as a valid LEGACY_AGENT_DISPATCH env-read (banned-signal checks unchanged). Full fast `-race` GREEN; kernel untouched. |

| 2026-06-15 | 1.4 | paths adoption — NO-OP this phase; no file migrated onto `internal/paths`. | Force-migrate `bridge/manifest.go` + `ship/gitops.go` cycle-state path onto a `Layout` now; rewrite `research/kb` `defaultSearchPaths` through paths. | All three 1.4 targets need a `Layout` API design first (none is a 1:1 swap): `bridge/manifest` + `ship/gitops` are hub/kernel files under the Phase-4 trust-kernel byte-identical invariant (same deferral as `ship/native.go` in 1.3), and `research/kb.go:73` is a relative const (`defaultSearchPaths`), not a `Layout` target. The broad paths sweep is deferred pending the `Layout` API design. | No Go change; design-spec:96 keeps `internal/paths` at `partial`; audit §4.4. |

| 2026-06-15 | 1.5 | Promote `internal/log` to a unified `Console{Out,Err,Quiet}` logger (`Infof`→Out, `Warnf`/`Errorf`→Err, nil-safe) + `Default()` (CLI, Info→stdout) + `Diag()` (diagnostics, ALL levels→stderr). Migrate NON-hub printers: verdictcache/clihealth WARN→`Default().Warnf`; releasepipeline/bridges + phases/runner (9) info→`Diag().Infof`, WARN→`Diag().Warnf`. | One Console with `Infof`→Out only; migrate every printer incl. hubs. | Added `Diag()` because the codebase logs INFO-diagnostics to **stderr** (Unix convention) — `Default().Infof`→stdout would REDIRECT those (a stream bug); `Diag` keeps Info on stderr so every migration is byte-identical. Hub printers (orchestrator ~115, bridge/manifest, ship/*) DEFERRED to Phase 4 (trust-kernel byte-identical invariant). | go-reviewer APPROVE, 0 CRIT/0 HIGH; **stream preservation verified for all 13 sites (zero info→stdout redirects)**; format strings/args byte-identical. RED pins Warnf→Err-not-Out + Diag-Info→stderr + Quiet-suppresses-Info-not-Warn. `-race`+vet+gofmt clean. |

| 2026-06-15 | 1.docs | Batch-land the Phase-1 decision-log rows (1.1–1.5) + the 1.4 no-op rationale as one docs-only slice (PR #112 / commit `64095c18`). | Co-commit each row with its code slice (the line-3 ideal). | Per-slice append in code PRs collides on the shared table; one reconciling docs slice keeps the reasoning trail complete while the code slices merge row-less for velocity. | Docs-only; commit `64095c18`. |
| 2026-06-15 | 2.audit | Adversarially audit the plan's 6 concurrency-test candidates before writing any → **2 WARRANTED (2.1, 2.5), 5 REJECTED** as no-op / phantom-caller. | Backfill all 6 as the plan listed them. | P7/P9: a stress test with no real >1-writer caller is test theatre. Rejected: quota (per-subprocess, called once on the main goroutine), phases/registry (Register is `init()`-only), observer (one Watch goroutine/Observer), log/SidecarWriter (per-fd `O_APPEND`, no shared Go mem), the 2.4 AST meta-guard (no Go linkage). | Audit→challenge workflow; rejections recorded with their phantom-caller reason. Mirrors the prior test-arch campaign's "ZERO warranted" lesson. |
| 2026-06-15 | 2.1 | Backfill `internal/adapters/ledger` concurrency: `TestSeal_ConcurrentWithCrossProcessAppend_ChainStaysVerifiable` + `TestAnchor_ConcurrentWithSeal_NoCorruptAnchor` (white-box; count via `gatherAllLines`; assert `VerifyDeep()==nil` + gapless-unique `entry_seq`). | The naive `ConcurrentAppend` test the plan named. | append-vs-append is ALREADY covered by `TestAppend_TwoProcessStress`; the REAL gap is Seal releasing then re-acquiring the lock (`seal.go:78-96` window) and Anchor taking no lock (`anchor.go:55`). RED-check = strip the inner `flock.Lock` → cross-proc corruption. | PR #114 (`440db700`); `-race` GREEN; released **v18.13.0**. |
| 2026-06-15 | 2.5 | Backfill `internal/swarm` `SessionRegistry.mu`: `StressN(8,40)` Register/Snapshot/MarkReaped, assert no-lost-update under `-race`. | — (not in the plan; surfaced by the 2.4 meta-challenge). | Dispatch spawns N workers each calling Register concurrently at `Concurrency≥2` (the default); `registry_test.go` had ZERO concurrency tests. RED-check = strip the mutex (challenger empirically confirmed a reliable -race trip). | PR #114 (`440db700`); `-race` GREEN; released **v18.13.0**. |
| 2026-06-15 | 3.1 | New leaf `internal/phaseio`: sealed `PhaseInput`/`Handoffs`/`CycleInputs`/`ErrorContext`/`CorrectionState`/`PhaseOutput` (typed `Verdict`+`FailureBlock`), getters-only. Imports ONLY `phasespec`. | Put the types in `core`; expose mutable fields. | D3 target; a dependency-free leaf is importable by both `core` and `router` without a cycle (phaseio→phasespec→config); sealing forbids the mutable-`Context` coupling the campaign removes. | PR #115; apicover **44/44**, 100% line; review fix = `NewPhaseInput` deep-copies Error/Correction pointees + `cloneAnyMap`. |
| 2026-06-15 | 3.2 | Add `RolloutStages.PhaseIO Stage` + `EVOLVE_PHASE_IO` env, default **off**, full 4-value ladder (off/shadow/advisory/enforce). | Fork a 3-value parser (`parseEvidenceStage`) as the plan's wording implied. | Reused the existing 4-value `parseStage` parameterized with `varName` — one parser, no fork. `dynamic_routing` warning byte-identical. | PR #116; RED pins each stage + typo→off. |
| 2026-06-15 | 3.3 | `router.Digest` reads the payload-wrapped envelope (`unwrapPayload`, flat fallback) at the 4 anchor reads; add `router.AssembleHandoffs` (the router→phaseio bridge, lives in router so phaseio stays pure). | A second on-disk reader in phaseio. | Single on-disk-shape authority (no second reader); dormant (no wrapped writer yet → flat in prod → byte-identical). Review documented the envelope **authority contract** (inner payload = SSOT; promoted top-level signals = a copy). | PR #117 (`51bc6336`); `routingtest.WrappedHandoffFiles`; review CORRECTED a worried failure-sentinel gap (foldFailureSentinel reads a separate report, not handoff JSON). |
| 2026-06-15 | 3.4 | Shadow assembly + comparator on the live dispatch path, gated `cfg.PhaseIO>=StageShadow` at `cyclerun_dispatch.go:113`; writes `phaseio-shadow-<phase>.json`; mismatch → ledger `kind:phaseio_shadow_mismatch`. | Compare `AssembleHandoffs` vs `Digest` (tautological — AssembleHandoffs wraps Digest). | **PROJECTION-FIDELITY** comparator: assembled `Handoffs` vs an independent field-projection of the live `Digest` signals — catches `handoffsFromSignals` bugs on real-cycle diversity. Separate testable `comparePhaseIOShadow`. Default off ⇒ byte-identical. | PR #118 (`6fd3323a`); `core/phaseio_shadow.go`; review: comparator field-completeness + ledger-err WARN, RED-checked. |
| 2026-06-15 | 3.5 | Typed `CycleInputs.PreviousVerdict` (additive leaf) + `assembleCycleInputs`/`assembleErrorContext`/`compareCycleInputsShadow`; fold CycleInputs/ErrorContext divergence into the same shadow ledger entry. | Migrate a live reader (retro) now. | Lowest-risk reader FIRST as a comparator extension, not a phase-file edit (retro keeps `req.Context`). Comparator `want`=legacy Context key, `got`=typed getter → catches assembler KEY DRIFT. | PR #119 (`bc564245`); apicover 45/45; go-reviewer APPROVE; 2 test-gaps hardened (`b05178dd`). |
| 2026-06-15 | 3.6 | Make the typed envelope + shadow comparator comprehensive across EVERY reader's field (net-new `CycleInputs.Carryover()`, the one field 3.5 missed). Last PURE-CODE slice. | Literal per-phase file edits (the plan's "migrate reader"). | Verified 3.5 precedent (`git show 1ebbeb6a` proved retro.go untouched): reader-code deletion is **3.10's** job, gated on a clean soak (Risk #5). One PR (both changes land in the same 2 files — phaseio leaf + core comparator). | PR #120 (`53991f71`); 8 named per-field tests + table-driven `TestCompareCycleInputsShadow_KeyDrift`; 4-lens review (byte-identity refutation FAILED → invariant holds). |
| 2026-06-15 | 3.shadow-soak | **Gate (plan §Verification): shadow soak** — `EVOLVE_PHASE_IO=shadow ./go/bin/evolve loop --cycles 3`, assert ZERO `phaseio_shadow_mismatch`. PASSED: 24 shadow dispatches across diverse phases, **0 mismatches**, positive-control shadow files present → shadow→advisory CLEARED. | Advance to advisory on code-review alone. | P6/P8: golden-equivalence on real-cycle data before the next stage. Rebuilt-binary cycles ship-fail `SELF_SHA` = expected (comparator runs at dispatch, verdict-independent). | log `evolve-batch-phaseio-soak.log`; nothing shipped (main clean at `53991f71`). `ledger verify` line-1740 break is PRE-EXISTING (cycle-93, ship-tolerated). |
| 2026-06-16 | 3.9 | Phase-agnostic ledger binding: `recordPhaseBinding(phase,in)` DISPATCHER delegating audit→`recordAuditBinding`/build→`recordBuildBinding` UNCHANGED + new `recordGenericBinding` (role=`phaseRole(phase)` identity); user-phase binding gated `>=StageEnforce`. | Merge the two asymmetric bodies into one shared body. | Highest-risk; the audit/build bodies are asymmetric (worktree-SHA, FATAL read, verdict-derived ExitCode, verdictcache.Put vs best-effort) → merging risks the byte drift the gate guards. Collapse ONLY at the role-naming/dispatch layer. | PR #121 (`56da860c`); go-reviewer APPROVE 0C/0H; dual-ledger byte-equality on a pinned git fixture + rt-001 + ship `findLatestAudit`. |
| 2026-06-16 | 3.7 | (DESCOPED) Migrate the ONE clean upstream disk read — `PhaseRequest.BuildPlan` + pure `readUpstreamBuildPlan` (gated advisory+/PhaseBuild/`EVOLVE_BUILD_PLANNER=1`); build.ComposePrompt prefers `req.BuildPlan` w/ byte-identical disk fallback. | The plan-literal `audit reads acs-verdict.json via Upstream.Audit()`. | Investigation CORRECTED the plan: audit GENERATES acs-verdict.json in-phase (audit.go:110) — it is NOT an upstream read; the only genuine downstream reader is ship's `checkEGPSGate`, whose migration is high-risk/low-value → **acs-verdict/ship migration DEFERRED** (separate needs-design item). | PR #122 (`bf5412fa`); `TestReadUpstreamBuildPlan` (9) + `TestComposePrompt_BuildPlanEnvelope` (3); go-reviewer APPROVE 0C/0H. |
| 2026-06-16 | 3.8 | Generalized failure block: Contract field `RequireFailureContextPhaseIO` (build/scout/triage) + `VerifyWithStage` (`VerifyWith`=…StageOff, byte-identical) gated `phaseIO>=StageEnforce`; stage-aware `RenderContractBlock` instructs those phases to EMIT the failure sentinel gated `>=StageAdvisory`. | Plan-literal "set `RequireFailureContext` for all verdict phases" (a NO-OP). | build/scout/triage have `Verdicts:nil` + emit NO sentinel (classify on section presence); forcing a verdict = the false-block risk the registry comment warns of. 3.8 = self-reported FAIL/WARN must carry a structured block. Prompt gates `>=advisory` (NOT ungated — `classifyFromSentinels` is not PhaseIO-gated, so emitting at off would change classification). | PR #123 (`95725369`); TDD compile-RED→behavioral-RED→GREEN; `TODO(3.10)` left for the `VerifyCatalogAware` reconcile rung (deferred, non-byte-identity). |
| 2026-06-16 | 3.advisory-soak | **Gate (plan:215, 2nd of two): advisory soak** — `EVOLVE_PHASE_IO=advisory ./go/bin/evolve loop --cycles 3`, assert ZERO `phaseio_shadow_mismatch`. PASSED throughout (incl. FAIL previous_verdict paths) → advisory→enforce CLEARED. Plus a **sentinel-coverage pre-flight**: cycles 320–348 ALL carry both `## Verdict`+`**PASS/FAIL**` AND the `<!-- evolve-verdict -->` sentinel. | Big-bang flip to enforce without the 2nd soak. | Both flip dimensions (envelope-correctness + verdict-grading) must be GREEN on real cycles before the cutover; the sentinel pre-flight closes the critics' #1 hard blocker (a prose-only audit report would false-FAIL at enforce). | soak2 cycles 347/348 SHIPPED; locked plan `~/.claude/plans/phaseio-3.10-cutover-locked.md` from a read-only blueprint workflow. |
| 2026-06-16 | 3.10 | **THE CUTOVER** (8-slice DAG S0–S7): S0 `PhaseRequest.Input` assembled at the dispatch seam only `>=enforce`; S1 `VerifyCatalogAwareStage` (resolves `TODO(3.10)`); S2 intent/triage/debugger typed reads gated on `PhaseInput.Active()`; S3 scout (goal/strategy/challengeToken, incl. Classify); S4 ship `commit_message`; S5 audit prose-verdict fallbacks GATED `<StageEnforce`; S6 ship gate `parseVerdicts` SENTINEL-FIRST at enforce; **S7 flip default off→enforce**. | Delete the prose-verdict regex + `req.Context` reads outright. | GATE the prose surfaces behind `<StageEnforce` (do NOT delete — else off/shadow/advisory break + lose the rollback path). retro EXCLUDED (out-of-band `failure_learning` dispatch bypasses the seam → keeps `Context["previous_verdict"]`). Stage threaded explicitly via `Config.PhaseIO` (REJECT req.Env self-parse). S6 adversarial-review CRITICAL fixed: parseVerdicts requires `s.Phase=="audit"` (a foreign-phase sentinel must not satisfy the ship gate). | PRs #124–#131 (`51846c0b`); `PhaseIO: StageEnforce` now DEFAULT; `EVOLVE_PHASE_IO=off` rolls back; enforce-soak cycle-350 `phaseio_shadow_mismatch=0` + 0 gate failures; **all 3 CI tiers** (default/integration/e2e) green. ⚠️ a default-flip ripples through ALL THREE tiers (S6 missed integration, S7 missed e2e). |
| 2026-06-16 | 4.5 | Migrate `internal/core`'s direct `exec.Command("git",…)` onto the `internal/gitexec` leaf (Phase 1.1) via a package seam `var gitRunner sysexec.RunFunc = sysexec.DefaultRunner`; route `defaultGitHEAD` + the `gitCapture` chokepoint (~25 callers incl. `recoverBuildLeak`) + 4 standalone sites (worktree.go ×4, resume.go, correction_ladder.go); dedup `porcelainPath`/`porcelainOldPath` onto gitexec. | A field on `cycleRun`/Orchestrator (core's existing `gitHEAD`/`gitDirtyPaths` seam shape) per site; OR a syntactic `exec.Command`→gitexec swap. | `gitCapture` is a free function with no receiver to hang a field on; one package-var seam makes the WHOLE call graph fakeable at once. Used `Capture` (not gitexec `Output`, which trims) for `defaultCurrentHead`/`gitStatusNames` (callers depend on UNTRIMMED stdout — porcelain leading-space, raw HEAD SHA). | PR #132 (`dffa2b09`, commit `33e75e07`); new `git_seam_test.go` (`TestRecoverBuildLeak_UsesInjectedGit`, default-tier — the prior coverage needed an `//go:build integration` real repo). ⚠️ `test/fixtures` imports core → white-box core test CANNOT use `FakeExec` (cycle) → hand-rolled `gitRec`. go-reviewer + silent-failure-hunter APPROVE, no new bugs. `-race`+integration-real-git+3-tier-vet GREEN. **Phase 4 now effectively complete** (orchestrator split was #100/#101; S4.6/S4.7 OPTIONAL/YAGNI). DEFERRED: `[orchestrator] WARN fmt.Fprintf`→`log.Console` fold (~140 sites/18 files) is its own slice. |
| 2026-06-15 | rel-18.13.0 | **Phase 2 milestone release** (per the operating-loop directive: cut a release at each phase boundary). | Defer all releases to the end. | D4 + Core Rule 10: a phase boundary is the natural, audit-bound checkpoint; `evolve release` runs the full preflight/changelog/ship/propagate pipeline. | tag `v18.13.0` (`a16e3660`); CLEAN (zero structural residues). |
| 2026-06-16 | rel-18.14.0 | **Phase 3 CORE interim release** (captures 3.1–3.7 + 3.9 — typed envelope, flag, router bridge, shadow comparator, build-plan envelope, phase-agnostic binding — ALL byte-identical at `EVOLVE_PHASE_IO=off`). | Wait for the full enforce cutover. | The core value was shadow-soak-validated and deliverable; releasing it de-risks the back-half (3.8 active-verdict + 3.10 cutover) as a separate focused effort. | tag `v18.14.0` (`733cf95c`); CLEAN. |
| 2026-06-16 | rel-18.15.0 | **Phase 3 COMPLETE release** (the enforce cutover, S0–S7; `PhaseIO: StageEnforce` is now the default). | — | Phase-boundary milestone; the typed phase-I/O envelope is now authoritative across the loop. | tag `v18.15.0` (`5621aba6`); GitHub Latest; marketplace 1s; preflight auditor PASS from cycle-348; ALL 3 structural residues CLEAN (4th consecutive). |
| 2026-06-16 | 234.docs | Batch-land the Phase-2/3/4 decision-log rows (2.audit/2.1/2.5, 3.1–3.10 incl. both soak gates, 4.5) + the three release-milestone rows as one docs-only catch-up slice. | Co-commit each row with its code slice (the line-3 ideal); leave the trail at Phase 1. | The code slices merged row-less for velocity (and across `/clear`-isolated sessions); this reconciling docs slice restores the single chronological audit trail the campaign promises. Operator directive: "make sure all the implementation are well documented in detail and stored in structured files." | Docs-only; this PR. |
| 2026-06-16 | 5.1 | Introduce per-package `apicover` hard-fail in CI: a checked-in SSOT allowlist `go/.apicover-enforce` (one `go list` pattern/line, `#`/blank ignored) + a new `go.yml` enforce step (`apicover -enforce -cover` over the listed dirs); seed `failuregrade`. The warning-only `./internal/...` sweep stays for non-graduated packages. | Hardcode the package list in the workflow yaml; OR bake the gate straight into an acs ship-gate predicate first. | One SSOT file means CI (and any future acs predicate) read the SAME list — the two surfaces cannot drift, and a future slice graduates a package by adding one line. `apicover -enforce` REQUIRES `-cover` (without it every exported func/method is a false-green: classify.go:42-47). The acs predicate is deferred (needs coverage generated inside the gate — nested `go test`). | PR #134 (`d2470826`); `failuregrade` already DoD-clean (8/8, +1 godoc on `Tier.String`); enforce step verified in real CI on both runners; RED-proven locally (soakreport exit 1, no-cover exit 1). go-reviewer APPROVE (1 MEDIUM: unquoted `$dirs` → piped through `xargs`). |
| 2026-06-16 | 5.2 | Graduate `skillcheck` (already DoD-clean — listed only, no code change) + `flagregistry` (name the never-tested exported `type Flag` via `TestRenderDoc_FoldsAndEscapes`, pinning the `renderDoc` ReplacedBy/RemoveIn fold + GFM pipe-escape; +godoc on `var All`). | A naming-only test that satisfies apicover without probing intent. | Rule 9: a test that merely names a type is a no-op; `RenderIndex` only exercised the pipe-escape incidentally, so the targeted test pins a real table-safety invariant. | PR #135 (`6865257d`); `flagregistry` 9/10→10/10 covered; all 3 listed packages enforce-clean in real CI (8/8, 3/3, 10/10); go-reviewer APPROVE. |
| 2026-06-16 | 5.3 | Graduate `soakreport`: name the never-tested exported types `Report`/`CycleRow` via `TestReport_RenderFromConstructedRows` (reaches Render's `> NOTE:` branch the Collect-driven tests never hit) + `TestAppendOnce_DedupesNotes` (dedup early-return). | A throwaway naming-only test for the two types. | The naming tests double as branch coverage: Render 94.7%→100%, appendOnce 50%→100%, package 91.5%→95.8%. The survey's "84.8% below floor" was stale — verify live numbers each slice. | PR #136 (`0b504faf`); apicover over all 4 listed packages exits 0 (8/8, 3/3, 10/10, 5/5); go-reviewer APPROVE (2 MEDIUM, both non-blocking). |
| 2026-06-16 | rel-18.16.0 | **Interim Phase-5 release** — cut off the usual phase-boundary cadence, capturing 4.5 + 234.docs + Phase-5 slices 5.1–5.3 (the per-package `apicover` enforce gate + the first 4 graduated packages). | Wait for the Phase-5 boundary (final v19.0.0) and release the whole tail at once. | User choice (AskUserQuestion): the gate mechanism + first graduations were deliverable and audit-bound, and Phase 4 was already effectively complete (4.5); shipping the mechanism early de-risks the long Phase-5 tail as a separate focused effort. | tag `v18.16.0` (`15ba0b34`); GitHub PUBLISHED + Latest (11:19:27Z); marketplace converged 1s; preflight 5 gates green (auditor PASS from cycle-348); ALL 3 structural residues CLEAN (5th consecutive). |
| 2026-06-16 | 5.4 | Batch-graduate 35 already-DoD-clean packages into `.apicover-enforce` in one list-only PR (4→39 enforced); zero code change. | One graduation PR per package; OR run go-reviewer on the batch. | A reviewer cannot run coverage — the CI enforce step over all 39 packages IS the authoritative verification; one line per package, no parallel-append collision. Integration-tagged enforce over a scoped set == CI's full-tree measurement (Go measures each package from its own test binary; `-race` doesn't change which statements execute) → high-confidence big batch. | PR #138 (`064c5c78`); CI enforced all 39 packages; local integration-tagged enforce over 39 = exit 0. List-only, no go-reviewer (per the "list-only batch needs no reviewer" decision). |
| 2026-06-16 | 5.5 | Godoc 9 apicover/coverage-clean packages (24 exported symbols: `gc`, `faillearn`, `semvercheck`, `versionbump`, `changeloggen`, `phases/{scout,intent,triage}`, `phasecoherence`) + graduate them (39→48 enforced). Documentation-only. | Defer godoc until a package also needs test work, graduating fewer at once. | These 9 failed ONLY the `-require-doc` floor (apicover + coverage already clean); godoc is a mechanical, low-risk graduation. Subagent-drafted, full diff reviewed; documentation-only proven (`git diff -U0` non-comment lines = only 2 gofmt whitespace fixes). | PR #139 (`396eeabb`); CI enforced all 48 (after a macOS channel-E2E flake rerun); local 48-pkg enforce exit 0; gofmt/vet clean; godoc accuracy spot-checked against impls. |
| 2026-06-16 | 5.6 | Graduate `adapters/bridge` (48→49 enforced): the lone apicover-clean adapter sitting under the ≥85% floor (84.6%) — two meaningful nil-resolver fallback tests (`SetContractResolver(nil)` restores built-in-only resolution; a zero-value `Adapter` degrades to built-in instead of nil-panicking) lift it to 88.5%. | A naming-only test to clear the floor. | The ≥85% floor is a real DoD criterion the campaign applies manually (`apicover -enforce` itself gates only gap=0); the two tests pin a genuine degrade-to-built-in contract, not coverage padding. | PR #141 (`35871913`); apicover 11/11; 3-tier CI green; go-reviewer APPROVE. |
| 2026-06-16 | 5.7 | Graduate 10 named-return-type packages (49→59): each was 100% line-covered but had one exported type returned by a constructor and never NAMED in a test; one meaningful test each (`want := T{...}` full-struct equality / `var x T = New(...)` + a real contract). | A `_ = pkg.X` naming line per type. | Rule 9: name the type via a binding that ALSO asserts a contract — swarmplan strengthened to pin the `Skipper` contract `TestPhase_Identity` lacks. | PR #142 (`ca0ab4be`); 3-tier CI green (enforce over 59); go-reviewer APPROVE. |
| 2026-06-16 | 5.8 | Graduate the WHOLE gap=1 tail — 21 packages (59→80): 18 get one meaningful test (14 types; 4 const/var via their real consumer); 3 had a DEAD exported error sentinel (`aggregator.ErrUnknownPhase`/`cyclesimulator.ErrCycleNonPositive`/`doctor.ErrUnknown` — zero refs tree-wide; the real failure paths return exit codes) → DELETED + the now-unused `errors` import removed. | Pin the orphan sentinels with a meaningless `errors.Is` test. | Rule 9: a naming-padding test passes CI while lying about the contract; deletion is the honest, YAGNI graduation for a truly dead export. Parallel read-only subagents draft; compiler + go-reviewer are the safety net. | PR #143 (`3f380fad`); 3-tier vet clean; apicover-enforce over all 80 = exit 0; go-reviewer APPROVE (1 MEDIUM fixed). |
| 2026-06-17 | 5.9 | Graduate the entire gap=2 tail — 19 packages (80→99): 15 get one meaningful `apicover_named_test.go`; 4 also DELETE a dead export — error sentinels `checkpoint.ErrAutoResumeExhausted`/`consensusdispatch.MissingInputError`/`fanoutdispatch.ErrUsage` + `dispatchevents.EventSameCycleStreak` (an enum member emitted by nothing — the same-cycle breaker emits `EventCircuitBreakerTripped`; doc comment corrected). | Test the dead exports / pin EventSameCycleStreak's literal value. | The test-vs-delete discriminator is whether a TRUTHFUL load-bearing contract exists: a false API surface (returned/emitted by nothing) → delete, not a value-pin. | PR #144 (`4d083f19`); full-list enforce over 99 = exit 0; `-race` green (cover 90–100%); go-reviewer APPROVE (1 MEDIUM: checkpoint magic-`3` pin → relationship + positivity). |
| 2026-06-17 | 5.10 | Graduate the entire gap=3 tail — 10 packages (99→109): one meaningful test per package (interface satisfaction + exercise for the port/seam types; JSON-tag round-trips for Journal/LedgerEntry; output structs via their real producer; consts via their consumer). TEST-only (no dead exports). | — | Mostly types: `var _ Iface = impl` is load-bearing (compile assertion) when paired with a real method call. | PR #145 (`3c7022f4`); full-list enforce over 109 = exit 0; `-race` green (90.4–100%); go-reviewer APPROVE. |
| 2026-06-17 | 5.11 | Graduate the entire gap=4 tail — 8 packages (109→117): one meaningful test per package. | — | 4 go-reviewer MEDIUMs addressed: `recovery.PhaseOutcome` is a leaf DTO (core's producer is unexported & uncyclable) → documented invariant test; `reflection.Friction` → real yaml round-trip; `config.Load` returns `[]Warning` NOT error (reviewer misread) → capture warnings; bridge/channel 30ms sleep → poll-until-ready. | PR #146 (`cda70006`); full-list enforce over 117 = exit 0; `-race` green (93.6–100%). |
| 2026-06-17 | 5.12 | Graduate the gap=5–7 tail — 5 packages (117→122): meaningful tests per package; also DELETE `phaseobserver.ExitFatal` (an unused member of the bash-parity exit-code enum — `Run` returns only ExitOK/ExitInvalidArgs). | Pin ExitFatal's value in an enum-ordering test. | Same dead-symbol rule as 5.9: an exit-code member produced by nothing → delete; envchain's 7 env-key consts are driven through their real `IntMin`/`Int` getters (can't import the core/cyclehealth consumers — cycle). | PR #147 (`7b50f60b`); full-list enforce over 122 = exit 0; `-race` green (95.1–100%); go-reviewer APPROVE. |
| 2026-06-17 | 5.13 | Graduate the heavy tier — the 5 highest-gap packages (gap 11–14: bridge, phasecontract, router, interaction, swarm; 122→127). | — | bridge's pre-existing live-tmux `TestRealTmux_*` FAIL on this host (fake-CLI/tmux REPL) but pass-or-skip in CI; bridge is 84/84 apicover-clean even when they contribute no coverage (every func also covered by a passing fake test), so CI's enforce passes. go-reviewer MEDIUM: router advisor-DTOs (BenchedCLI/CarryoverTodo/PhaseCard) set-then-read → json wire round-trip. | PR #148 (`eca13a8e`); full-list enforce over 127 = exit 0; the 4 non-bridge pkgs `-race` green (92.3–98.2%). |
| 2026-06-17 | 5.14 | Graduate `internal/core` (193 exports; gap 62; 127→128 = EVERY internal package — the gap≥2 tail is now EXHAUSTED). 3 cohesive test files: shiperror protocol (table-driven through NewShipError+AsShipError, each code's Class transcribed from its live ship call-site), ports (Bridge/Guard/Ledger/Storage via the in-package fakes + the DTOs), misc (advisor/state seams + the `PhaseBoundaryCheckpointer` var via a recording-closure + full RunCycle). | — | core is the central package; the per-code Class column pins the real protocol contract (a production drift fails the table). | PR #149 (`48e92976`); apicover internal/core = 193/193; full-list enforce over 128 = exit 0; `-race` green (90.0%); go-reviewer APPROVE. |
| 2026-06-17 | 5.15 | acs ship-gate apicover **completeness** predicate (`go/acs/regression/apicover/predicates_test.go`, `//go:build acs`): every `./internal/...` package must be in the `.apicover-enforce` SSOT, so a new package cannot escape the gate at ship time. | Run the coverage-based `apicover -enforce` inside the acs gate. | The CORRECTNESS half (each package apicover-clean) needs an integration coverage profile a per-cycle ship-gate can't afford to re-run — it stays hard-gated by CI's `go.yml`; the predicate adds the cheap COMPLETENESS half (closes the acs-predicate deferral from slice 5.1). | PR #150 (`850ca691`); passes against main's 128-package list; gofmt + `go vet -tags acs` clean; acssuite auto-discovers the sub-package. |
| 2026-06-17 | rel-19.0.0 | **CAMPAIGN-FINAL release** (ADR-0050 Phase 6 finalization): all 128 internal packages in the apicover hard-fail gate + the acs completeness predicate. The gap≥2 tail (48 packages) landed this session as 6 batched PRs #144–149. | An interim minor for the gap≥2 tail, then v19.0.0 later. | User directive: drive the whole tail to completion then cut the designated final **v19.0.0** (a major bump — also justified by the 5 dead-export removals across 5.8/5.9/5.12). | tag `v19.0.0` (`8be6ae7f`); GitHub Latest; marketplace converged 1s; preflight 5/5 + advisory PASS; release-commit CI green (CI+go); all 3 structural residues CLEAN. |

**Accepted-by-design (Phase 0 review, not fixed — recorded so they are explicit choices):**
`NamesReferencedInTests` collects all idents/selectors (broad "named" signal) — intentional: it is the AST half of a *two-signal* check; a method sharing a name with a common local (e.g. `Error`) may be over-credited, acceptable for warning-only tooling and revisited if Phase 5 needs `go/types`. · `Enumerate` measures against the ambient `build.Default` GOOS/GOARCH — intentional (matches what `go test` compiles on that host). · `CoverEntry.Func` is parsed but not used in the join — kept as the parser's correctness anchor (asserted by `cover_test.go`) and for future import-path qualification.
