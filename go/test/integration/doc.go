// Package integration holds cross-package integration tests that wire several
// internal/ packages together against real (temp-dir) filesystem state — without
// spawning a CLI subagent. See go/docs/testing.md for the tier model.
package integration
