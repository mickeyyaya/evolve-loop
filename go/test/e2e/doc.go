// Package e2e holds end-to-end tests that exercise a full cycle path through the
// orchestrator/ship pipeline against fixture workspaces. These are the heaviest,
// slowest tier and may shell out to git. See go/docs/testing.md for the tier model.
package e2e
