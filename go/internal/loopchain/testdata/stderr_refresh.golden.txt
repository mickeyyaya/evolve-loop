== ahead_check_failed refreshed=false
[chain] boundary-refresh: ahead-check failed (git fetch: network unreachable) — skipping refresh, continuing on the current binary
== lane_check_unverifiable refreshed=false
[chain] boundary-refresh: WARN fleet lane check unverifiable (runlease: parse .lease: unexpected end of JSON input) — cannot prove the plane is idle, refusing to rebuild the shared binary; continuing on the current binary
== lane_active refreshed=false
[chain] boundary-refresh: a sibling fleet lane is active (fresh run lease) — refusing to rebuild the plane binary mid-batch; continuing on the current binary
== breaker_refused refreshed=false
[chain] boundary-refresh: REFUSED — a refresh was already performed for build commit cafebabe1234 and the running binary still reports it; the rebuild did not move the binary. Continuing on the current binary rather than re-execing again (see .evolve/boundary-refresh-attempt.json)
== rebuild_failed refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: rebuild failed (make -C go build: exit status 2: build failed: syntax error) — skipping refresh, continuing on the current binary
== no_target refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: no re-exec target (rebuilt binary {ROOT}/go/bin/evolve: stat {ROOT}/go/bin/evolve: no such file or directory) — skipping refresh, continuing on the current binary
== repin_refused refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: repin refused (phaseintegrity: re-pin refused — binary build-commit "cafebabe1234deadbeef" is not provenance-verified (not an ancestor of HEAD); pass operator authorization (--reset-sha) to override) — skipping refresh, continuing on the current binary
== audit_log_failed refreshed=true
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: WARN: audit log write failed (open boundary-refresh log: open {EVOLVE_DIR}/boundary-refresh-log.jsonl: is a directory) — pin already moved
[chain] boundary-refresh: re-pinned STALE_PIN -> 7448e675c81e — re-executing {ROOT}/go/bin/evolve to pick up the rebuilt binary
== empty_argv refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: empty re-exec argv — skipping refresh
== arm_failed refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: cannot arm the re-exec loop breaker (write boundary-refresh attempt marker: open {EVOLVE_DIR}/boundary-refresh-attempt.json: is a directory) — skipping refresh rather than risking an unbounded re-exec loop
== reexec_failed refreshed=false
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: re-pinned STALE_PIN -> 7448e675c81e — re-executing {ROOT}/go/bin/evolve to pick up the rebuilt binary
[chain] boundary-refresh: re-exec failed (exec format error) — continuing on the current binary
== refreshed refreshed=true
[chain] boundary-refresh: HEAD has advanced past the running binary's build commit (cafebabe1234) — rebuilding
[chain] boundary-refresh: re-pinned STALE_PIN -> 7448e675c81e — re-executing {ROOT}/go/bin/evolve to pick up the rebuilt binary
