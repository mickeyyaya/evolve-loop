{
  "schema_version": 1,
  "cli": "agy-tmux",
  "binary": "agy",
  "binary_min_version": "1.0.0",
  "default_tier": "hybrid",
  "tier_dependencies": {
    "full": [
      "agy",
      "tmux"
    ],
    "hybrid": [
      "agy",
      "tmux"
    ],
    "degraded": [
      "agy"
    ]
  },
  "prompt_marker": "? for shortcuts",
  "default_model": "gemini-3.5-flash",
  "default_args": [],
  "params": {
    "model_tier": {
      "channel": "flag",
      "flag": "-m",
      "from": "model_tier_map"
    },
    "permission": {
      "channel": "flag",
      "values": {
        "bypass": [
          "--dangerously-skip-permissions"
        ]
      }
    },
    "settings_scope": {
      "channel": "noop"
    },
    "session_mode": {
      "channel": "controller"
    }
  },
  "interactive_prompts": [
    {
      "name": "trust_prompt",
      "regex": "Do you trust the contents of this project|Antigravity CLI requires permission",
      "response_keys": "Enter",
      "policy": "auto_respond",
      "note": "agy asks to trust dir on first launch; default selection is 'Yes, I trust this folder'"
    },
    {
      "name": "auth_recheck",
      "regex": "Please log in|Authentication required|Login expired",
      "response_keys": null,
      "policy": "escalate"
    },
    {
      "name": "rate_limit",
      "regex": "(usage|rate)[ -]limit (reached|exceeded|hit)|too many requests|RESOURCE_EXHAUSTED",
      "response_keys": null,
      "policy": "escalate"
    },
    {
      "name": "quota_exhausted",
      "regex": "quota.*exceed|daily limit|monthly limit|free.tier limit",
      "response_keys": null,
      "policy": "escalate",
      "note": "Distinguishable from transient rate-limit; usually requires plan upgrade"
    },
    {
      "name": "permission_prompt",
      "regex": "Allow .*\\?|Grant permission|Approve action",
      "response_keys": "y,Enter",
      "policy": "auto_respond",
      "note": "Belt-and-suspenders alongside --dangerously-skip-permissions; harmless if bypass already accepted it"
    }
  ],
  "stub": false,
  "model_tier_map": {
    "fast": "gemini-3.5-flash",
    "balanced": "gemini-3.5-flash",
    "deep": "gemini-3.5-flash"
  },
  "note": "Interactive agy via tmux. Preserves Google AI Pro subscription billing (OAuth). REPL marker is the '? for shortcuts' footer text \u2014 unique to the idle state. agy DOES accept a -m/--model flag (corrected 2026-05; prior manifests wrongly set channel=noop). model_tier_map values pinned to the documented default gemini-3.5-flash pending live -m value validation; tier differentiation (deep -> gemini-3.1-pro) is a follow-up once the exact -m strings agy accepts are confirmed. See docs/architecture/cli-capability-matrix.md."
}
