{
  "schema_version": 1,
  "cli": "agy",
  "binary": "agy",
  "binary_min_version": "1.0.0",
  "default_tier": "full",
  "tier_dependencies": {
    "full": ["agy"]
  },
  "prompt_marker": null,
  "default_model": "gemini-3.5-flash",
  "default_args": [],
  "interactive_prompts": [
    {
      "name": "auth_recheck",
      "regex": "Authentication required|Please log in|Sign in to (Antigravity|Google|Gemini)|Login expired",
      "response_keys": null,
      "policy": "escalate",
      "note": "Bridge cannot complete OAuth/Google login; escalate to operator"
    },
    {
      "name": "rate_limit",
      "regex": "rate.?limit|too many requests|429|RESOURCE_EXHAUSTED",
      "response_keys": null,
      "policy": "escalate",
      "note": "Operator decides retry vs abandon"
    },
    {
      "name": "quota_exhausted",
      "regex": "quota.*exceed|daily limit|monthly limit|free.tier limit",
      "response_keys": null,
      "policy": "escalate",
      "note": "Distinguishable from transient rate-limit; usually requires plan upgrade"
    },
    {
      "name": "permission_prompt",
      "regex": "Allow .*\\?|Grant permission|Approve action",
      "response_keys": "y,Enter",
      "policy": "auto_respond",
      "note": "Belt-and-suspenders alongside --dangerously-skip-permissions; harmless if bypass already accepted it"
    }
  ],
  "stub": false,
  "model_tier_map": {
    "fast": "gemini-3.5-flash",
    "balanced": "gemini-3.5-flash",
    "deep": "gemini-3.5-flash"
  },
  "note": "agy is Antigravity, Gemini-backed (NOT Anthropic). agy CLI exposes no model selection flag (-c is --continue, not --config). All abstract tier values resolve to the same gemini-3.5-flash default — model_tier_map is informational here since params.model_tier.channel is noop. Cost-leak guards: GEMINI_API_KEY / GOOGLE_API_KEY."
}
