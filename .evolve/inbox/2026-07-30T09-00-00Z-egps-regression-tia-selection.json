{
  "connects_to": [
    "go/internal/acssuite/acssuite.go",
    "go/internal/changedpkgs/changedpkgs.go",
    "kb/research/failed-loop-analysis-2026-07/README.md"
  ],
  "created_at": "2026-07-30T09:00:00Z",
  "fix": "In acssuite runGoTest: partition ./acs/regression/... by predicate package's declared target packages (predicates already grep/shell specific packages \u2014 derive mapping from acs metadata or a manifest); select intersect(changedpkgs). Config via policy.json gates block (stage: off|shadow|enforce, shadow logs would-skip counts first). Full-corpus triggers: batch boundary, any red in selected set, weekly floor. Wiring proof: predicate pinning that an out-of-selection regression still runs at the boundary sweep.",
  "id": "egps-regression-tia-selection",
  "kind": "pipeline-integrity",
  "notes": "From the 2026-07-30 literature review. Serves BOTH operator priorities (pipeline improvement + token optimization). | pass-rate directive 2026-07-30: RAISED \u2014 shrinks the class-B flake surface AND per-cycle load AND tokens; Meta PTS evidence 99.9% catch at 1/3 tests. | [3RD LIVE INSTANCE 2026-08-03] cycle-1250 changed internal/router; changed-scope gates never selected internal/routingtest (which imports router and holds the KEYSTONE parity test) \u2014 main red 5 commits while the batch shipped on top, caught only by repo-wide CI. Reverse-dependency selection (router -> routingtest) is exactly this item. The apicover class, the stub-coherence class, and now signal-parity: three costumes of one scope disease in one week. | [LANDED 2026-08-04] Core build SHIPPED by cycles 1257\u21921260 (origin fcdd466e): internal/regressiontia (reverse-dependency selection), policy staging + regressiontia.go, audit-phase wiring WITH tia_wiring_test proof, ADR-0082 \u2014 stage=SHADOW. Slice-1 PTS groundwork landed cycle-1254. DO NOT re-attempt the build. REMAINING SCOPE ONLY: (1) shadow-soak \u226510 cycles \u2014 compare shadow-selected set vs full-corpus outcomes, bar = zero missed-red where shadow would have skipped a red predicate's package; (2) after clean soak, flip stage to enforce via policy (operator-visible change); (3) verify the three scope-disease costumes (apicover exports, stub coherence, signal parity e.g. cycle-1250 routingtest keystone) are each covered by a reverse-dependency edge in the live map. Weight 0.91\u21920.6: build done, soak+flip pending. | [4TH LIVE INSTANCE 2026-08-04] cycle-1262 shipped a runtime-minted .evolve/phases/ship-stage-hygiene-check/phase.json; changed-scope never selected internal/phasespec (catalog-on-disk scanner) -> TestPhaseCatalog_OptionalPhasesHaveSelectMetadata RED on main both platforms. TIA was ALREADY LANDED IN SHADOW when this shipped \u2014 strongest enforce-flip evidence yet: the shadow map's phasespec reverse-edge (catalog dir -> phasespec) must be verified during soak.",
  "priority": "P1",
  "summary": "EGPS runs the FULL ~200-predicate regression corpus every cycle (~minutes of go-test under fleet load). Meta's predictive test selection (engineering.fb.com 2018, arXiv:1810.05286) catches 99.9% of regressions running ~1/3 of tests. Our deterministic equivalent needs no ML: select regression predicates whose packages intersect the cycle's changed packages (internal/changedpkgs is the existing SSOT) plus always-on red-team + this-cycle scopes; run the FULL corpus at batch boundaries and on any selected-red so selection can never hide a regression class. Double win: fewer predicate-seconds under load directly reduces the class-B env-flake surface (batches 17-20: ~20% of FAILs) AND cuts per-cycle token/wall cost.",
  "title": "EGPS regression suite: changed-package test selection + boundary full-sweep (Meta PTS shape)",
  "weight": 0.6
}