{
  "connects_to": [
    "go/internal/policy/policy.go (defaultPhaseArtifactTimeoutS, PhaseArtifactTimeouts)",
    "go/internal/bridge/driver_tmux_repl.go (tmuxArtifactTimeoutS=300)",
    "go/internal/bridge/stopreview.go (defaultArtifactMaxExtends=6)",
    "docs/operations/runtime-reference.md:66",
    ".evolve/policy.json"
  ],
  "created_at": "2026-07-30T16:10:00Z",
  "fix": "CONSOLE MITIGATION ALREADY APPLIED 2026-07-30 (runtime .evolve/policy.json, protected surface — needs its own operator ship at the boundary): bridge.phase_artifact_timeout_s raised to 1200 for tdd/tdd-engineer, build/builder, audit/auditor, adversarial-review; retro stays 900; EVERY OTHER PHASE DELIBERATELY LEFT on the 300s builtin so global hang detection is not broadly weakened (the compiled default's own stated rationale). DURABLE WORK: (1) decide whether the compiled defaults themselves should carry deep-tier budgets rather than requiring operator config — a fresh clone has the same cliff; (2) the budget should arguably scale with the resolved TIER (deep/top get more than fast/balanced) rather than being phase-keyed, since it is thinking time that varies; (3) an artifact-timeout death currently produces NO deliverable and NO reason beyond the code — it should record how long it waited and how many extends it consumed, so the next reader can tell 'too slow' from 'wedged'; (4) FIX THE DOCS BUG already recorded in state.json: docs/operations/runtime-reference.md:66 publishes the WRONG KEY for this setting, so any operator following the docs silently gets no effect.",
  "id": "deep-phase-artifact-budget-too-small",
  "kind": "pipeline-integrity",
  "notes": "This is a pass-rate lever with the clearest cost of any item in the queue: 6 cycles lost in a single day, every one of them a phase that was working, not failing. Found by correlating the VERDICT-FORENSIC teardown-FAIL lines (which exist because of the cycle-3 retro ask) with the observer-events timestamps (650s attempt-1 duration) on a quiet host.",
  "priority": "P1",
  "summary": "SIX phases died with codes=[missing_artifact], report+acs absent, across FOUR phase types in one day: audit x2 (batch-19 cycle-1201), retro x1 (1201), adversarial-review x1 (batch-21 cycle-1217), tdd x2 (batch-21 cycle-1218, batch-22 cycle-1219). The last occurred on a QUIET host (load 3.0, zero leaked processes) which rules out contention as the sole cause — my earlier attribution to a leaked load generator was only partly right: the load made it worse, the budget is the floor. Arithmetic: the bridge base artifact-wait is 300s (driver_tmux_repl.go tmuxArtifactTimeoutS) plus up to 6 extends (stopreview.go defaultArtifactMaxExtends) = ~650s effective, and policy.go's defaultPhaseArtifactTimeoutS raises ONLY retrospective/retro to 900. A deep-tier (opus) agent doing real analysis on this repo legitimately needs more than 11 minutes, so every such phase is one slow turn away from a content-free infra FAIL that burns the cycle.",
  "title": "Deep-tier phases die at ~650s with NO artifact — the per-phase artifact budget only exempts retro, and it cost 6 cycles in one day",
  "weight": 0.95,
  "consumed_by": "console-2026-07-30: LANDED — PR #390 squash 32671641 (rebuilt from the closed #384). SIX phases died missing_artifact in one day across FOUR phase types — audit x2, retro, adversarial-review, tdd x2 — one on a PROVABLY QUIET HOST, which is what ruled out contention as the sole cause. Arithmetic: base wait 300s (bridge.tmuxArtifactTimeoutS) + 6 extends (defaultArtifactMaxExtends) = ~650s effective ceiling for every phase the exemption map did not name, and only retro was exempt at 900s. Deep-tier analysis phases now carry 1200s as COMPILED defaults (policy.defaultPhaseArtifactTimeoutS), operator-overridable via bridge.phase_artifact_timeout_s with positive-only merge so a typo can neither restore the cliff nor yield a negative deadline. Both vocabularies are keyed (tdd/tdd-engineer, build/builder, audit/auditor) because the phase-name vs agent-label skew is permanent — a map keyed on one alone is unit-green and live-dead, the cycle-1054 defect. The list stays NARROW BY DESIGN: every unnamed phase keeps 300s, so a wedged scout/intent/triage/ship still surfaces in ~11 minutes rather than ~2.3 hours. SECOND DEFECT FOUND EN ROUTE: the exit-81 cause came from a helper returning the last non-empty line when it sees no '[bridge]' prefix, but tmux drivers prefix '[<cli>-tmux]' — so a dead phase's recorded error message was A FILENAME. A CLAIM IN MY OWN FILED ITEM WAS FALSE (wrong config key at runtime-reference.md), refuted by reflecting the struct tag against every doc mention; the stale default cell was real and is fixed, with a reflection-derived drift pin."
}
