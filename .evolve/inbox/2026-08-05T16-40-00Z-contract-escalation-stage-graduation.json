{
  "acceptance": [
    "Review the escalation shadow ledger since the cycles-1293/1294 landing: for each shadow 'would-escalate' record, would the escalation have preempted a real contract block or circuit-open? Tabulate hits/misses in the report.",
    "If the shadow record supports it, graduate contract-block CLI escalation stage shadow→advisory (or straight to live if the ledger is unanimous) — as a policy.json escalation block (config only, no Go), with the compiled default left at shadow for fresh deployments.",
    "Regression: 3 consecutive contract blocks on one (cli,phase) with escalation live must escalate the CLI BEFORE the contract-gate circuit opens (the circuit stays as the backstop, not the first responder).",
    "The interplay with quota-bench is defined and tested: a benched CLI (codex repl_boot_timeout tonight) must not be escalation's target NOR keep receiving build dispatches that block 3x.",
    "go vet ./..., -race green; gate-WIRING-proof for whatever stage flips."
  ],
  "connects_to": [
    "contract-block-cli-escalation (landed cycles 1293/1294 — the machinery; stage has sat in shadow since)",
    "audit-warn-prescriptions-unenforced (sibling producer-side-gap class, in flight this batch)",
    "demotion ledger (records tonight's enforce->advisory demotion)"
  ],
  "created_at": "2026-08-05T16:40:00Z",
  "files": [
    ".evolve/policy.json (escalation block — operator protected surface, own ship)",
    "go/internal/ (escalation stage resolution + wiring proof tests)"
  ],
  "fix": "Graduate the already-landed contract-block CLI escalation from shadow using its own shadow-ledger evidence, so the FIRST responder to repeated contract blocks is 'route this phase to a compliant CLI', and enforce->advisory demotion becomes the backstop it was designed to be.",
  "id": "contract-escalation-stage-graduation",
  "priority": 0.86,
  "problem": "LIVE INSTANCE 2026-08-05 ~16:30Z, lane materialize-crosspoll-debounce-eval: contract-gate CIRCUIT OPEN after 3 consecutive build-deliverable blocks (missing '## Changes' section + challenge token not echoed) forced enforce->advisory demotion. The escalation machinery that exists precisely to preempt this (swap the blocking CLI after early blocks) ran in stage=shadow (compiled default; loop log 'escalation boundary (cycle 5, stage=shadow): bumped=0 filed=0 skipped=0 planned=3') and therefore did nothing live. Token mechanism itself is healthy — the same batch's bug-reproduction phase echoed its token (cycle-1344, e70fee1cb855ea91); the non-compliant producer was the codex-tmux build agent, the same CLI family already quota-benched this batch (repl_boot_timeout, wave 3->2).",
  "source": "console-session monitor event during post-#417 steady state, 2026-08-05",
  "title": "Graduate contract-block CLI escalation out of shadow — circuit-open/demotion should be backstop, not first responder"
}
