---
score_cap:
  - criterion: "Kernel floor (ADR-0099 document contract, the same engine the build handoff floor and the audit gate run): solutions/token-frontier-watch/ has >=2 options/*.md, recommendation.md (Options Compared + Recommendation) and assumptions-and-evidence.md (Assumptions + Evidence), no placeholder, and every option and the recommendation cite the evidence file"
    max_if_missing: 3
    evidence: "cd go && go run ./cmd/evolve solution check token-frontier-watch --project-root .."
  - criterion: "AC1 location + verdicts: a dossier docs/research/token-optimization-2026/frontier-watch-<YYYY-MM-DD>.md exists (none under knowledge-base/, a runtime write surface since #410), with a ## section for each family (heading names 'ollama' / 'latent'), and each family carries exactly one Verdict line whose value is exactly BUILD or DEFER"
    max_if_missing: 4
    evidence: "python3 -c 'import re,glob,sys;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);S=lambda t: [(s.split(\"\\n\",1)[0].lower(),s) for s in re.split(r\"(?m)^(?=##[ \\t])\",nf(t)) if re.match(r\"##[ \\t]\",s)];FAM={\"ollama\":lambda h: \"ollama\" in h and \"latent\" not in h,\"latent\":lambda h: \"latent\" in h and \"ollama\" not in h};fam=lambda f,k: \"\\n\".join(s for h,s in S(open(f).read()) if FAM[k](h));V=lambda b: re.findall(r\"(?im)^[ \\t>*_|-]*verdict[ \\t*_]*[:|][ \\t*_]*([^\\n]*)\",b);ok=lambda r: bool(re.match(r\"(BUILD|DEFER)\\b\",r)) and not re.match(r\"(BUILD|DEFER)[ \\t*_]*(/|or\\b|and\\b|\\|)[ \\t*_]*(BUILD|DEFER)\",r,re.I);kb=glob.glob(\"knowledge-base/**/frontier-watch-*.md\",recursive=True);E=([\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\"] if not fs else [])+[\"RED: \"+p+\" is documentation under knowledge-base/, a runtime write surface (knowledge-base/README.md); the token-optimization-2026 package lives in \"+D for p in kb]+[f\"RED: {f}: no ## section whose heading names the {k} family\" for f in fs for k in FAM if not fam(f,k).strip()]+[f\"RED: {f}: the {k} family carries {len(v)} Verdict line(s); need exactly one\" for f in fs for k in FAM for v in [V(fam(f,k))] if fam(f,k).strip() and len(v)!=1]+[f\"RED: {f}: the {k} verdict {v[0].strip()!r} is not exactly BUILD or DEFER\" for f in fs for k in FAM for v in [V(fam(f,k))] if len(v)==1 and not ok(v[0])];OKMSG=\"OK: \"+\", \".join(fs);print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "AC1 citations: every repo path the dossier cites (a docs/ go/ solutions/ knowledge-base/ path, or a relative markdown link) exists, and each family section carries >=2 distinct citations of which >=1 is external (URL or arXiv id)"
    max_if_missing: 5
    evidence: "python3 -c 'import re,glob,sys,os;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));fs or sys.exit(\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\");nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);S=lambda t: [(s.split(\"\\n\",1)[0].lower(),s) for s in re.split(r\"(?m)^(?=##[ \\t])\",nf(t)) if re.match(r\"##[ \\t]\",s)];FAM={\"ollama\":lambda h: \"ollama\" in h and \"latent\" not in h,\"latent\":lambda h: \"latent\" in h and \"ollama\" not in h};fam=lambda f,k: \"\\n\".join(s for h,s in S(open(f).read()) if FAM[k](h));URL=r\"https?://[^\\s)>\\]`\\\"]+\";ARX=r\"(?<![\\d.])\\d{4}\\.\\d{4,5}(?:v\\d+)?(?![\\d.])\";BARE=r\"(?<![\\w/.-])((?:docs|go|solutions|knowledge-base)/[\\w./-]*?\\.(?:md|go|json|ya?ml))(?![\\w/-])\";LNK=r\"\\]\\((?!https?:|mailto:)([^)\\s#]+)\";rs=lambda f,p: os.path.normpath(os.path.join(os.path.dirname(f),p));refs=lambda f,b: [(p,p) for p in re.findall(BARE,re.sub(URL,\"\",b))]+[(p,rs(f,p)) for p in re.findall(LNK,b)];cites=lambda f,b: set(re.findall(URL,b))|set(re.findall(ARX,re.sub(URL,\"\",b)))|{r for p,r in refs(f,b) if os.path.exists(r)};ext=lambda b: re.findall(URL,b)+re.findall(ARX,re.sub(URL,\"\",b));E=[f\"RED: {f}: cites {p} but {r} does not exist (dangling path)\" for f in fs for p,r in refs(f,nf(open(f).read())) if not os.path.exists(r)]+[f\"RED: {f}: the {k} family has {len(c)} distinct citation(s); need >=2\" for f in fs for k in FAM for c in [cites(f,fam(f,k))] if len(c)<2]+[f\"RED: {f}: the {k} family cites no external source (URL or arXiv id)\" for f in fs for k in FAM if not ext(fam(f,k))];OKMSG=\"OK: citations resolve in \"+\", \".join(fs);print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "AC1 quantified ollama estimates: the ollama family states a token-denominated estimate (N tokens, or X% tied to tokens) AND a latency-denominated estimate (N ms/s/min, or X% tied to latency/prefill/TTFT/wall-clock) in prose, lists or tables (not code fences; dates and section numbers do not count)"
    max_if_missing: 5
    evidence: "python3 -c 'import re,glob,sys;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));fs or sys.exit(\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\");nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);S=lambda t: [(s.split(\"\\n\",1)[0].lower(),s) for s in re.split(r\"(?m)^(?=##[ \\t])\",nf(t)) if re.match(r\"##[ \\t]\",s)];FAM={\"ollama\":lambda h: \"ollama\" in h and \"latent\" not in h,\"latent\":lambda h: \"latent\" in h and \"ollama\" not in h};fam=lambda f,k: \"\\n\".join(s for h,s in S(open(f).read()) if FAM[k](h));N=r\"(?<![\\w.,])(?<!\\w-)\\d[\\d.,]*\";tok=lambda u: re.search(N+r\"\\s*[kKmM]?\\s*(?:(?:input|output|prompt|cached|prefill|cache[- ]read)\\s+)?tokens?\\b\",u,re.I) or (re.search(N+r\"\\s*%\",u) and re.search(\"token\",u,re.I));lat=lambda u: re.search(N+r\"\\s*(?:ms|s|secs?|seconds?|mins?|minutes?)\\b\",u,re.I) or (re.search(N+r\"\\s*%\",u) and re.search(r\"latency|wall[- ]?clock|ttft|time[- ]to[- ]first|prefill|duration\",u,re.I));U=lambda b: [u for u in nf(b).split(\"\\n\") if u.strip()];E=[f\"RED: {f}: the ollama family states no token-denominated estimate (N tokens, or X% of tokens)\" for f in fs if not any(tok(u) for u in U(fam(f,\"ollama\")))]+[f\"RED: {f}: the ollama family states no latency-denominated estimate (N ms/s, or X% latency)\" for f in fs if not any(lat(u) for u in U(fam(f,\"ollama\")))];OKMSG=\"OK: ollama estimates quantified in \"+\", \".join(fs);print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "AC2 BUILD emits a todo: for every family whose verdict is BUILD, its section references an emitted inbox record .evolve/inbox/<name>.json (found anywhere under .evolve/inbox/) that parses with a task id (not token-frontier-watch), a numeric weight in (0,1] and a non-empty acceptance[] of non-empty strings; DEFER verdicts need no record"
    max_if_missing: 6
    evidence: "python3 -c 'import re,glob,sys,json;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));fs or sys.exit(\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\");nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);S=lambda t: [(s.split(\"\\n\",1)[0].lower(),s) for s in re.split(r\"(?m)^(?=##[ \\t])\",nf(t)) if re.match(r\"##[ \\t]\",s)];FAM={\"ollama\":lambda h: \"ollama\" in h and \"latent\" not in h,\"latent\":lambda h: \"latent\" in h and \"ollama\" not in h};fam=lambda f,k: \"\\n\".join(s for h,s in S(open(f).read()) if FAM[k](h));V=lambda b: re.findall(r\"(?im)^[ \\t>*_|-]*verdict[ \\t*_]*[:|][ \\t*_]*([^\\n]*)\",b);ok=lambda r: bool(re.match(r\"(BUILD|DEFER)\\b\",r)) and not re.match(r\"(BUILD|DEFER)[ \\t*_]*(/|or\\b|and\\b|\\|)[ \\t*_]*(BUILD|DEFER)\",r,re.I);INB=r\"\\.evolve/inbox/(?:[\\w-]+/)*([\\w.:-]+\\.json)\";recs=lambda b: [p for n in re.findall(INB,b) for p in glob.glob(\".evolve/inbox/**/\"+n,recursive=True)[:1]];good=lambda d: isinstance(d,dict) and isinstance(d.get(\"id\"),str) and bool(re.match(r\"^[a-z0-9][a-z0-9-]*$\",d[\"id\"])) and d[\"id\"]!=\"token-frontier-watch\" and type(d.get(\"weight\")) in (int,float) and 0<d[\"weight\"]<=1 and isinstance(d.get(\"acceptance\"),list) and len(d[\"acceptance\"])>0 and all(isinstance(a,str) and a.strip() for a in d[\"acceptance\"]);bk=lambda f: [k for k in FAM for v in [V(fam(f,k))] if len(v)==1 and re.match(r\"BUILD\\b\",v[0])];E=[f\"RED: {f}: the {k} verdict is BUILD but its section references no emitted inbox record (.evolve/inbox/<ts>-<id>.json) with an id, a weight in (0,1] and non-empty acceptance[]\" for f in fs for k in bk(f) if not any(good(json.load(open(p))) for p in recs(fam(f,k)))];OKMSG=\"OK: BUILD verdicts backed by inbox records: \"+str(sum(len(bk(f)) for f in fs));print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "Deliverable coherence: solutions/token-frontier-watch/recommendation.md links an existing frontier-watch dossier (bare repo path or relative markdown link), so the solution package the kernel floor judges and the dossier AC1 names are one deliverable, not two drifting documents"
    max_if_missing: 4
    evidence: "python3 -c 'import re,glob,sys,os;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);URL=r\"https?://[^\\s)>\\]`\\\"]+\";ARX=r\"(?<![\\d.])\\d{4}\\.\\d{4,5}(?:v\\d+)?(?![\\d.])\";BARE=r\"(?<![\\w/.-])((?:docs|go|solutions|knowledge-base)/[\\w./-]*?\\.(?:md|go|json|ya?ml))(?![\\w/-])\";LNK=r\"\\]\\((?!https?:|mailto:)([^)\\s#]+)\";rs=lambda f,p: os.path.normpath(os.path.join(os.path.dirname(f),p));refs=lambda f,b: [(p,p) for p in re.findall(BARE,re.sub(URL,\"\",b))]+[(p,rs(f,p)) for p in re.findall(LNK,b)];R=\"solutions/token-frontier-watch/recommendation.md\";os.path.isfile(R) or sys.exit(\"RED: \"+R+\" missing\");b=nf(open(R).read());tg={os.path.normpath(p) for p in re.findall(BARE,re.sub(URL,\"\",b))}|{rs(R,p) for p in re.findall(LNK,b)};E=[] if [f for f in fs if os.path.normpath(f) in tg] else [\"RED: \"+R+\" links no existing dossier (have: \"+(\", \".join(fs) or \"none\")+\")\"];OKMSG=\"OK: \"+R+\" links the dossier\";print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "H1 size-matched latency counterfactual: the ollama latency counterfactual for the 7,464-token triage prompt is derived from the ollama PR #17953 rows that bracket that size (the dossier's ollama family and assumptions-and-evidence.md each quote a lower row 3,806/5,181 tok and an upper row 9,932/19,805 tok and state a 3B cold prefill in 0.85-1.05 s and a restore saving in 0.68-0.83 s at 3B or 1.81-2.21 s at 8B), OR it carries a numbered assumption that assumptions-and-evidence.md defines as a prompt-length extrapolation labelled an upper bound; no unit of the dossier, the evidence file, the recommendation or any option states the withdrawn 30,643-row products (1.7/4.5 s cold, 1.5/4.0 s saved, 1.2-3.1% of latency) without that tag; and every A<n> they reference is defined"
    max_if_missing: 5
    evidence: "python3 -c 'import re,glob,sys,os;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));fs or sys.exit(\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\");nf=lambda t: re.sub(r\"(?ms)^[ \\t]*(`{3,}|~{3,}).*?^[ \\t]*\\1[^\\n]*$\",\"\",t);S=lambda t: [(s.split(\"\\n\",1)[0].lower(),s) for s in re.split(r\"(?m)^(?=##[ \\t])\",nf(t)) if re.match(r\"##[ \\t]\",s)];FAM={\"ollama\":lambda h: \"ollama\" in h and \"latent\" not in h,\"latent\":lambda h: \"latent\" in h and \"ollama\" not in h};fam=lambda f,k: \"\\n\".join(s for h,s in S(open(f).read()) if FAM[k](h));SOL=\"solutions/token-frontier-watch\";EV=SOL+\"/assumptions-and-evidence.md\";PRX=r\"pull/17953|PR #17953\";UN=lambda t: [re.sub(r\"\\s+\",\" \",u).strip() for u in re.split(r\"\\n[ \\t]*\\n|\\n(?=[ \\t]*(?:[-*+]|\\d+[.)])[ \\t]|[ \\t]*[|#])\",nf(t)) if u.strip()];NUM=r\"(?<![\\w.,])(\\d[\\d,]*(?:\\.\\d+)?)\";SU=r\"\\s*(ms|s|secs?|seconds?)\\b\";RG=r\"\\s*(?:[–—-]|to)\\s*\\d[\\d,]*(?:\\.\\d+)?\";fl=lambda a: float(a.replace(\",\",\"\"));sec=lambda u: [fl(a)/(1000 if m.lower()==\"ms\" else 1) for a,m in re.findall(NUM+SU,u,re.I)+re.findall(NUM+RG+SU,u,re.I)];pc=lambda u: [fl(a) for a in re.findall(NUM+r\"\\s*%\",u)+re.findall(NUM+r\"\\s*%?\"+RG+r\"\\s*%\",u)];STS=lambda v: round(v,1) in (1.7,4.5,1.5,4.0) or round(v,2) in (1.68,4.48,1.49,3.98);STP=lambda v: round(v,1) in (1.2,3.1) or round(v,2) in (1.15,3.07);LATW=r\"latency|dispatch|wall|ttft|prefill|phase\";ASEC=\"\\n\".join(s for h,s in S(open(EV).read()) if h.startswith(\"## assumption\")) if os.path.isfile(EV) else \"\";AD=dict(re.findall(r\"(?ms)^[ \\t]*[-*+][ \\t]+\\*{0,2}(A\\d{1,2})\\b\\*{0,2}(.*?)(?=^[ \\t]*[-*+][ \\t]+\\*{0,2}A\\d{1,2}\\b|\\Z)\",ASEC));QA={a for a,d in AD.items() if re.search(r\"length|size\",d,re.I) and re.search(r\"extrapolat\",d,re.I) and re.search(r\"upper[- ]bound|conservative|overstat\",d,re.I)};tg=lambda u: set(re.findall(r\"(?<![\\w-])A\\d{1,2}\\b\",u));rd=lambda p: open(p).read();DS=[f for f in fs if re.search(PRX,fam(f,\"ollama\"))];SF=[p for p in [EV,SOL+\"/recommendation.md\"]+sorted(glob.glob(SOL+\"/options/*.md\")) if os.path.isfile(p)];bad=lambda u: not (tg(u)&QA) and (any(STS(v) for v in sec(u)) or (bool(re.search(LATW,u,re.I)) and any(STP(v) for v in pc(u))));SRC=(0.09,0.121,0.239,0.48,0.765,0.431,0.604,1.348,3.472,6.891);vals=lambda b: [v for u in UN(b) for v in sec(u) if not any(abs(v-x)<0.0005 for x in SRC)];cold=lambda b: any(0.85<=v<=1.05 for v in vals(b));save=lambda b: any(0.68<=v<=0.83 or 1.81<=v<=2.21 for v in vals(b));brk=lambda b: bool(re.search(r\"(?<![\\d,])(?:3,?806|5,?181)(?![\\d,])\",b)) and bool(re.search(r\"(?<![\\d,])(?:9,?932|19,?805)(?![\\d,])\",b));okA=lambda b: cold(b) and save(b) and brk(b);okB=lambda b: bool(tg(nf(b))&QA);E=([f\"RED: {EV} missing\"] if not os.path.isfile(EV) else [])+([] if DS else [\"RED: no dossier ollama family cites ollama PR #17953 (https://github.com/ollama/ollama/pull/17953), the measured prefill source the latency counterfactual rests on\"])+[f\"RED: {p}: states a 30,643-token-row latency product for the 7,464-token prompt (1.7/4.5 s cold, 1.5/4.0 s saved, 1.2-3.1%) with no tag of a length-extrapolation upper-bound assumption: {u[:110]!r}\" for p in DS+SF for u in UN(rd(p)) if bad(u)]+[f\"RED: {n}: the latency counterfactual is neither size-matched to PR #17953 (3B cold 0.85-1.05 s, saving 0.68-0.83 s at 3B or 1.81-2.21 s at 8B, bracket rows 5,181/9,932 tok quoted) nor tagged with an assumption that {EV} defines as a prompt-length extrapolation labelled an upper bound\" for n,b in [(f+\" ollama family\",fam(f,\"ollama\")) for f in DS]+[(EV,rd(EV)) for p in SF if p==EV] if not (okA(b) or okB(b))]+[f\"RED: {p}: references {a}, which the ## Assumptions of {EV} does not define\" for p in DS+SF for a in sorted(tg(nf(rd(p)))-set(AD))];OKMSG=\"OK: latency counterfactual size-matched or labelled in \"+\", \".join(DS+SF);print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
  - criterion: "L1 recorded telemetry query: each dossier records the query behind its measured lane share as a fenced code block that reads llm-calls.ndjson and names the ollama lane, the block parses (bash -n for sh/bash/shell/zsh/console/unlabelled fences, compile() for python), and any prose reference to a query above follows the first such block"
    max_if_missing: 8
    evidence: "python3 -c 'import re,glob,sys,subprocess;D=\"docs/research/token-optimization-2026\";fs=sorted(glob.glob(D+\"/frontier-watch-2[0-9][0-9][0-9]-[01][0-9]-[0-3][0-9].md\"));fs or sys.exit(\"RED: no dossier \"+D+\"/frontier-watch-<YYYY-MM-DD>.md\");FEN=r\"(?ms)^[ \\t]*(`{3,}|~{3,})[ \\t]*([\\w+-]*)[^\\n]*\\n(.*?)^[ \\t]*\\1[ \\t]*$\";QF=lambda t: [(m.start(),m.group(2).lower(),m.group(3)) for m in re.finditer(FEN,t) if \"llm-calls.ndjson\" in m.group(3) and \"ollama\" in m.group(3)];SH=(\"\",\"sh\",\"bash\",\"shell\",\"zsh\",\"console\");PY=(\"python\",\"python3\",\"py\");run=lambda a,c: subprocess.run(a,input=c,capture_output=True,text=True).returncode==0;parses=lambda l,c: run([\"bash\",\"-n\"],c) if l in SH else (run([sys.executable,\"-c\",\"import sys;compile(sys.stdin.read(),\\\"q\\\",\\\"exec\\\")\"],c) if l in PY else True);above=lambda t,Q: [m.start() for m in re.finditer(r\"(?i)\\bquer(?:y|ies)\\s+above\\b\",t) if not Q or m.start()<Q[0][0]];rd=lambda p: open(p).read();E=[f\"RED: {f}: records no fenced query over llm-calls.ndjson naming the ollama lane (the measured lane share must be one reproducible command)\" for f in fs if not QF(rd(f))]+[f\"RED: {f}: refers to a query above (offset {p}) but no recorded llm-calls.ndjson query precedes it\" for f in fs for t in [rd(f)] for p in above(t,QF(t))]+[f\"RED: {f}: the recorded {l!r} fence query does not parse\" for f in fs for s,l,c in QF(rd(f)) if not parses(l,c)];OKMSG=\"OK: telemetry query recorded in \"+\", \".join(fs);print(\"\\n\".join(E) or OKMSG);sys.exit(1 if E else 0)'"
---

# Eval: token-frontier-watch (document task — research dossier)

> Pins the frontier-watch re-check of the two technique families the 2026-07-05 token-optimization
> survey classified "track, don't build": (a) soft-prompt/KV compression on the self-hosted ollama
> lane, and (b) latent inter-agent channels / compressed agent language across the closed CLI fleet
> (inbox `token-frontier-watch`, cycle 1704). The deliverable has two parts, and both are required:
>
> - **The dossier** (inbox AC1): `docs/research/token-optimization-2026/frontier-watch-<YYYY-MM-DD>.md`.
>   The inbox item, written 2026-07-06, names `knowledge-base/research/token-optimization-2026/`. That
>   package moved to `docs/research/token-optimization-2026/` on 2026-08-05 (#410, commit `82629a2f`).
>   `knowledge-base/README.md` now declares `knowledge-base/` a runtime write surface where no
>   documentation goes. Cap 2 therefore pins the package's current home and rejects a copy under
>   `knowledge-base/`.
> - **The solution package** `solutions/token-frontier-watch/` (the ADR-0099 document contract). The
>   cycle is `deliverable_kind: document`, so the kernel's build handoff floor
>   (`go/internal/core/solution_floor.go`) and the audit's `solution-contract` gate
>   (`go/internal/phases/audit/gates.go`) both run `solutioncheck` against it, whatever the inbox names.
>   Its options are the candidate strategies for the watch (e.g. DEFER-and-re-check vs BUILD a spike).
>   Cap 6 binds it to the dossier so the two documents cannot drift.
>
> A document cycle has no Go predicate inventory, so these caps are the RED contract. Each cap parses the
> documents: section-scoped verdicts, resolving citations, quantities with units, and inbox JSON. None of
> them is a keyword grep. Each cap is RED on the empty tree, GREEN on a well-formed calibration fixture
> under `bash` and `sh`, RED on fixtures built to break its criterion, and GREEN on alternate legal
> shapes (`.evolve/runs/cycle-1704/test-red-output.txt`).
>
> **Authoring shapes the caps parse.**
> - Each family is a `##` section whose heading names it: `ollama` for (a), `latent` for (b), never both.
>   Its `###` sub-sections belong to it.
> - Each family has exactly one verdict line: `**Verdict:** DEFER` or `**Verdict:** BUILD`, or a
>   `| Verdict | DEFER |` table row. The value is upper-case and comes first; prose may follow it.
> - A citation is an `https://` URL, an arXiv id (`2511.09149`), a repo path (`docs/…`, `go/…`,
>   `solutions/…`), or a relative markdown link. Every cited repo path and link must exist.
> - A BUILD verdict names its emitted record `.evolve/inbox/<ts>-<id>.json` in its own section.
> - The ollama latency counterfactual (cap 7) takes one of two shapes. **Size-matched:** the ollama family
>   and `assumptions-and-evidence.md` each quote the PR #17953 rows that bracket the ≈7,464-token triage
>   prompt (5,181 and 9,932 tokens), and state the 3B cold prefill (≈0.96 s) and the restore saving
>   (≈0.76 s at 3B, or ≈2.0 s at 8B). **Labelled bound:** every unit that states a figure built on the
>   30,643-token row carries a numbered assumption, `(A<n>)`. That assumption is a
>   `- **A<n>** — …` bullet under `## Assumptions`, and its text names the prompt-length extrapolation
>   and calls the result an upper bound. Every `A<n>` referenced anywhere in the package is defined there.
> - The lane-share telemetry query (cap 8) is a fenced block (`sh`, `bash`, `console`, `python`, or
>   unlabelled) that reads `llm-calls.ndjson` and names the ollama lane. It must parse, and it must come
>   before any prose that refers to "the query above".

> **Audit round 2 (cycle 1704).** Caps 1–6 were green and the deliverable still FAILed on **H1** (HIGH).
> The ollama latency counterfactual applied the per-token rate of PR #17953's largest row (30,643 tokens,
> 0.225 ms/tok, 89% cut) to a 7,464-token prompt. That source's own rows at 5,181 and 9,932 tokens bracket
> the prompt: interpolated, they give ≈0.96 s cold and a ≈79% cut, so every downstream figure was
> ≈1.75–2× high. The extrapolation was also unlabelled, although the dossier said "the assumptions are
> labelled". Cap 4 checked that a quantity with a unit exists, not that it follows from its citation.
> Cap 7 closes that gap. **L1** (LOW) left "the telemetry query above" pointing at nothing, and cap 8 pins
> the recorded query. The explanation document's Limitations had the same root cause. It is run-specific,
> so it is graded cycle-scoped (`.evolve/runs/cycle-1704/tdd-caps/h1-explain-check.sh`), not by a permanent cap.

## Code Graders (bash commands that must exit 0)
- `[code]` `cd go && go run ./cmd/evolve solution check token-frontier-watch --project-root ..`

## Acceptance Checks
- `[code]` `ls docs/research/token-optimization-2026/frontier-watch-2*.md >/dev/null 2>&1 && ! ls knowledge-base/research/token-optimization-2026/frontier-watch-*.md >/dev/null 2>&1`

## Model-Based Checks
- `[model]` Rubric: "The ollama-lane verdict rests on the actual serving surface: what ollama exposes today for prompt/KV reuse (keep_alive residency, the deprecated `context` array, any server-side prompt/KV cache), versus what a soft-prompt/KV-compression technique (500xCompressor, gist tokens) needs. The token and latency estimates are derived from the lane's MEASURED dispatch share and the phases actually routed to ollama, with the arithmetic shown. They are not asserted" — threshold: >= 70
- `[model]` Rubric: "The latent-channel re-survey covers the named lineage (hidden-state exchange 2511.09149, HyLaT 2605.25421, EcoLANG 2505.06904) plus anything newer, and checks vendor APIs across the fleet's CLIs (claude, codex, agy) for a productized compressed-context or latent handoff. The verdict follows from that evidence, and a corpus-only check says so" — threshold: >= 70
- `[model]` Rubric: "Each DEFER verdict names the observable trigger that would flip it to BUILD (e.g. an ollama release exposing KV export/import, or a vendor API accepting compressed or latent context), so the next re-check has a concrete test. Any BUILD verdict names its emitted inbox record, and that record's acceptance is concrete" — threshold: >= 60
- `[model]` Rubric: "solutions/token-frontier-watch/ options are genuinely distinct strategies for the watch, not one lever restated. The recommendation's per-family verdicts match the dossier's, and its evidence entries trace to the dossier's citations" — threshold: >= 70

## Thresholds
- All `[code]` checks: pass@1 = 1.0
- `[model]` rubrics: each at or above its threshold

## Score Cap Rationale

| Pattern | Criterion | max_if_missing | Evidence |
|---|---|---|---|
| contract-floor | ADR-0099 document contract holds for `solutions/token-frontier-watch/` (kernel floor + audit gate) | 3/10 | `evolve solution check token-frontier-watch` |
| dossier-verdicts | dossier at the package's current home; one ollama and one latent section, each with exactly one BUILD/DEFER verdict (AC1) | 4/10 | python: `##` sections × verdict lines |
| citations-resolve | every cited repo path/link exists; each family has ≥2 distinct citations, ≥1 external (AC1) | 5/10 | python: URL/arXiv/path/link extraction + `os.path.exists` |
| ollama-quantified | the ollama family states a token AND a latency estimate (AC1) | 5/10 | python: quantity-with-unit per line |
| build-emits-todo | every BUILD family references a valid inbox record: id, weight in (0,1], non-empty acceptance[] (AC2) | 6/10 | python: section refs × `.evolve/inbox/**` JSON |
| deliverable-coherence | the recommendation links the dossier | 4/10 | python: link resolution |
| size-matched-counterfactual | the ollama latency counterfactual follows from the PR #17953 rows that bracket the prompt size, or is a labelled upper-bound assumption; no untagged 30,643-row product at any site (audit H1) | 5/10 | python: per-unit seconds/percent extraction × the source table × `## Assumptions` definitions |
| telemetry-query-recorded | the lane-share query over `llm-calls.ndjson` is recorded, parses, and precedes "the query above" (audit L1) | 8/10 | python: fence extraction + `bash -n` / `compile()` |

AC3 ("No source-code changes (research-only task; docs tree only)") is a property of this cycle's diff, not of
the deliverable. It is deliberately NOT a permanent score cap: a replayed cap would read a later cycle's own
diff, and would cap every code cycle. The auditor grades it from the cycle workspace
(`.evolve/runs/cycle-1704/tdd-caps/ac3-docs-only.sh`).
