# Eval: builder-discovery-appendix
## Graders
- `grep -c "Discovery Scan" agents/evolve-builder.md | grep -qv "^0$"`
- `grep -c "## Discoveries" agents/evolve-builder.md | grep -qv "^0$"`
- `grep -c "latent-bug" agents/evolve-builder.md | grep -qv "^0$"`
- `grep -c "Proposal Extraction" skills/evolve-loop/phase5-learn.md | grep -qv "^0$"`
- `grep -c "Discoveries" skills/evolve-loop/phase5-learn.md | grep -qv "^0$"`
