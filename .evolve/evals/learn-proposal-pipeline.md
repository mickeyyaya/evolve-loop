# Eval: learn-proposal-pipeline
## Graders
- `grep -c "Proposal Extraction" skills/evolve-loop/phase5-learn.md | grep -qv "^0$"`
- `grep -c "builder-discovery" skills/evolve-loop/phase5-learn.md | grep -qv "^0$"`
- `grep -c "proposals" agents/evolve-scout.md | grep -qv "^0$"`
- `grep -c "Proposal Pipeline" skills/evolve-loop/SKILL.md | grep -qv "^0$"`
